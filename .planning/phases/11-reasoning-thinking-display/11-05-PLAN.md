---
phase: 11-reasoning-thinking-display
plan: 05
type: execute
wave: 3
depends_on: [11-01, 11-03, 11-04]
files_modified:
  - libs/ui/src/lib/message-bubble/message-bubble.ts
  - libs/ui/src/lib/message-bubble/message-bubble.html
  - libs/ui/src/lib/message-bubble/message-bubble.stories.ts
  - apps/frontend/src/app/chat/chat.ts
  - apps/frontend/src/app/chat/chat.html
autonomous: true
requirements:
  - REQ-1
  - REQ-5
  - REQ-6
  - REQ-7
  - REQ-9

must_haves:
  truths:
    - "`MessageBubble` imports `ReasoningBlock` and renders `<app-reasoning-block>` inside the assistant branch ONLY, ABOVE the existing markdown/content slot, gated on `message.reasoning` truthy"
    - "Persisted-assistant rendering: `<app-reasoning-block [reasoning]=\"message.reasoning ?? ''\" [streaming]=\"false\" [durationMs]=\"message.reasoningDurationMs\" [autoCollapsed]=\"true\" />`"
    - "Tool-call rendering (the existing `tool` role branch and any splice logic) is NOT modified — Req 9 invariant"
    - "Streaming assistant bubble in `chat.html` renders a separate `<app-reasoning-block>` above the in-progress markdown, bound to `streamingReasoning()`, `streaming=true`, `durationMs` undefined, `autoCollapsed=reasoningCollapsed()`"
    - "ChatComponent `imports` array includes `ReasoningBlock`"
    - "Model selector dropdown shows `DeepSeek R1 (free, reasoning)` as a NEW option; default `selectedModel = 'openai/gpt-4o-mini'` unchanged"
    - "`chat.ts` `models` array gains `{ id: 'deepseek/deepseek-r1:free', label: 'DeepSeek R1 (free, reasoning)' }` as the last entry"
    - "`message-bubble.stories.ts` gets a new story 'WithReasoning' demonstrating the assistant bubble with reasoning collapsed by default"
  artifacts:
    - path: "libs/ui/src/lib/message-bubble/message-bubble.html"
      provides: "Assistant bubble that hosts ReasoningBlock above markdown"
---

<objective>
Compose `ReasoningBlock` into the assistant `MessageBubble` (persisted-message view) and into the live streaming bubble (in `chat.html`). Add the DeepSeek R1 reasoning model to the selector dropdown. After this plan, the end-to-end UX is functional: user picks the reasoning model → reasoning streams visibly → collapses on first content token → persists across sidebar navigation.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-reasoning-thinking-display/11-CONTEXT.md
@.planning/phases/11-reasoning-thinking-display/11-RESEARCH.md
@.planning/phases/11-reasoning-thinking-display/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire ReasoningBlock into MessageBubble assistant branch</name>
  <files>
    libs/ui/src/lib/message-bubble/message-bubble.ts
    libs/ui/src/lib/message-bubble/message-bubble.html
  </files>

  <read_first>
    libs/ui/src/lib/message-bubble/message-bubble.ts
    libs/ui/src/lib/message-bubble/message-bubble.html
    libs/ui/src/lib/reasoning-block/reasoning-block.ts
  </read_first>

  <behavior>
    - `MessageBubble` `imports` array includes `ReasoningBlock`
    - In the assistant branch of `message-bubble.html` (locate by `message.role === 'assistant'` block), an `<app-reasoning-block>` is rendered immediately INSIDE the bubble container `<div>`, BEFORE the `<markdown>` element (or the existing assistant text element if markdown is not used here)
    - Block is gated on `message.reasoning` truthy: `@if (message.reasoning) { <app-reasoning-block ... /> }`
    - Bindings: `[reasoning]="message.reasoning ?? ''"`, `[streaming]="false"`, `[durationMs]="message.reasoningDurationMs"`, `[autoCollapsed]="true"`
    - Tool-role branch (`message.role === 'tool'`) is NOT modified
    - Error-role branch is NOT modified
    - User-role branch is NOT modified
    - The host bubble's existing classes (background, padding, border, shadow) are unchanged — the reasoning block inherits them by virtue of nesting
  </behavior>

  <action>
  Edit `libs/ui/src/lib/message-bubble/message-bubble.ts`:
  - Add `import { ReasoningBlock } from '../reasoning-block/reasoning-block';` (or via the `@chatbot/ui` barrel if the project convention is for libraries-importing-from-themselves to use the relative path — match what is already used for sibling components)
  - Add `ReasoningBlock` to the component `imports` array

  Edit `libs/ui/src/lib/message-bubble/message-bubble.html`:
  - Locate the assistant branch (`@else if (message.role === 'assistant')` or the equivalent existing structure)
  - Inside the bubble container `<div>`, immediately before the markdown/text rendering, insert:
  ```html
  @if (message.reasoning) {
    <app-reasoning-block
      [reasoning]="message.reasoning ?? ''"
      [streaming]="false"
      [durationMs]="message.reasoningDurationMs"
      [autoCollapsed]="true">
    </app-reasoning-block>
  }
  ```
  </action>

  <verify>
    <automated>pnpm nx build ui --skip-nx-cache 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm nx build ui` exits 0
    - `pnpm nx test ui` exits 0
    - Tool-call bubble rendering path is untouched (verify by grep: tool-role branch line ranges unchanged compared to pre-edit `git diff`)
  </acceptance_criteria>

  <done>Persisted assistant messages with reasoning render the block above content.</done>
</task>

<task type="auto">
  <name>Task 2: Add WithReasoning story to message-bubble</name>
  <files>libs/ui/src/lib/message-bubble/message-bubble.stories.ts</files>

  <read_first>
    libs/ui/src/lib/message-bubble/message-bubble.stories.ts
    libs/ui/src/lib/reasoning-block/reasoning-block.stories.ts
  </read_first>

  <behavior>
    - One new story export `WithReasoning` with an assistant message whose `reasoning` is multi-line and `reasoningDurationMs: 4200`
    - Story renders the bubble with the reasoning block collapsed by default (`autoCollapsed: true` is hard-coded in the host template)
  </behavior>

  <action>
  Append a `WithReasoning` story export to `message-bubble.stories.ts` following the existing pattern in the file.
  </action>

  <verify>
    <automated>pnpm nx build ui --skip-nx-cache 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - Story export `WithReasoning` exists
    - UI build is green
  </acceptance_criteria>

  <done>Visual regression surface (Storybook) covers the integrated assistant bubble.</done>
</task>

<task type="auto">
  <name>Task 3: Add deepseek-r1 to model selector + streaming reasoning block in chat.html</name>
  <files>
    apps/frontend/src/app/chat/chat.ts
    apps/frontend/src/app/chat/chat.html
  </files>

  <read_first>
    apps/frontend/src/app/chat/chat.ts
    apps/frontend/src/app/chat/chat.html
    libs/ui/src/lib/reasoning-block/reasoning-block.ts
  </read_first>

  <behavior>
    - `chat.ts` `imports` array on the `@Component` decorator gains `ReasoningBlock` (alongside the existing `MessageBubble`, etc.)
    - `chat.ts` `models` array gains exactly one new entry as the LAST item: `{ id: 'deepseek/deepseek-r1:free', label: 'DeepSeek R1 (free, reasoning)' }`
    - `selectedModel` initial value remains `'openai/gpt-4o-mini'`
    - `chat.html`: locate the in-progress streaming bubble (the part of the template that renders `streamingText()`). Immediately ABOVE the streaming text rendering, add:
      ```html
      @if (streamingReasoning().length > 0) {
        <app-reasoning-block
          [reasoning]="streamingReasoning()"
          [streaming]="true"
          [autoCollapsed]="reasoningCollapsed()">
        </app-reasoning-block>
      }
      ```
      Place this INSIDE the same container element that holds the streaming markdown/text — the block must visually sit above the answer, matching the persisted-message layout from Task 1
  </behavior>

  <action>
  Edit `apps/frontend/src/app/chat/chat.ts`:
  - Add `ReasoningBlock` to the `imports: [...]` array on the `@Component` decorator
  - Add `{ id: 'deepseek/deepseek-r1:free', label: 'DeepSeek R1 (free, reasoning)' }` as the last entry of the `readonly models = [...] as const;` array

  Edit `apps/frontend/src/app/chat/chat.html`:
  - Locate the streaming bubble block (where `streamingText()` is bound)
  - Insert the streaming reasoning block snippet above the streaming text, per the behavior above. Mirror the surrounding wrapper structure used for the persisted assistant bubble so the reasoning block sits inside the same visual container
  </action>

  <verify>
    <automated>pnpm nx build frontend --skip-nx-cache 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `pnpm nx build frontend` exits 0
    - `pnpm nx lint frontend` exits 0
    - With `pnpm nx serve frontend` running locally, the model selector dropdown shows `DeepSeek R1 (free, reasoning)` as the last option
    - Selecting it and sending a message shows a live reasoning block above the streaming answer
  </acceptance_criteria>

  <done>End-to-end UI integration complete; reasoning streams live and persists.</done>
</task>

</tasks>

<verification>
- All workspace `build`, `lint`, and `test` targets green: `pnpm nx run-many -t build,lint,test`
- Manual smoke (skipping the dev-server launch counts as "deferred to E2E plan 06"): selector shows the new model; sending a message with the reasoning model shows live reasoning streaming and a collapsed block on final answer
</verification>

<success_criteria>
- ReasoningBlock composed into both persisted and streaming assistant bubble surfaces
- Tool-call bubble rendering path unchanged
- DeepSeek R1 selectable; default model unchanged
- All builds + lint + unit tests green
</success_criteria>

<output>
Create `.planning/phases/11-reasoning-thinking-display/11-05-SUMMARY.md` when done
</output>
