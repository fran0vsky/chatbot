---
phase: 11-reasoning-thinking-display
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/shared-types/src/lib/chat.types.ts
autonomous: true
requirements:
  - REQ-3
  - REQ-4

must_haves:
  truths:
    - "`StreamEvent` union in `@org/shared-types` includes a new `StreamReasoningTokenEvent` variant with shape `{ type: 'reasoning_token'; text: string }`"
    - "`StreamDoneEvent` gains optional `reasoning?: string` and `reasoningDurationMs?: number`"
    - "`ChatMessage` gains optional `reasoning?: string` and `reasoningDurationMs?: number`"
    - "All existing exports (ChatRequest, ChatResponse, MessageRole, ToolCallRecord, ConversationSession, prior StreamEvent variants) remain unchanged"
    - "`pnpm nx build shared-types` succeeds with no errors"
  artifacts:
    - path: "libs/shared-types/src/lib/chat.types.ts"
      provides: "Extended StreamEvent union and ChatMessage with reasoning fields"
      exports: ["StreamReasoningTokenEvent", "StreamEvent", "StreamDoneEvent", "ChatMessage"]
---

<objective>
Extend the shared wire/persistence types so reasoning traces can flow across the SSE channel and persist on assistant messages. This is the foundation every other Phase 11 plan depends on — backend emission, frontend consumption, and history round-trip all use these types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-reasoning-thinking-display/11-SPEC.md
@.planning/phases/11-reasoning-thinking-display/11-CONTEXT.md
@.planning/phases/11-reasoning-thinking-display/11-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend chat.types.ts with reasoning fields</name>
  <files>libs/shared-types/src/lib/chat.types.ts</files>

  <read_first>
    libs/shared-types/src/lib/chat.types.ts
    libs/shared-types/src/index.ts
  </read_first>

  <behavior>
    - Existing exports remain unchanged in shape and order above the union definition
    - Add `StreamReasoningTokenEvent` interface with `type: 'reasoning_token'` literal and `text: string`
    - Add `reasoning?: string` and `reasoningDurationMs?: number` to `StreamDoneEvent`
    - Add `reasoning?: string` and `reasoningDurationMs?: number` to `ChatMessage`
    - Add `StreamReasoningTokenEvent` to the `StreamEvent` union
  </behavior>

  <action>
  Edit `libs/shared-types/src/lib/chat.types.ts`:

  1. Add optional `reasoning?: string` and `reasoningDurationMs?: number` fields to the existing `ChatMessage` interface (append after `toolResult?: string;`).

  2. Insert a new interface above the existing `StreamDoneEvent`:
  ```ts
  export interface StreamReasoningTokenEvent {
    type: 'reasoning_token';
    text: string;
  }
  ```

  3. Extend `StreamDoneEvent` with two optional fields:
  ```ts
  export interface StreamDoneEvent {
    type: 'done';
    response: string;
    toolCalls?: ToolCallRecord[];
    reasoning?: string;
    reasoningDurationMs?: number;
  }
  ```

  4. Add `StreamReasoningTokenEvent` to the `StreamEvent` union (place it after `StreamTokenEvent`).

  Do NOT modify `libs/shared-types/src/index.ts` — it already re-exports everything from chat.types.
  </action>

  <verify>
    <automated>pnpm nx build shared-types --skip-nx-cache 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - `chat.types.ts` contains `export interface StreamReasoningTokenEvent { type: 'reasoning_token'; text: string; }`
    - `chat.types.ts` `StreamDoneEvent` declares `reasoning?: string` and `reasoningDurationMs?: number`
    - `chat.types.ts` `ChatMessage` declares `reasoning?: string` and `reasoningDurationMs?: number`
    - `chat.types.ts` `StreamEvent` union includes `StreamReasoningTokenEvent`
    - `pnpm nx build shared-types` exits 0
  </acceptance_criteria>

  <done>Reasoning fields available across the monorepo; downstream plans can import and use them.</done>
</task>

</tasks>

<verification>
After this plan:
- `pnpm nx build shared-types` is green
- Backend and frontend can import `StreamReasoningTokenEvent` and reference `ChatMessage.reasoning` without TS errors
</verification>

<success_criteria>
- Five existing StreamEvent variants unchanged; one new variant added
- ChatMessage gains two optional reasoning fields
- shared-types build green
</success_criteria>

<output>
Create `.planning/phases/11-reasoning-thinking-display/11-01-SUMMARY.md` when done
</output>
