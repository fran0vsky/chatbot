---
phase: 11-reasoning-thinking-display
plan: 06
type: execute
wave: 4
depends_on: [11-02, 11-05]
files_modified:
  - apps/frontend-e2e/src/reasoning.spec.ts
autonomous: false
requirements:
  - REQ-1
  - REQ-7
  - REQ-9

must_haves:
  truths:
    - "New Playwright spec `apps/frontend-e2e/src/reasoning.spec.ts` covers three scenarios driven against a mocked or real backend (use the existing project's E2E pattern — check sibling spec files for how the backend is mocked or proxied)"
    - "Scenario A (zero-regression on default model): default model selected → send a message → no `[data-testid=\"reasoning-block\"]` ever appears in the DOM during or after streaming"
    - "Scenario B (live reasoning + auto-collapse): switch model to DeepSeek R1 (free, reasoning) → send a message → reasoning block appears expanded during stream → after the first content token, the block transitions to collapsed → clicking the toggle re-expands it"
    - "Scenario C (session persistence): with the reasoning model, send a message → wait for done → open the history sidebar, switch to a new chat, switch back → the prior assistant bubble still shows the reasoning block (collapsed)"
    - "Manual smoke checklist is recorded as the final task (no automation) — tool-call interleaving with reasoning model (Req 9) is in this checklist because the existing tools-prompt is non-deterministic"
    - "The Storybook story for `ReasoningBlock` must include a `data-testid=\"reasoning-block\"` selector on the root container (added as part of this plan if not added in 11-03) so Playwright assertions can target it deterministically"
---

<objective>
End-to-end coverage of the reasoning UX and a manual smoke checklist for the non-deterministic parts (real network + real model behavior). Locks in: no regression on the default model, live + auto-collapse behavior on the reasoning model, persistence round-trip via the sidebar.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/11-reasoning-thinking-display/11-VALIDATION.md
@.planning/phases/11-reasoning-thinking-display/11-UI-SPEC.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add data-testid to ReasoningBlock root</name>
  <files>libs/ui/src/lib/reasoning-block/reasoning-block.html</files>

  <read_first>
    libs/ui/src/lib/reasoning-block/reasoning-block.html
  </read_first>

  <behavior>
    - The root `<div>` of the reasoning block (the one with `border-l-2 ...`) carries `data-testid="reasoning-block"`
    - The toggle button carries `data-testid="reasoning-toggle"`
    - The body div (when expanded) carries `data-testid="reasoning-body"`
  </behavior>

  <action>
  Edit `libs/ui/src/lib/reasoning-block/reasoning-block.html` and add the three `data-testid` attributes to the three elements named above. Do not change any other markup or classes.
  </action>

  <verify>
    <automated>pnpm nx build ui --skip-nx-cache 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - The three test ids are present in the template
    - UI build green
  </acceptance_criteria>

  <done>Playwright has deterministic selectors.</done>
</task>

<task type="auto">
  <name>Task 2: Playwright spec for reasoning UX</name>
  <files>apps/frontend-e2e/src/reasoning.spec.ts</files>

  <read_first>
    apps/frontend-e2e/src
    apps/frontend-e2e/playwright.config.ts
  </read_first>

  <behavior>
    - File location and import style match an existing sibling spec (e.g. the streaming or tool-call E2E spec from Phase 9/10 if present)
    - The spec uses Playwright's request-routing / fetch-mocking to fabricate SSE responses so the test is deterministic and offline. If the project's existing E2E specs hit a live backend, mirror that pattern instead (read the sibling files first to decide)
    - Three test cases as described in the Truths section
    - All selectors use `data-testid` attributes set in Task 1 plus existing selectors on the model selector and input composer (check what test ids already exist by reading `model-selector.ts` and `input-composer.ts`)
  </behavior>

  <action>
  Read the existing `apps/frontend-e2e/src` directory to identify the established pattern (mocked backend vs real backend). Create `reasoning.spec.ts` that exercises the three scenarios from the Truths section using that pattern.

  If mocking SSE: use `page.route('**/agents/chat', async (route) => { ... })` to send a hand-crafted `text/event-stream` body containing `reasoning_token` frames followed by `token` frames followed by a `done` frame with `reasoning` + `reasoningDurationMs` populated.

  If using a real backend: gate the reasoning-model scenarios with a `test.skip(!process.env['OPENROUTER_API_KEY'])` so CI can run them only when the key is provided.
  </action>

  <verify>
    <automated>pnpm nx e2e frontend-e2e --skip-nx-cache 2>&1 | tail -30</automated>
  </verify>

  <acceptance_criteria>
    - Three new test cases discovered and (where backend is mocked) green
    - Default-model regression test passes (no reasoning block in DOM)
    - Auto-collapse test passes (block visible expanded during stream, collapsed after first content token, re-expandable via toggle click)
    - Persistence test passes (block survives sidebar navigation)
  </acceptance_criteria>

  <done>E2E coverage in place for the deterministic parts of the reasoning UX.</done>
</task>

<task type="manual">
  <name>Task 3: Manual smoke against live OpenRouter</name>
  <files></files>

  <read_first>
    .planning/phases/11-reasoning-thinking-display/11-VALIDATION.md
  </read_first>

  <behavior>
    Run a documented manual smoke against a live backend + OpenRouter to cover requirements that E2E can't deterministically verify.
  </behavior>

  <action>
  Boot `pnpm nx serve backend` and `pnpm nx serve frontend` locally with `OPENROUTER_API_KEY` set. Walk through this checklist and record outcomes in the SUMMARY file when this plan closes:

  1. Default model (`GPT-4o mini`): send "What is 2+2?" → no reasoning block at any point → ✅/❌
  2. Switch to `DeepSeek R1 (free, reasoning)`: send "Explain why the sky is blue, step by step." → reasoning block appears expanded → text streams in token-by-token → first answer-content token triggers collapse within ~1 frame → toggle re-expands and shows full trace → "Thought for Xs" label shows a sensible whole-second count → ✅/❌
  3. Tool-call interleaving (Req 9): with the reasoning model, send "What time is it in Tokyo? Reason briefly first." → if the model calls `get_current_time`, the tool-call bubble renders in its Phase-9 position → there is exactly one reasoning block on the assistant bubble → ✅/❌ (note: non-deterministic; rerun a few times if model doesn't call the tool)
  4. Persistence: after smoke #2, open the sidebar → start a new chat → switch back → reasoning block is still there, collapsed → toggle expands the original trace → ✅/❌
  5. Night/day theme: toggle theme during smoke #2 → muted contrast is preserved in both themes (Req 6) → ✅/❌
  6. Two sequential turns on reasoning model (RESEARCH Pitfall 2 / 5 watch): without switching model, send a second message → 400 from OpenRouter? If yes, file a follow-up task to strip `additional_kwargs.reasoning_details` from persisted AIMessages before MemorySaver checkpointing → ✅/❌
  7. Model switch mid-thread: with the same thread, switch to GPT-4o mini and send a third message → 400 from OpenRouter? Same follow-up trigger as smoke #6 → ✅/❌
  </action>

  <acceptance_criteria>
    - All seven items recorded in the SUMMARY with ✅/❌ outcomes
    - Any ❌ items become open follow-ups (recorded as Phase 11.x or backlog items, not silently dropped)
  </acceptance_criteria>

  <done>Manual coverage of non-deterministic and visual requirements captured.</done>
</task>

</tasks>

<verification>
- `pnpm nx e2e frontend-e2e` green (or only the env-gated scenarios skipped)
- Manual smoke checklist recorded with outcomes
- Phase 11 acceptance criteria from 11-SPEC.md §Acceptance Criteria are individually checkable against the artifacts produced across plans 01-06
</verification>

<success_criteria>
- E2E spec covers zero-regression, auto-collapse, and persistence scenarios
- Manual smoke covers visual contract, tool-call interleaving with reasoning, and the multi-turn / model-switch pitfalls
- Any pitfall regressions discovered during smoke become follow-up phases — not silent failures
</success_criteria>

<output>
Create `.planning/phases/11-reasoning-thinking-display/11-06-SUMMARY.md` when done, including the manual smoke checklist outcomes verbatim
</output>
