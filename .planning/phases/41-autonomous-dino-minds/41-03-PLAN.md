---
phase: 41-autonomous-dino-minds
plan: 03
type: execute
wave: 3
depends_on: ["41-02"]
files_modified:
  - libs/shared-types/src/lib/group.types.ts
  - apps/frontend/src/app/chat/groupchat.service.ts
  - .planning/phases/41-autonomous-dino-minds/41-HUMAN-UAT.md
requirements:
  - GRP3-03
must_haves:
  truths:
    - "The frontend group transcript renders the v3 stream unchanged: an initial empty `plan` creates no premature slots, `dino_token`/`dino_done` build/settle dino bubbles in arrival order, `reaction` pins an emoji chip on its target message, and `group_done` ends the turn — verified by a frontend unit test against a scripted v3 event sequence"
    - "A multi-round turn renders top-to-bottom with attribution: later dino bubbles appear after earlier ones in the same turn, and intent chips / reply stubs still display from dino_done's intent/replyTo* (GRP3-03)"
    - "The vestigial Phase 35 orchestrator-plan types (DinoTurnDecision, GroupOrchestratorPlan) carry a doc comment marking them v3-legacy (only the empty `plan` event still uses GroupOrchestratorPlan); DinoDecision is the live decision contract"
    - "The v3 cost ceiling is documented in code where the engine constants live and/or a short runbook note: MAX_GROUP_DINOS=4, MAX_ROUNDS=3, MAX_ANSWERS_PER_DINO=2, MAX_TOTAL_ANSWERS=8, with the worst-case call count, replacing the Phase 37 governor budget (Success Criterion #4)"
    - "A HUMAN-UAT.md exists with concrete, observable acceptance steps proving GRP3-01/02/03 on the live multi-dino chat"
    - "Full workspace lint+build+test are green for both @org/backend and @org/frontend"
  artifacts:
    - path: "apps/frontend/src/app/chat/groupchat.service.ts"
      provides: "Confirmed/adjusted v3 event handling (no premature slot creation from empty plan; reactions + intent chips intact) + a unit test for the v3 sequence"
      exports: ["GroupchatService"]
    - path: "libs/shared-types/src/lib/group.types.ts"
      provides: "Doc comments marking DinoTurnDecision/GroupOrchestratorPlan as v3-legacy; DinoDecision as the live contract"
      exports: []
    - path: ".planning/phases/41-autonomous-dino-minds/41-HUMAN-UAT.md"
      provides: "Observable UAT script for the autonomous multi-round group engine"
      exports: []
  key_links:
    - from: "groupchat.service.ts applyEvent"
      to: "v3 GroupStreamEvent sequence from Plan 02 streamGroup"
      via: "plan(empty)→dino_token/dino_done(with intent/replyTo)→reaction→group_done; slots created dynamically on dino_token"
      pattern: "existing applyEvent routing (L273-322) — confirm it needs no change for empty plan + autonomous reactions"
---

<objective>
Close the phase: confirm the frontend renders the v3 autonomous stream correctly (multi-round, mixed
answers/reactions/silences, attribution + intent chips) with a unit test, mark the now-legacy
orchestrator-plan types, document the new cost ceiling that replaces the governor budget, and write the
human UAT script that proves GRP3-01/02/03 end-to-end. Run the full workspace gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/41-autonomous-dino-minds/41-CONTEXT.md
@.planning/phases/41-autonomous-dino-minds/41-02-PLAN.md
Frontend consumer: [apps/frontend/src/app/chat/groupchat.service.ts](../../../apps/frontend/src/app/chat/groupchat.service.ts)
- `applyEvent` (L273-322): `plan` pre-creates Round-1 answerer slots — but v3 sends `round1: []`, so it
  returns early and creates nothing (slots come from `dino_token` via `appendToken`/`findOpenSlot`).
  `reaction` → `attachReaction` (L415-424) matches by `id` or `serverMessageId`. `dino_done` → `finalizeDino`
  carries `intent`/`replyToMessageId`/`replyToAgentId`/`confidence` (L297-305). Confirm all paths still hold;
  add a unit test if none covers the multi-round/empty-plan case.
Shared contracts: [libs/shared-types/src/lib/group.types.ts](../../../libs/shared-types/src/lib/group.types.ts)
- `DinoTurnDecision` (L11-24), `GroupOrchestratorPlan` (L26-32) are Phase 35 — now legacy except the empty
  `plan` event. `DinoDecision` (added in Plan 01) is the live contract. Add doc comments only — do NOT
  delete the plan types (the `plan` event + frontend still reference `GroupOrchestratorPlan`).
Engine constants (Plan 02): cost ceiling lives in [apps/backend/src/app/agents/group/decision.ts] — document there.
UI rendering of intents/reply stubs: [libs/ui/src/lib/group-response/group-response.ts] (intent chips/stubs from Phase 37 — read to confirm it consumes `intent`/`replyTo*`, no change expected).
Frontend rules: [apps/frontend/CLAUDE.md](../../../apps/frontend/CLAUDE.md) — standalone OnPush, Tailwind, types from `@org/shared-types`, no `console.log`.
</context>

<design_decisions>
- **Frontend is expected to need little/no behavior change (D-01):** the v3 stream reuses the exact
  `GroupStreamEvent` shapes. The task is primarily to PROVE it with a test against a scripted v3 sequence
  (empty plan → two answers in order → a reaction on the second → group_done) and only patch
  `groupchat.service.ts` if the test surfaces a real gap (e.g. an assumption that `plan.round1` is non-empty).
- **Legacy type annotation (D-02):** Add a comment block above `DinoTurnDecision`/`GroupOrchestratorPlan`:
  "Phase 35 orchestrator-plan contract — superseded by the autonomous per-dino `DinoDecision` (Phase 41).
  Retained because the engine still emits an empty `plan` event for SSE contract stability."
- **Cost-ceiling doc (D-03):** Expand the comment on the Plan 01 constants in `decision.ts` to state the
  worst-case (4 dinos × 3 rounds decision calls + ≤8 answer calls) and that this replaces the Phase 37
  `TurnBudget`. Optionally add a one-paragraph note to the group-chat section of the project README/runbook
  if one exists; if not, the code comment suffices.
- **UAT (D-04):** Write `41-HUMAN-UAT.md` with concrete steps (below) and PENDING checkboxes, matching the
  style of prior phases' HUMAN-UAT files.
- **Out of scope:** any new UI affordance for reactions config (Phase 43); engine logic (Plan 02).
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Prove (and patch if needed) v3 frontend rendering with a unit test</name>
  <files>apps/frontend/src/app/chat/groupchat.service.ts</files>
  <read_first>
    apps/frontend/src/app/chat/groupchat.service.ts
    libs/shared-types/src/lib/group.types.ts
  </read_first>
  <behavior>
    - Add a unit test (co-located spec) that drives `applyEvent` with a scripted v3 sequence:
      `plan{round1:[],round2:[]}` → `dino_token`(A) → `dino_done`(A, intent set) → `dino_token`(B) →
      `dino_done`(B, replyToAgentId=A) → `reaction`(C targets B's messageId) → `group_done`, and asserts
      the resulting `messages()` transcript is A then B (in order), B carries `replyToAgentId`, B has C's
      reaction chip, and streaming is false at the end.
    - Only modify `groupchat.service.ts` if an assertion fails because of a real v3 mismatch (e.g. empty
      `plan` mishandled). Keep changes minimal and within the existing OnPush/signal patterns.
  </behavior>
  <action>
  Add a `groupchat.service.spec.ts` (or extend the existing one) covering the scripted v3 event sequence
  above. If the empty `plan` path or autonomous reactions reveal a real bug, patch `applyEvent`/helpers
  minimally; otherwise leave the service unchanged and let the test stand as the regression guard.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && npx nx test @org/frontend --skip-nx-cache 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - A frontend test drives the v3 event sequence (empty plan + ordered answers + a targeted reaction + group_done)
    - It asserts transcript order (A before B), B's reply metadata, the reaction chip on B, and streaming=false at end
    - Frontend test target passes
  </acceptance_criteria>
  <done>The v3 stream is proven to render correctly, with a regression test.</done>
</task>

<task type="auto">
  <name>Task 2: Mark legacy plan types and document the cost ceiling</name>
  <files>
    libs/shared-types/src/lib/group.types.ts
    apps/backend/src/app/agents/group/decision.ts
  </files>
  <read_first>
    libs/shared-types/src/lib/group.types.ts
    apps/backend/src/app/agents/group/decision.ts
  </read_first>
  <behavior>
    - Add the D-02 legacy comment above `DinoTurnDecision`/`GroupOrchestratorPlan`; do not delete them.
    - Expand the cost-ceiling comment on the `decision.ts` constants per D-03 (worst-case call count;
      "replaces the Phase 37 governor TurnBudget").
  </behavior>
  <action>
  Add doc comments only: legacy note on the orchestrator-plan types in group.types.ts, and the worst-case
  cost-ceiling explanation above the constants in decision.ts. No behavioral change.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && npx nx run-many -t lint,build --projects=@org/backend,@org/frontend 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - group.types.ts has a comment marking DinoTurnDecision/GroupOrchestratorPlan as Phase 35 legacy superseded by DinoDecision
    - decision.ts documents the worst-case call count and that it replaces the Phase 37 governor budget
    - lint + build pass for both projects
  </acceptance_criteria>
  <done>The contract documents what is live vs legacy and the new cost ceiling.</done>
</task>

<task type="auto">
  <name>Task 3: Write the human UAT script and run the full workspace gate</name>
  <files>.planning/phases/41-autonomous-dino-minds/41-HUMAN-UAT.md</files>
  <read_first>
    .planning/phases/41-autonomous-dino-minds/41-CONTEXT.md
    .planning/ROADMAP.md
  </read_first>
  <behavior>
    - Write 41-HUMAN-UAT.md with observable steps and PENDING checkboxes proving each success criterion:
      1. (GRP3-01) Start a group chat with 3+ dinos; send one message; observe that different dinos
         independently answer/react/stay silent in the same turn — no fixed "everyone answers" pattern;
         backend logs show one decision call per dino on its own model.
      2. (GRP3-02) In a turn with ≥2 answers, a later dino's reply visibly references what an earlier dino
         just said (e.g. agrees/disagrees/builds on it) — and across rounds a dino responds to another dino.
      3. (GRP3-03) A single turn shows a mix of answer bubbles, emoji reaction chips, and silent dinos,
         rendered top-to-bottom with correct attribution (names/mascots) and intent chips.
      4. (cost) Confirm a turn never exceeds the documented ceiling (no runaway loop); @mention forces that
         dino to answer in round 1.
    - Include a note to run against both localhost and the live site (https://dinoagents.duckdns.org) per
      the v2.2 parity goal.
  </behavior>
  <action>
  Create 41-HUMAN-UAT.md with the four observable check sections above as a checklist, matching the prior
  phases' HUMAN-UAT format. Then run the full lint/build/test gate across both projects.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && npx nx run-many -t lint,build,test --projects=@org/backend,@org/frontend --skip-nx-cache 2>&1 | tail -25</automated>
  </verify>
  <acceptance_criteria>
    - 41-HUMAN-UAT.md exists with observable checks for GRP3-01, GRP3-02, GRP3-03, and the cost ceiling
    - The full workspace gate (lint+build+test for @org/backend and @org/frontend) is green
  </acceptance_criteria>
  <done>The phase has a human UAT script and the whole workspace passes its gate.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-41-03-01 | Rendering regression | empty `plan` event handling | mitigate | A unit test drives the empty-plan v3 sequence and asserts slots are created from `dino_token`, not from `plan.round1`; a regression here is caught in CI. |
| T-41-03-02 | Stale contract confusion | legacy DinoTurnDecision/GroupOrchestratorPlan kept but unused for decisions | accept | Documented as legacy in the type file; retained only for the empty `plan` event so the SSE contract and frontend slot-layout assumptions stay stable. |
</threat_model>

<verification>
- `npx nx run-many -t lint,build,test --projects=@org/backend,@org/frontend` green
- 41-HUMAN-UAT.md present with PENDING checks (human-run, not automated)
- Frontend renders a scripted multi-round mixed turn correctly (unit test)
</verification>

<success_criteria>
- Roadmap Phase 41 Success Criterion #3 (GRP3-03): mixed answers/reactions/silences render top-to-bottom
  with attribution — proven by the frontend test + UAT.
- Success Criterion #4 (doc half): the cost ceiling replacing the governor budget is documented.
- Phase verification: GRP3-01/02/03 have observable UAT acceptance steps.
</success_criteria>

<output>
Create `.planning/phases/41-autonomous-dino-minds/41-03-SUMMARY.md` when done
</output>
