---
phase: 39-deploy-truth-and-smoke-checks
plan: 03
type: execute
wave: 3
depends_on: ["39-02"]
files_modified:
  - .planning/INFRASTRUCTURE.md
  - README.md
  - infra/caddy/Caddyfile
autonomous: true
requirements:
  - PROD-05
must_haves:
  truths:
    - "INFRASTRUCTURE.md describes the REAL live architecture: a Compute Engine VM (spinochat-backend) on Container-Optimized OS running the backend Docker container (spinochat) on port 3000, with a Caddy container terminating TLS on 80/443 and reverse-proxying over the shared 'web' Docker network; the Angular SPA is baked into the backend image and served same-origin (useStaticAssets), NOT from a GCS bucket"
    - "Stale/contradictory content is removed from INFRASTRUCTURE.md: GCS-bucket frontend hosting as the live path, Firebase mentions, Cloud Run as current, the http://<external-ip> frontend env step, and 'HTTPS comes later' — these describe a superseded design and must not read as current"
    - "The Secret Manager secret list is documented and complete: openrouter-api-key, database-url, and tavily-api-key (added in Phase 38) — matching what scripts/vm-deploy.sh actually fetches"
    - "README '## Deployment' remains accurate to the Caddy/baked-frontend model and contains no live-path nginx/certbot/Firebase/GCS-frontend instructions (the existing nginx note may remain only as an explicit 'retained for reference, does NOT apply' disclaimer)"
    - "infra/caddy/Caddyfile reflects the live VM config and documents domain templating: the {DOMAIN} placeholder convention and the real hostname (dinoagents.duckdns.org) are explained so the file matches what runs on the VM"
    - "Documentation does not contradict the smoke-checked reality from plan 39-02 (one serving path, web_search configured via tavily-api-key secret)"
  artifacts:
    - path: ".planning/INFRASTRUCTURE.md"
      provides: "Rewritten runbook describing the live Caddy + baked-frontend + Secret Manager architecture"
      exports: []
    - path: "README.md"
      provides: "Deployment section verified/trimmed to the real architecture (no stale live-path instructions)"
      exports: []
    - path: "infra/caddy/Caddyfile"
      provides: "Caddyfile matching live config with documented domain templating"
      exports: []
  key_links:
    - from: "INFRASTRUCTURE.md secret list"
      to: "scripts/vm-deploy.sh fetch_secret calls"
      via: "openrouter-api-key, database-url, tavily-api-key"
      pattern: "every documented secret is one vm-deploy.sh actually reads, and vice versa"
---

<objective>
Make the deployment docs tell the truth. `INFRASTRUCTURE.md` (last touched 2026-05-27) still describes a
superseded design — GCS-bucket frontend hosting, Firebase teardown, Cloud Run as current, an
`http://<external-ip>` frontend with "HTTPS comes later" — none of which matches the live system: a single
COS VM running the backend container behind a Caddy TLS reverse proxy, serving the Angular SPA baked into the
backend image, with secrets in Secret Manager. Rewrite it to reality, confirm the README Deployment section
stays accurate, and make the committed Caddyfile reflect the live config with documented domain templating.

Purpose: the runbooks describe what actually runs, so the next deploy/debug doesn't follow a dead map.
Output: a rewritten `INFRASTRUCTURE.md`, a verified/trimmed README Deployment section, and a truthful Caddyfile.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
Stale doc to rewrite: [.planning/INFRASTRUCTURE.md](../../../.planning/INFRASTRUCTURE.md) — its architecture
table lists GCS-bucket frontend hosting and Cloud Run; Step 7 sets `apiUrl: 'http://<external-ip>'`; Step 9
tears down Firebase/Cloud Run; "Deferred" still lists "HTTPS for backend" as future. All superseded.
Source of truth for the LIVE architecture:
- [README.md](../../../README.md) "## Deployment" (~lines 111–148) — already accurate: COS VM, backend
  container `spinochat` on 3000, Caddy on 80/443 over the `web` network, baked-in SPA, Let's Encrypt
  auto-renew, no certbot. Mirror this; do not contradict it.
- [apps/backend/src/main.ts](../../../apps/backend/src/main.ts) — `useStaticAssets` serves the baked SPA
  same-origin; global `api` prefix.
- [apps/backend/Dockerfile](../../../apps/backend/Dockerfile) — copies `dist/apps/frontend/browser` into the
  image (the baking).
- [scripts/vm-deploy.sh](../../../scripts/vm-deploy.sh) — the real deploy: metadata-token auth, AR docker
  login, `fetch_secret` for `openrouter-api-key` + `database-url` (Phase 38 plan 38-02 adds
  `tavily-api-key`), runs the container on the `web` network, NOT publishing port 80 (Caddy owns it).
- [infra/caddy/Caddyfile](../../../infra/caddy/Caddyfile) — `{DOMAIN} { reverse_proxy spinochat:3000 }`,
  with a header comment about COS/Caddy; live domain is `dinoagents.duckdns.org`.
- [.github/workflows/ci.yml](../../../.github/workflows/ci.yml) — after plan 39-02: build-image →
  deploy-backend (SSH) → smoke; NO deploy-frontend.
Project memory: live HTTPS is at https://dinoagents.duckdns.org via a Caddy container (NOT nginx+certbot);
infra resource names still use `spinochat-*` and must NOT be renamed.
There is also a retained-for-reference file [infra/nginx/dinoagents.conf](../../../infra/nginx/dinoagents.conf)
— it does NOT apply to the live VM; keep it only with a clear disclaimer, do not present it as live.
</context>

<design_decisions>
- **Rewrite, don't patch (D-01):** Replace INFRASTRUCTURE.md's architecture table, runbook steps, and
  deferred section so the document reads as a current, accurate runbook. Keep the genuinely-still-true parts
  (Cloud SQL Postgres + Drizzle, dev vs prod database split + the local SSL note, WIF-based CI, `spinochat-*`
  resource names). Update "Last updated" to 2026-06-12 and "Status" to reflect "live".
- **Architecture table truth (D-02):** Frontend hosting row = "baked into the backend Docker image, served
  same-origin via `useStaticAssets`" — NOT a GCS bucket. TLS/ingress row = "Caddy container, 80/443,
  auto Let's Encrypt, reverse-proxy over `web` network". Remove Cloud Run as a current choice (a one-line
  "(historical: an earlier Cloud Run service was torn down)" note is fine; do not present it as live).
- **Secret list (D-03):** Document exactly the three secrets vm-deploy.sh fetches: `openrouter-api-key`,
  `database-url`, `tavily-api-key`. Cross-check against `scripts/vm-deploy.sh` so the doc and script agree.
- **Drop dead steps (D-04):** Remove/replace the GCS frontend provisioning + sync steps, the
  `apiUrl: 'http://<external-ip>'` env step, the Firebase/Cloud Run teardown step, and the "HTTPS comes
  later" deferral. Replace the deploy-flow description with the real CI order (build-image → deploy-backend →
  smoke) and the on-VM Caddy runbook (reference README rather than duplicating it verbatim, to avoid drift).
- **Storybook (D-05):** Storybook GCS deploy still exists in CI (continue-on-error) — keep its row but mark it
  accurately (separate bucket, best-effort, currently broken on Angular 21 per the ci.yml note). Do not imply
  the app frontend uses the same path.
- **README (D-06):** The README Deployment section is already accurate — verify it, and only trim anything
  that contradicts the live model. The existing nginx "retained for reference / does not apply" note is fine
  as-is. Do not rewrite the README runbook wholesale.
- **Caddyfile (D-07):** Keep `{DOMAIN}` as the templating placeholder (it is the convention vm/README use),
  and ensure the header comment documents the substitution AND names the live host
  (`dinoagents.duckdns.org`). The reverse-proxy target `spinochat:3000` already matches live — confirm it.
  If everything already matches, the change may be limited to clarifying the domain-templating comment.
- **Out of scope:** Actually deleting the GCS frontend bucket or changing `vm-deploy.sh`/`ci.yml` (those are
  plan 39-02 / infra teardown), and renaming any `spinochat-*` resource.
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite INFRASTRUCTURE.md to the live architecture</name>
  <files>.planning/INFRASTRUCTURE.md</files>
  <read_first>
    .planning/INFRASTRUCTURE.md
    README.md
    scripts/vm-deploy.sh
    apps/backend/Dockerfile
    apps/backend/src/main.ts
  </read_first>
  <behavior>
    - Update the header (`Status`, `Last updated: 2026-06-12`).
    - Rewrite the architecture table: frontend = baked-into-backend-image (same-origin useStaticAssets);
      ingress/TLS = Caddy container (80/443, auto Let's Encrypt, `web` network); keep Cloud SQL Postgres +
      Drizzle, dev/prod DB split, WIF CI rows. Remove GCS-frontend-as-live and Cloud-Run-as-current.
    - Document the Secret Manager secret list: openrouter-api-key, database-url, tavily-api-key.
    - Replace the dead runbook steps (GCS frontend sync, http external-ip apiUrl, Firebase/Cloud Run teardown,
      "HTTPS later") with the real CI deploy order (build-image → deploy-backend → smoke) and a pointer to the
      README on-VM Caddy runbook.
    - Preserve the "Dev vs prod database" section and its local SSL note verbatim (still true).
  </behavior>
  <action>
  Rewrite .planning/INFRASTRUCTURE.md so it describes the live system: COS VM `spinochat-backend`, backend
  container `spinochat` on 3000, Caddy container on 80/443 reverse-proxying over the `web` network, Angular
  SPA baked into the backend image (Dockerfile copies `dist/apps/frontend/browser`; main.ts `useStaticAssets`).
  Document the three Secret Manager secrets (openrouter-api-key, database-url, tavily-api-key) matching
  `scripts/vm-deploy.sh`. Remove all GCS-frontend-as-live, Firebase, Cloud-Run-as-current, and
  `http://<external-ip>`/"HTTPS comes later" content. Keep the Cloud SQL/Drizzle, dev-vs-prod DB + local SSL
  note, and WIF CI material. Set Last updated to 2026-06-12. Do not rename any `spinochat-*` resource.
  </action>
  <acceptance_criteria>
    - INFRASTRUCTURE.md states the frontend is served baked into the backend image / Caddy, and contains no instruction presenting GCS-bucket frontend hosting as the live serving path
    - The secret list names openrouter-api-key, database-url, AND tavily-api-key
    - No `apiUrl: 'http://<external-ip>'` step and no "HTTPS comes later" deferral remain
    - The "Dev vs prod database" section and its SSL note are preserved
    - `Last updated:` is 2026-06-12
  </acceptance_criteria>
  <done>INFRASTRUCTURE.md is an accurate runbook for the live Caddy + baked-frontend architecture.</done>
</task>

<task type="auto">
  <name>Task 2: Verify/trim the README Deployment section and the Caddyfile</name>
  <files>
    README.md
    infra/caddy/Caddyfile
  </files>
  <read_first>
    README.md
    infra/caddy/Caddyfile
    scripts/vm-deploy.sh
  </read_first>
  <behavior>
    - Read the README "## Deployment" section; confirm it matches the live model. Remove only content that
      contradicts it (it currently appears accurate; the nginx "retained for reference" disclaimer may stay).
      If a Secret Manager list appears, ensure tavily-api-key is included.
    - In the Caddyfile, ensure the header comment documents the `{DOMAIN}` templating AND names the live host
      `dinoagents.duckdns.org`, and that the reverse-proxy target is `spinochat:3000` (matches live). Make the
      minimal change needed for truth; do not introduce a hardcoded domain in the directive (keep `{DOMAIN}`).
  </behavior>
  <action>
  Verify README.md "## Deployment" is consistent with the live Caddy/baked-frontend architecture and remove
  any line that contradicts it (do not rewrite the accurate runbook). In infra/caddy/Caddyfile, confirm
  `reverse_proxy spinochat:3000` and ensure the header comment explains the `{DOMAIN}` placeholder is replaced
  with the real hostname `dinoagents.duckdns.org` at deploy time. Keep `{DOMAIN}` as the templated placeholder.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && node -e "const fs=require('fs');const i=fs.readFileSync('.planning/INFRASTRUCTURE.md','utf8');if(!/tavily-api-key/.test(i))throw new Error('tavily secret missing in INFRASTRUCTURE.md');if(/http:\/\/<external-ip>/.test(i))throw new Error('stale external-ip step remains');const c=fs.readFileSync('infra/caddy/Caddyfile','utf8');if(!/spinochat:3000/.test(c))throw new Error('caddy target wrong');if(!/dinoagents\.duckdns\.org/.test(c))throw new Error('live domain not documented in Caddyfile');console.log('DOCS_TRUTH_OK')"</automated>
  </verify>
  <acceptance_criteria>
    - README "## Deployment" contains no live-path GCS-frontend / Firebase / certbot instruction (a clearly-labelled nginx "does not apply" disclaimer is acceptable)
    - infra/caddy/Caddyfile keeps `reverse_proxy spinochat:3000`, documents the `{DOMAIN}` templating, and names `dinoagents.duckdns.org`
    - The node check prints `DOCS_TRUTH_OK`
  </acceptance_criteria>
  <done>README and Caddyfile agree with the live architecture and the rewritten INFRASTRUCTURE.md.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-39-03-01 | Operational risk | stale runbook misguiding a future deploy/debug | mitigate | This plan's whole purpose: replace the superseded GCS/Firebase/Cloud-Run/nginx narrative with the live Caddy/baked-frontend reality, cross-checked against vm-deploy.sh and the Dockerfile. |
| T-39-03-02 | Information disclosure | secrets in docs | accept | Docs name secret IDs only (openrouter-api-key, database-url, tavily-api-key), never values — same exposure level as vm-deploy.sh already in the repo. |
</threat_model>

<verification>
- node doc-truth check prints `DOCS_TRUTH_OK` (tavily secret documented, no stale external-ip step, Caddyfile
  truthful)
- Manual skim: INFRASTRUCTURE.md architecture table + secret list + deploy flow match README "## Deployment"
  and scripts/vm-deploy.sh; no GCS-frontend-as-live / Firebase / Cloud-Run-as-current content remains
</verification>

<success_criteria>
- Roadmap Phase 39 Success Criteria #3 and #4 (PROD-05): INFRASTRUCTURE.md + README runbook describe the real
  architecture (Caddy container, baked-in frontend, Secret Manager secret list, vm-deploy flow) with stale
  nginx/certbot/Firebase/Cloud-Run content removed, and infra/caddy/Caddyfile reflects the live VM config with
  documented domain templating.
</success_criteria>

<output>
Create `.planning/phases/39-deploy-truth-and-smoke-checks/39-03-SUMMARY.md` when done
</output>
