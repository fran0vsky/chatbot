---
phase: 33-composer-knowledge-reorg
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/ui/src/lib/input-composer/input-composer.ts
  - libs/ui/src/lib/input-composer/input-composer.html
  - libs/ui/src/lib/input-composer/input-composer.stories.ts
  - apps/frontend/src/app/chat/chat.ts
  - apps/frontend/src/app/chat/chat.html
autonomous: true
requirements:
  - CMP-01
  - CMP-02
  - CMP-05
must_haves:
  truths:
    - "D-01: The composer's primary action button is a BRAIN icon (replacing the wrench glyph); clicking it emits a teachOpen output that the chat wires to openSkillPanel (same teach surface the header 'Teach' button opens)"
    - "D-02: The tools toggle is a SEPARATE dedicated icon-only button using a non-wrench (tools/sliders) glyph, whose title AND aria-label both read 'Tools' so CMP-01 'clearly-labeled' holds; the existing checkbox popover and toolToggled output are unchanged"
    - "D-03: Typing '/' at the start of an otherwise-command-only draft opens a small autocomplete menu listing /teach; selecting it (or typing '/teach ' then choosing it) opens the teach flow and clears the draft"
    - "D-04: Trailing text after '/teach ' is emitted as a prefill string with teachOpen and pre-fills the teach modal's instruction field (skill name left blank)"
    - "D-05: The teach modal overlay is noticeably larger than today — max-w-lg grows to at least max-w-2xl (wider) and remains tall (max-h kept/raised) (CMP-02)"
    - "No regression (CMP-05): tool checkboxes still toggle via toolToggled, send/stop/mic/image/paste all still work, and Enter-to-send is unaffected by the command menu when the draft is a normal message"
  artifacts:
    - path: "libs/ui/src/lib/input-composer/input-composer.ts"
      provides: "Brain teachOpen output, dedicated Tools button, /teach command-menu logic + prefill"
      exports: ["InputComposer", "ComposerSubmit"]
    - path: "apps/frontend/src/app/chat/chat.html"
      provides: "teachOpen wired on main composers; enlarged teach modal"
      exports: []
  key_links:
    - from: "InputComposer brain button + /teach menu"
      to: "Chat.openSkillPanel(prefillInstruction?)"
      via: "(teachOpen) output → chat handler that opens the existing skill panel and seeds skillInstruction"
      pattern: "teachOpen emits string|undefined; chat opens panel and sets instruction signal when present"
---

<objective>
Restructure the composer around a brain entry point and a clearly-labeled, dedicated tools button, and
add a `/teach` slash-command. The brain (replacing the wrench) opens the existing teach panel; the tools
toggle becomes its own icon-only button tooltipped "Tools"; typing `/` shows a command menu offering
`/teach`, with any trailing text pre-filling the teach modal's instruction. The teach modal is enlarged.
Tool toggling and message sending must keep working (CMP-05).

Purpose: a user can reach "teach a skill" straight from the composer (brain or `/teach`) and still toggle
tools from an obvious, separate control.
Output: an updated `InputComposer` (brain output, dedicated tools button, command menu), its Storybook
story, and chat wiring that opens the (now larger) teach panel — optionally instruction-prefilled.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/33-composer-knowledge-reorg/33-CONTEXT.md
Composer: [input-composer.ts](../../../libs/ui/src/lib/input-composer/input-composer.ts) +
[input-composer.html](../../../libs/ui/src/lib/input-composer/input-composer.html) — presentational
(`@chatbot/ui`), OnPush, FormsModule, `draft` ngModel, `toolsOpen` popover (wrench button at html line ~36,
already `aria-label="Tools"`), outputs `send`/`stop`/`toolToggled`/`micToggle`. Story:
[input-composer.stories.ts](../../../libs/ui/src/lib/input-composer/input-composer.stories.ts).
Chat host: [chat.ts](../../../apps/frontend/src/app/chat/chat.ts) — `openSkillPanel()` (line ~396) sets
`skillPanelOpen`, loads learned items, and `skillInstruction`/`skillTitle` signals back the teach form;
`<app-input-composer>` appears 3× in [chat.html](../../../apps/frontend/src/app/chat/chat.html): the
groupchat one (~line 150) and the two main-chat ones (~441 and ~535). The teach overlay (~659) is the panel
to open; its modal box is at ~line 672 (`max-w-lg max-h-[85vh]`).
Frontend rules: [apps/frontend/CLAUDE.md](../../../apps/frontend/CLAUDE.md) — standalone + OnPush, logic in
component/services not templates, Tailwind only (no inline styles), types from `@org/shared-types`. Library
presentational rule: `libs/ui` components stay service-free with a Storybook story.
</context>

<design_decisions>
- **Brain replaces wrench (D-01, CMP-01):** Add a NEW brain-glyph button as the leftmost/primary composer
  control. It emits a new `@Output() teachOpen = new EventEmitter<string | undefined>()`. It does NOT open
  the tools popover.
- **Dedicated tools button (D-02, CMP-01):** Keep the existing tools popover button + checkbox list +
  `toolToggled`, but swap its icon from the wrench (`M14.7 6.3...`) to a distinct tools/sliders glyph (e.g. a
  sliders/gear icon). Keep `aria-label="Tools"` and set `title="Tools"` (currently "Tools available to Spino"
  — a "Tools…" title is fine as long as it starts with "Tools"). Icon-only, no text label.
- **/teach menu (D-03):** Live autocomplete. Compute `commandMenuOpen` true when the trimmed `draft` matches
  `^/\w*$` (a leading slash command being typed, no spaces yet) AND `/teach` matches the typed prefix. Render
  a small popover above the textarea with a single `/teach` item. Selecting it calls a `chooseTeach()` method.
  Also support the trailing-text form: if `draft` matches `^/teach(\s+(.*))?$`, `chooseTeach()` extracts group 2.
- **Prefill (D-04):** `chooseTeach()` emits `teachOpen.emit(trailingText?.trim() || undefined)` and clears
  `draft`. Trailing text → instruction prefill; name stays blank.
- **Modal resize (D-05, CMP-02):** In chat.html teach overlay, change the modal box from `max-w-lg` to
  `max-w-2xl` (noticeably wider) and keep `max-h-[85vh]` (or `max-h-[90vh]`). Tailwind classes only.
- **Wiring:** Only the two MAIN-chat composers (~441, ~535) get `(teachOpen)`; the groupchat composer (~150)
  is left unchanged (no teach there). `openSkillPanel` gains an optional `prefillInstruction` param.
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Add brain teachOpen output + /teach command menu to InputComposer (TS)</name>
  <files>libs/ui/src/lib/input-composer/input-composer.ts</files>
  <read_first>libs/ui/src/lib/input-composer/input-composer.ts</read_first>
  <behavior>
    - Add `@Output() teachOpen = new EventEmitter<string | undefined>();`
    - Add a getter `commandMenuOpen` (or a method) returning true when `this.draft` matches `^/\w*$` and
      `'/teach'.startsWith(this.draft.trim())` — i.e. the user is typing a leading command that /teach matches.
    - Add `chooseTeach(): void` — parse `this.draft` with `/^\/teach(?:\s+([\s\S]*))?$/`; capture trailing
      text; `this.teachOpen.emit(trailing?.trim() || undefined)`; reset `draft = ''`, run autoResize, and
      close the menu/markForCheck.
    - In `onKeydown`, when `commandMenuOpen` is true and the key is Enter (no shift), call `chooseTeach()`
      and `preventDefault()` INSTEAD of `submit()` — but only when the draft is purely a `/teach…` command
      (so normal messages still send on Enter).
  </behavior>
  <action>
  Add the `teachOpen` EventEmitter, a `commandMenuOpen` predicate keyed on a leading `/` slash-command that
  /teach matches, and a `chooseTeach()` that emits the optional trailing instruction text and clears the
  draft. Guard `onKeydown` so Enter selects /teach only while the menu is open for a `/teach` draft;
  otherwise Enter still sends. OnPush — call `cdr.markForCheck()` where state changes. No `any`.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && npx nx lint @chatbot/ui --quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - input-composer.ts declares `@Output() teachOpen` of type `EventEmitter<string | undefined>`
    - A `commandMenuOpen` predicate and a `chooseTeach()` method exist; chooseTeach emits trailing text and clears draft
    - Enter triggers chooseTeach only when the command menu is open; normal sends unaffected
    - lint passes, no `any`
  </acceptance_criteria>
  <done>The composer can fire a teach intent (with optional prefill) and detect /teach.</done>
</task>

<task type="auto">
  <name>Task 2: Brain button, dedicated Tools button, and command menu (HTML)</name>
  <files>libs/ui/src/lib/input-composer/input-composer.html</files>
  <read_first>libs/ui/src/lib/input-composer/input-composer.html</read_first>
  <behavior>
    - Add a brain-glyph button as the FIRST control inside the `flex items-center gap-1` row (before the tools
      popover). `type="button"`, `(click)="teachOpen.emit(undefined)"`, `aria-label="Teach a skill"`,
      `title="Teach a skill"`, reusing the existing icon-button Tailwind classes. Use an inline brain SVG.
    - In the existing tools toggle button, replace the wrench `<path d="M14.7 6.3...">` with a distinct
      tools/sliders SVG and change `title="Tools available to Spino"` to `title="Tools"`. Keep
      `aria-label="Tools"`, the popover, the amber dot, and all `toolToggled` wiring intact.
    - Add the command-menu popover: `@if (commandMenuOpen) { ... }` rendering a small absolute panel above
      the textarea with one button row `/teach — Teach this dino a skill`, `(click)="chooseTeach()"`. Style
      with Tailwind to match the tools popover.
  </behavior>
  <action>
  Insert the brain button (emits `teachOpen.emit(undefined)`) as the leftmost control; re-icon the tools
  button to a non-wrench glyph with `title="Tools"`/`aria-label="Tools"`; add a `@if (commandMenuOpen)`
  autocomplete popover containing a single `/teach` item calling `chooseTeach()`. Tailwind classes only, no
  inline styles. Do not alter send/stop/mic/image/textarea blocks.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && npx nx build @chatbot/ui --skip-nx-cache 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - input-composer.html has a brain button bound to `teachOpen.emit(undefined)` with aria-label/title "Teach a skill"
    - The tools button no longer uses the wrench path, has `title="Tools"` and `aria-label="Tools"`, and keeps its popover + toolToggled
    - A `@if (commandMenuOpen)` block renders a /teach item calling `chooseTeach()`
    - `npx nx build @chatbot/ui` exits 0
  </acceptance_criteria>
  <done>The composer shows a brain entry point, a labeled Tools button, and a /teach menu.</done>
</task>

<task type="auto">
  <name>Task 3: Update the Storybook story for the new outputs/controls</name>
  <files>libs/ui/src/lib/input-composer/input-composer.stories.ts</files>
  <read_first>libs/ui/src/lib/input-composer/input-composer.stories.ts</read_first>
  <behavior>
    - Ensure the story renders without error with the new `teachOpen` output (add an action/arg for it).
    - Optionally add a story variant with a `/teach` draft pre-set to showcase the command menu.
  </behavior>
  <action>
  Add `teachOpen` to the story's actions/argTypes so the component renders cleanly. Keep existing stories
  working. Optionally add a "CommandMenu" story seeding `draft = '/te'`.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && npx nx lint @chatbot/ui --quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - input-composer.stories.ts references `teachOpen`
    - lint passes
  </acceptance_criteria>
  <done>The presentational component's story covers the new teach output.</done>
</task>

<task type="auto">
  <name>Task 4: Wire teachOpen and enlarge the teach modal (chat host)</name>
  <files>
    apps/frontend/src/app/chat/chat.ts
    apps/frontend/src/app/chat/chat.html
  </files>
  <read_first>
    apps/frontend/src/app/chat/chat.ts
    apps/frontend/src/app/chat/chat.html
  </read_first>
  <behavior>
    - chat.ts: change `openSkillPanel()` to `openSkillPanel(prefillInstruction?: string)`; after opening,
      if `prefillInstruction` is a non-empty string, `this.skillInstruction.set(prefillInstruction)` (leave
      `skillTitle` blank). Keep existing learned-items loading.
    - chat.html: on BOTH main-chat `<app-input-composer>` instances (~441 and ~535), add
      `(teachOpen)="openSkillPanel($event)"`. Do not add it to the groupchat composer (~150).
    - chat.html: in the teach overlay modal box (~672), change `max-w-lg` → `max-w-2xl` and keep `max-h-[85vh]`
      (or raise to `max-h-[90vh]`).
  </behavior>
  <action>
  Give `openSkillPanel` an optional `prefillInstruction` param that seeds the `skillInstruction` signal when
  present. Bind `(teachOpen)="openSkillPanel($event)"` on the two main composers only. Widen the teach modal
  to `max-w-2xl`. Tailwind only; existing header "Teach" button and `closeSkillPanel` reset behavior unchanged.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && npx nx lint frontend --quiet 2>&1 | tail -8</automated>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && npx nx build frontend --skip-nx-cache 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - chat.ts `openSkillPanel` accepts an optional instruction and sets `skillInstruction` when provided
    - Both main composers bind `(teachOpen)="openSkillPanel($event)"`; the groupchat composer does not
    - The teach modal box uses `max-w-2xl` (no longer `max-w-lg`)
    - frontend lint and build pass
  </acceptance_criteria>
  <done>Brain/`/teach` open the enlarged teach panel, instruction-prefilled when text trailed /teach.</done>
</task>

<task type="manual" autonomous="false">
  <name>Task 5: UAT — composer reorg + /teach</name>
  <files>apps/frontend/src/app/chat/chat.html</files>
  <read_first>apps/frontend/src/app/chat/chat.html</read_first>
  <action>
  Run the app. In a 1:1 chat: (1) confirm the composer shows a brain button and a separate tools button;
  hover the tools button and confirm its tooltip reads "Tools" and the checkbox popover still toggles tools.
  (2) Click the brain — confirm the (enlarged) teach panel opens. (3) Type `/` and confirm a /teach
  autocomplete appears; select it — confirm the teach panel opens. (4) Type `/teach Always answer in haiku`
  and select /teach — confirm the panel opens with the instruction field pre-filled "Always answer in haiku"
  and the name field blank. (5) Send a normal message and confirm Enter still sends (no menu interference).
  </action>
  <acceptance_criteria>
    - Brain button opens teach; dedicated Tools button is clearly labeled and still toggles tools (CMP-01)
    - /teach autocomplete opens the teach flow; modal is noticeably larger; trailing text pre-fills instruction (CMP-02)
    - Normal message sending and tool toggling are unaffected (CMP-05)
  </acceptance_criteria>
  <done>CMP-01 and CMP-02 verified live with no send/tool regression.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-33-02-01 | Improper Input Handling | /teach trailing text → instruction prefill | mitigate | Trailing text is set as a plain value into an Angular-bound textarea (`[value]`/signal), never as HTML; Angular escapes interpolation, so no markup injection. The prefill is user-authored text that the user reviews before saving. |
| T-33-02-02 | UI Redress / Confused Control | Brain vs Tools buttons | accept | Both buttons carry distinct aria-labels ("Teach a skill" / "Tools") and tooltips; no destructive action is reachable by misclick (both open non-destructive surfaces). |
</threat_model>

<verification>
- `npx nx run-many -t lint,build --projects=@chatbot/ui` green
- `npx nx run-many -t lint,build --projects=frontend` green
- Manual UAT (Task 5): brain opens teach, Tools button labeled + functional, /teach menu + prefill, no send regression
</verification>

<success_criteria>
- CMP-01: a brain icon replaces the wrench as the composer's primary control and opens the teach panel; a
  separate, clearly-labeled ("Tools") dedicated button exposes the tool toggles.
- CMP-02: a `/teach` slash-command in the composer opens the teach flow, and the teach modal is noticeably
  larger (wider via max-w-2xl, still tall).
- CMP-05: tool toggling and normal message send/stop are unchanged.
</success_criteria>

<output>
Create `.planning/phases/33-composer-knowledge-reorg/33-02-SUMMARY.md` when done
</output>
