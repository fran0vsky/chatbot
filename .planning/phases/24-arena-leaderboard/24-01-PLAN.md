---
phase: 24-arena-leaderboard
plan: 01
type: execute
wave: 1
depends_on:
  - 19-01
  - 23-01
files_modified:
  - apps/backend/src/app/database/schema.ts
  - apps/backend/src/app/arena/elo.ts
  - apps/backend/src/app/arena/elo.spec.ts
  - apps/backend/src/app/arena/arena.service.ts
  - apps/backend/src/app/arena/arena.service.spec.ts
  - apps/backend/src/app/arena/arena.controller.ts
  - apps/backend/src/app/arena/arena.module.ts
  - apps/backend/src/app/app.module.ts
  - libs/shared-types/src/lib/arena.types.ts
  - libs/shared-types/src/index.ts
  - apps/frontend/src/app/chat/arena.service.ts
  - apps/frontend/src/app/chat/chat.ts
  - apps/frontend/src/app/chat/chat.html
  - libs/ui/src/lib/leaderboard/leaderboard.ts
  - libs/ui/src/lib/leaderboard/leaderboard.html
  - libs/ui/src/lib/leaderboard/leaderboard.stories.ts
  - libs/ui/src/index.ts
autonomous: true
requirements:
  - ARN-01
  - ARN-02
  - ARN-03
  - ARN-04

must_haves:
  truths:
    - "Arena shows two side-by-side answers to the same prompt from two randomly chosen dinos, identities hidden until the user votes"
    - "After voting, both dinos are revealed and their ranking scores update"
    - "Scores use a documented Elo-style system persisted server-side (per dino); a tie/skip option is handled"
    - "A Leaderboard tab ranks all dinos by score with wins/losses/games"
    - "Everything degrades gracefully when DATABASE_URL is unset (ratings just aren't persisted)"
  artifacts:
    - path: "apps/backend/src/app/arena/elo.ts"
      provides: "Pure Elo update function (documented K-factor) — unit tested"
      exports: ["updateElo", "DEFAULT_RATING", "K_FACTOR"]
    - path: "apps/backend/src/app/arena/arena.service.ts"
      provides: "Record a match result; compute new ratings; read leaderboard"
      exports: ["ArenaService"]
    - path: "libs/ui/src/lib/leaderboard/leaderboard.ts"
      provides: "Presentational leaderboard table"
      exports: ["Leaderboard"]
  key_links:
    - from: "arena.controller.ts POST /api/arena/vote"
      to: "ArenaService.recordVote -> updateElo"
      via: "winnerDinoId/loserDinoId (or draw) -> rating deltas persisted"
      pattern: "Elo update"
---

<objective>
Build the Dino Arena: a blind, split-screen head-to-head where two random dinos answer the same prompt, the user votes for the better answer, identities are revealed, and a documented Elo-style ranking updates. Add a Leaderboard tab ranking all dinos. Ratings persist server-side and degrade gracefully without a DB.

Purpose: Gamified, shareable comparison; gives the dino roster a competitive identity.
Output: Elo logic + ArenaService/Controller + dino_ratings schema + arena view (blind compare + vote) + Leaderboard component/tab.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/23-dino-groupchat/23-01-PLAN.md
Arena reuses the same SSE streaming as groupchat (two parallel streams) but hides identities until the vote. DatabaseModule provides a possibly-null db (degrade gracefully). The leaderboard is a new `activeView` tab.
</context>

<scoring_design>
Elo, documented:
- Every dino starts at `DEFAULT_RATING = 1000`.
- `K_FACTOR = 24` (moderate volatility for a small roster).
- Expected score for A vs B: `Ea = 1 / (1 + 10^((Rb - Ra)/400))`.
- Result S: winner 1, loser 0, draw 0.5 each.
- New rating: `Ra' = Ra + K * (Sa - Ea)` (and symmetrically for B).
- "Skip/both bad" → no rating change, no game counted (or count as draw — choose draw for simplicity and document it).
- Persist per-dino: rating, wins, losses, draws, games.
</scoring_design>

<tasks>

<task type="auto">
  <name>Task 1: Elo logic (pure + tested) + schema</name>
  <files>apps/backend/src/app/arena/elo.ts, apps/backend/src/app/arena/elo.spec.ts, apps/backend/src/app/database/schema.ts, libs/shared-types/src/lib/arena.types.ts, libs/shared-types/src/index.ts</files>
  <read_first>apps/backend/src/app/database/schema.ts</read_first>
  <behavior>
    - `elo.ts`: pure `updateElo(ra, rb, result: 'a'|'b'|'draw'): { ra: number; rb: number }` per <scoring_design>; exports DEFAULT_RATING, K_FACTOR.
    - schema: `dinoRatings` pgTable: dinoId (text pk), rating (integer, default 1000), wins/losses/draws/games (integer default 0), updatedAt.
    - shared types: `DinoRating { dinoId; rating; wins; losses; draws; games }`, `ArenaVote { promptId?; aDinoId; bDinoId; result: 'a'|'b'|'draw' }`, `LeaderboardRow extends DinoRating { name; species }`.
  </behavior>
  <action>
  Implement elo.ts + an exhaustive spec (symmetry, expected-score monotonicity, draw splits, winner gains/loser loses, rounding). Append dinoRatings to schema.ts. Add arena.types.ts + re-export.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx test backend --skip-nx-cache 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - updateElo matches the documented formula; spec covers win/loss/draw + symmetry
    - dinoRatings schema + shared types exist
    - tests pass
  </acceptance_criteria>
  <done>Correct, tested scoring math and persistence shape.</done>
</task>

<task type="auto">
  <name>Task 2: ArenaService + ArenaController + module</name>
  <files>apps/backend/src/app/arena/arena.service.ts, apps/backend/src/app/arena/arena.service.spec.ts, apps/backend/src/app/arena/arena.controller.ts, apps/backend/src/app/arena/arena.module.ts, apps/backend/src/app/app.module.ts</files>
  <read_first>
    apps/backend/src/app/agents/dinos/dinos.ts
    apps/backend/src/app/database/database.module.ts
  </read_first>
  <behavior>
    - ArenaService injects DATABASE_CONNECTION; methods: `getMatchup(): { aDinoId, bDinoId }` (two distinct random dino ids from DINOS), `recordVote(vote): Promise<void>` (loads both ratings or seeds DEFAULT, applies updateElo, increments counters, upserts), `getLeaderboard(): Promise<LeaderboardRow[]>` (joins ratings with DINOS for name/species; dinos with no rating row appear at DEFAULT_RATING/0 games, sorted by rating desc). Null-db: getMatchup still works (random from registry); recordVote no-ops; getLeaderboard returns registry defaults.
    - ArenaController: `GET /api/arena/matchup`, `POST /api/arena/vote`, `GET /api/arena/leaderboard`.
    - Register ArenaModule in AppModule.
  </behavior>
  <action>
  Implement service (Drizzle upserts guarded for null db), controller (HTTP only), module. Spec: with a fake db assert rating deltas + counters; with null db assert no-op + registry-default leaderboard.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint backend --quiet 2>&1 | tail -5 && pnpm nx test backend --skip-nx-cache 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - matchup returns two distinct dinos; vote updates+persists Elo; leaderboard ranks all dinos
    - null-db degrades gracefully; tests pass
  </acceptance_criteria>
  <done>Server records matches and serves a leaderboard.</done>
</task>

<task type="auto">
  <name>Task 3: Arena frontend (blind compare + vote)</name>
  <files>apps/frontend/src/app/chat/arena.service.ts, apps/frontend/src/app/chat/chat.ts, apps/frontend/src/app/chat/chat.html</files>
  <read_first>
    apps/frontend/src/app/chat/groupchat.service.ts
    apps/frontend/src/app/chat/chat.ts
    apps/frontend/src/app/chat/chat.html
  </read_first>
  <behavior>
    - ArenaService (frontend): fetch matchup, run two parallel streams (reuse the groupchat parallel-stream approach) labeled "A"/"B" WITHOUT showing dino identity, submit vote, fetch leaderboard.
    - Add `'arena'` to activeView. Arena view: prompt box → two anonymous panels stream → vote buttons (A better / B better / Tie) → on vote, reveal both dinos (name+mascot) and show updated ratings → "Next battle" resets.
    - Identities MUST be hidden until a vote is cast.
  </behavior>
  <action>
  Build ArenaService (HttpClient + reuse parallel streaming). Extend activeView + nav. Render anonymous A/B panels (reuse GroupResponse but suppress name/mascot until revealed — pass a `reveal` flag/blank dino). Wire vote → POST → reveal.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint frontend --quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Two anonymous answers stream; identities hidden until vote; vote reveals + posts result; "next battle" works
    - lint passes
  </acceptance_criteria>
  <done>Blind arena with voting and reveal.</done>
</task>

<task type="auto">
  <name>Task 4: Leaderboard component + tab</name>
  <files>libs/ui/src/lib/leaderboard/*, libs/ui/src/index.ts, apps/frontend/src/app/chat/chat.ts, apps/frontend/src/app/chat/chat.html</files>
  <read_first>
    libs/ui/src/lib/history-panel/history-panel.ts
    libs/ui/src/index.ts
  </read_first>
  <behavior>
    - Leaderboard (presentational, OnPush, story): input `rows: LeaderboardRow[]`; renders rank, mascot, name, rating, W/L/D, games, sorted as given.
    - Add `'leaderboard'` to activeView + a nav tab; load rows via ArenaService on entering the tab.
  </behavior>
  <action>
  Build Leaderboard + story; export; add the tab + data load.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint ui frontend --quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Leaderboard tab renders ranked dinos with stats; story present; exported; lint passes
  </acceptance_criteria>
  <done>A leaderboard tab reflects arena outcomes.</done>
</task>

<task type="manual" autonomous="false">
  <name>Task 5: Arena + leaderboard smoke test (DB + key)</name>
  <files>apps/backend/src/app/arena/arena.service.ts</files>
  <read_first>.env.example</read_first>
  <action>
  With DB (push the new tables) + key: run a few arena battles, vote, confirm both reveal and ratings move in the expected direction; open Leaderboard and confirm ranking/stats update. Repeat with DATABASE_URL unset → arena still runs; leaderboard shows defaults; no crash.
  </action>
  <acceptance_criteria>
    - Voting updates ratings; leaderboard reflects results; null-db safe
  </acceptance_criteria>
  <done>Arena + leaderboard verified end-to-end.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-24-01 | Tampering | Vote stuffing | accept-for-v1 | No auth; anyone can vote. Acceptable for a portfolio leaderboard; could rate-limit/per-user cap later |
| T-24-02 | Integrity | Concurrent rating updates | mitigate | Upsert reads-then-writes; acceptable at portfolio scale (note: a transaction/locking is a future hardening) |
| T-24-03 | DoS | Two parallel free-model streams | mitigate | Only 2 streams; reuse abort handling |
</threat_model>

<verification>
- `pnpm nx run-many -t lint,test --projects=shared-types,backend,ui,frontend` green
- Blind compare → vote → reveal → Elo persists; leaderboard ranks; null-db safe
</verification>

<success_criteria>
- ARN-01..04 satisfied; Elo documented + tested
</success_criteria>

<output>
Create `.planning/phases/24-arena-leaderboard/24-01-SUMMARY.md` when done
</output>
