---
phase: 04-dark-theme-and-visual-polish
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - apps/frontend/src/app/chat/message-bubble/message-bubble.html
  - apps/frontend/src/app/chat/message-bubble/message-bubble.ts
  - apps/frontend/src/app/chat/message-bubble/message-bubble.scss
autonomous: true
requirements: []

must_haves:
  truths:
    - "Every assistant message bubble (including the typing indicator) shows a small cactus-green pixel art snake avatar to its left"
    - "User message bubbles are terracotta/warm orange (not blue)"
    - "Assistant message bubbles are sand/parchment (not gray)"
    - "Error bubbles retain a desert-compatible warm red styling"
    - "The snake avatar is ~24x24px pixel art style and looks good on both day (parchment) and night (dark brown) backgrounds"
    - "No user bubble has a snake avatar"
    - "No inline styles ([style.*] bindings) exist in the typing indicator template"
  artifacts:
    - path: "apps/frontend/src/app/chat/message-bubble/message-bubble.ts"
      provides: "snakeSvg SafeHtml property using DomSanitizer"
      contains: "DomSanitizer"
    - path: "apps/frontend/src/app/chat/message-bubble/message-bubble.html"
      provides: "Snake avatar slot left of assistant and typing bubbles; desert palette bubble classes; CSS classes for dot delays"
      contains: "snakeSvg"
    - path: "apps/frontend/src/app/chat/message-bubble/message-bubble.scss"
      provides: "Desert bubble palette classes, markdown element overrides, dot-delay classes for typing animation"
      contains: "desert"
  key_links:
    - from: "message-bubble.html assistant branch"
      to: "snakeSvg property on MessageBubble class"
      via: "[innerHTML]=\"snakeSvg\" on avatar div"
      pattern: "snakeSvg"
    - from: ".markdown-bubble in message-bubble.scss"
      to: "desert palette CSS custom properties from styles.scss"
      via: "@apply with desert-* Tailwind tokens"
      pattern: "desert-"
    - from: ".dot-delay-* classes in message-bubble.scss"
      to: "typing indicator dot spans in message-bubble.html"
      via: "class attribute on each span"
      pattern: "dot-delay"
---

<objective>
Restyle message bubbles to desert palette and add the cactus-green pixel art snake avatar to the left
of every assistant/typing bubble.

Purpose: Completes the visual desert transformation. After Plan 04-02, the shell is desert-themed but
the bubbles still use blue (user) and gray (assistant). This plan replaces those with terracotta and
sand/parchment and injects the snake mascot.
Output: Updated message-bubble.html (desert classes, snake avatar, CSS-class-based dot delays),
message-bubble.ts (snakeSvg SafeHtml property via DomSanitizer), message-bubble.scss (desert bubble
palette, markdown overrides, dot-delay utility classes).
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-dark-theme-and-visual-polish/04-CONTEXT.md
@.planning/phases/04-dark-theme-and-visual-polish/04-01-SUMMARY.md

<interfaces>
From apps/frontend/src/app/chat/message-bubble/message-bubble.ts (current state before this plan):
  @Input({ required: true }) message!: ChatMessage;   // has .text and .role
  @Input() typing = false;
  copied = false;
  copyMessage(): void  // uses navigator.clipboard + setTimeout

From apps/frontend/src/app/chat/message-bubble/message-bubble.html (current state before this plan):
  4 branches: user (right, bg-blue-500 text-white), error (left, bg-red-50 border-red-200 text-red-700),
  typing (left, bg-gray-100, 3 bouncing dots with [style.animation-delay] bindings),
  assistant (left, bg-gray-100 text-gray-800, class="group relative markdown-bubble", has copy button)

From apps/frontend/src/app/chat/message-bubble/message-bubble.scss (current state before this plan):
  .markdown-bubble { ::ng-deep { ... } }  — styles for h1-h6, p, ul, ol, li, a, strong, em,
  blockquote, code, pre, pre code, table, th/td, hr
  Currently uses: bg-gray-200 (inline code), bg-gray-800 text-gray-100 (pre), border-gray-300,
  text-gray-600 (blockquote), text-blue-600 (links)

From tailwind.config.js (after Plan 04-01):
  desert-sand, desert-parchment, desert-terracotta, desert-terracotta-dark, desert-brown,
  desert-brown-muted, desert-border, desert-night-parchment, desert-night-amber,
  desert-night-sage, desert-night-text, desert-night-muted, cactus-green
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add snakeSvg SafeHtml property to MessageBubble and restyle bubbles with desert palette</name>
  <files>
    apps/frontend/src/app/chat/message-bubble/message-bubble.ts
    apps/frontend/src/app/chat/message-bubble/message-bubble.html
    apps/frontend/src/app/chat/message-bubble/message-bubble.scss
  </files>

  <read_first>
    apps/frontend/src/app/chat/message-bubble/message-bubble.ts — full current file (37 lines)
    apps/frontend/src/app/chat/message-bubble/message-bubble.html — full current file (50 lines)
    apps/frontend/src/app/chat/message-bubble/message-bubble.scss — full current file (92 lines);
    note the existing [style.animation-delay] pattern on typing indicator dots
  </read_first>

  <action>
    PART A — message-bubble.ts: Add DomSanitizer-based snakeSvg property.

    Add to imports at the top of the file:
      import { DomSanitizer, SafeHtml } from '@angular/platform-browser';

    Add to the inject() calls (alongside existing inject() usage or constructor injection pattern
    already used in the component):
      private readonly sanitizer = inject(DomSanitizer);

    Add the snakeSvg property after the `copied = false;` line. Use SafeHtml type with
    DomSanitizer to avoid NG0904 security warnings. The SVG is a hardcoded string constant —
    not user input — so bypassSecurityTrustHtml is appropriate:

      readonly snakeSvg: SafeHtml = this.sanitizer.bypassSecurityTrustHtml(
        `&lt;svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 8 8" shape-rendering="crispEdges" aria-hidden="true" focusable="false"&gt;
          &lt;!-- Body row 1 --&gt;
          &lt;rect x="2" y="0" width="2" height="1" fill="#4A7C59"/&gt;
          &lt;!-- Body row 2: wider --&gt;
          &lt;rect x="1" y="1" width="4" height="1" fill="#4A7C59"/&gt;
          &lt;!-- Body row 3: main body --&gt;
          &lt;rect x="0" y="2" width="5" height="1" fill="#4A7C59"/&gt;
          &lt;!-- Eyes --&gt;
          &lt;rect x="1" y="2" width="1" height="1" fill="#1A1209"/&gt;
          &lt;rect x="3" y="2" width="1" height="1" fill="#1A1209"/&gt;
          &lt;!-- Body row 4: coil --&gt;
          &lt;rect x="1" y="3" width="6" height="1" fill="#4A7C59"/&gt;
          &lt;!-- Body row 5: coil continues --&gt;
          &lt;rect x="2" y="4" width="5" height="1" fill="#4A7C59"/&gt;
          &lt;!-- Tail row --&gt;
          &lt;rect x="3" y="5" width="3" height="1" fill="#4A7C59"/&gt;
          &lt;rect x="5" y="6" width="2" height="1" fill="#4A7C59"/&gt;
          &lt;!-- Tongue --&gt;
          &lt;rect x="0" y="2" width="1" height="1" fill="#C1644A"/&gt;
        &lt;/svg&gt;`
      );

    The SVG uses an 8x8 pixel grid scaled to 24x24 via width/height attributes.
    shape-rendering="crispEdges" is set as an SVG attribute directly — this handles crisp
    pixel-art rendering without needing any external CSS rule. The main body color is #4A7C59
    (cactus-green token). The tongue is #C1644A (terracotta). Eyes are #1A1209 (desert-night dark).
    This design reads well on both parchment (#F5E6C8) and dark night (#1A1209) backgrounds.

    PART B — message-bubble.html: Make the following changes.

    1. USER bubble branch — replace bg-blue-500 with desert terracotta:
       FROM: class="max-w-[75%] rounded-2xl rounded-br-sm px-4 py-2 bg-blue-500 text-white whitespace-pre-wrap break-words"
       TO:   class="max-w-[75%] rounded-2xl rounded-br-sm px-4 py-2 bg-desert-terracotta text-white whitespace-pre-wrap break-words"

    2. ERROR bubble branch — no change needed. Error bubble retains red-50/red-200/red-700 which
       are appropriate and legible on both day and night backgrounds.

    3. TYPING INDICATOR bubble branch — wrap with snake avatar and replace inline style bindings
       with CSS classes. Replace the entire typing branch content:

       FROM:
         &lt;div class="flex w-full justify-start"&gt;
           &lt;div class="max-w-[75%] rounded-2xl rounded-bl-sm px-4 py-2 bg-gray-100 text-gray-800 flex items-center gap-1"&gt;
             &lt;span ... [style.animation-delay]="'0ms'"&gt;&lt;/span&gt;
             &lt;span ... [style.animation-delay]="'150ms'"&gt;&lt;/span&gt;
             &lt;span ... [style.animation-delay]="'300ms'"&gt;&lt;/span&gt;
           &lt;/div&gt;
         &lt;/div&gt;

       TO:
         &lt;div class="flex w-full justify-start items-end gap-2"&gt;
           &lt;div class="snake-avatar flex-shrink-0 self-end mb-1"
                [innerHTML]="snakeSvg"
                aria-hidden="true"&gt;&lt;/div&gt;
           &lt;div class="max-w-[75%] rounded-2xl rounded-bl-sm px-4 py-2 bg-desert-parchment text-desert-brown flex items-center gap-1"&gt;
             &lt;span class="inline-block w-1.5 h-1.5 rounded-full bg-desert-brown-muted animate-bounce dot-delay-0"&gt;&lt;/span&gt;
             &lt;span class="inline-block w-1.5 h-1.5 rounded-full bg-desert-brown-muted animate-bounce dot-delay-150"&gt;&lt;/span&gt;
             &lt;span class="inline-block w-1.5 h-1.5 rounded-full bg-desert-brown-muted animate-bounce dot-delay-300"&gt;&lt;/span&gt;
           &lt;/div&gt;
         &lt;/div&gt;

       Note: dot-delay-0, dot-delay-150, dot-delay-300 are CSS classes defined in message-bubble.scss
       (Part C below). Do NOT use [style.animation-delay] bindings — inline styles violate CLAUDE.md.

    4. ASSISTANT bubble branch (the @else block) — wrap with snake avatar container and restyle.
       Replace the outer wrapper div and inner bubble div classes:

       FROM:
         &lt;div class="flex w-full justify-start"&gt;
           &lt;div class="group relative markdown-bubble max-w-[75%] rounded-2xl rounded-bl-sm px-4 pt-2 pb-2 pr-8 bg-gray-100 text-gray-800 break-words text-sm"&gt;

       TO:
         &lt;div class="flex w-full justify-start items-end gap-2"&gt;
           &lt;div class="snake-avatar flex-shrink-0 self-end mb-1"
                [innerHTML]="snakeSvg"
                aria-hidden="true"&gt;&lt;/div&gt;
           &lt;div class="group relative markdown-bubble max-w-[75%] rounded-2xl rounded-bl-sm px-4 pt-2 pb-2 pr-8 bg-desert-parchment text-desert-brown break-words text-sm"&gt;

       Also update the copy button hover classes within the assistant branch:
       FROM: class="absolute top-1 right-1 p-1 rounded opacity-0 group-hover:opacity-100 transition-opacity hover:bg-gray-200 text-gray-500 hover:text-gray-700"
       TO:   class="absolute top-1 right-1 p-1 rounded opacity-0 group-hover:opacity-100 transition-opacity hover:bg-desert-border text-desert-brown-muted hover:text-desert-brown"

    5. Close the additional outer div — the assistant branch's outer div now has an extra child
       (the snake avatar div), so ensure the closing tags are correct. The structure is:
         &lt;div class="flex w-full justify-start items-end gap-2"&gt;   ← outer
           &lt;div class="snake-avatar ..." [innerHTML]="snakeSvg"&gt;&lt;/div&gt;  ← snake
           &lt;div class="group relative markdown-bubble ..."&gt;          ← bubble
             &lt;markdown&gt;...&lt;/markdown&gt;
             &lt;button&gt;...&lt;/button&gt;
           &lt;/div&gt;                                                    ← close bubble
         &lt;/div&gt;                                                      ← close outer

    PART C — message-bubble.scss: Add dot-delay utility classes to eliminate inline style bindings.

    Add these three classes to message-bubble.scss (outside the .markdown-bubble block, e.g. at the
    end of the file). These are host-scoped styles on span elements that ARE part of the Angular
    component tree, so view encapsulation applies correctly — no need for ::ng-deep or global styles:

      /* Typing indicator dot stagger delays — replaces [style.animation-delay] inline bindings */
      .dot-delay-0   { animation-delay: 0ms; }
      .dot-delay-150 { animation-delay: 150ms; }
      .dot-delay-300 { animation-delay: 300ms; }

    View encapsulation note: The .snake-avatar host div is part of the Angular component tree (it is
    declared directly in the template, not injected via innerHTML). Host div styles — display, size,
    flex shrink — stay in message-bubble.scss and are correctly scoped by Angular's emulated
    encapsulation. The SVG *inside* the [innerHTML] div is injected content and would NOT receive the
    component's attribute selector. However, shape-rendering="crispEdges" is set directly as an SVG
    attribute in the markup string, so no external CSS rule targeting the injected SVG children is
    needed. Do NOT add a `.snake-avatar svg { ... }` rule — it would be a dead rule under emulated
    encapsulation and is unnecessary since the SVG attribute handles crisp rendering directly.
  </action>

  <verify>
    <automated>grep -n "snakeSvg\|DomSanitizer\|SafeHtml\|desert-terracotta\|desert-parchment\|desert-brown\|items-end gap-2\|dot-delay\|animation-delay" apps/frontend/src/app/chat/message-bubble/message-bubble.ts apps/frontend/src/app/chat/message-bubble/message-bubble.html apps/frontend/src/app/chat/message-bubble/message-bubble.scss</automated>
  </verify>

  <acceptance_criteria>
    - message-bubble.ts imports `DomSanitizer, SafeHtml` from `@angular/platform-browser`
    - message-bubble.ts has `private readonly sanitizer = inject(DomSanitizer)`
    - message-bubble.ts has `readonly snakeSvg: SafeHtml = this.sanitizer.bypassSecurityTrustHtml(...)`
    - The SVG string inside bypassSecurityTrustHtml contains `shape-rendering="crispEdges"` and `fill="#4A7C59"`
    - message-bubble.ts does NOT declare `snakeSvg` as a plain string (must be SafeHtml type)
    - message-bubble.html typing branch outer div has `items-end gap-2`
    - message-bubble.html typing branch has `class="snake-avatar ..."` and `[innerHTML]="snakeSvg"` before the dots div
    - message-bubble.html typing dots use class `dot-delay-0`, `dot-delay-150`, `dot-delay-300` (not [style.animation-delay])
    - message-bubble.html does NOT contain `[style.animation-delay]` anywhere
    - message-bubble.html assistant branch outer div has `items-end gap-2`
    - message-bubble.html assistant branch has `class="snake-avatar ..."` and `[innerHTML]="snakeSvg"` before the markdown-bubble div
    - message-bubble.html user bubble has `bg-desert-terracotta` (not `bg-blue-500`)
    - message-bubble.html assistant bubble has `bg-desert-parchment text-desert-brown` (not bg-gray-100 text-gray-800)
    - message-bubble.html typing dots use `bg-desert-brown-muted` (not bg-gray-500)
    - message-bubble.scss contains `.dot-delay-0`, `.dot-delay-150`, `.dot-delay-300` rules
    - message-bubble.scss does NOT contain `.snake-avatar svg` rule (SVG attribute handles crisp rendering; CSS rule would be a dead rule under emulated encapsulation)
    - `pnpm nx build frontend` completes without TypeScript errors
  </acceptance_criteria>

  <done>Snake avatar appears left of typing indicator and all assistant bubbles; user bubbles are terracotta; assistant bubbles are sand/parchment; typing dots use CSS classes not inline styles; build passes</done>
</task>

<task type="auto">
  <name>Task 2: Update markdown-bubble SCSS styles for desert palette</name>
  <files>apps/frontend/src/app/chat/message-bubble/message-bubble.scss</files>

  <read_first>
    apps/frontend/src/app/chat/message-bubble/message-bubble.scss — full current file (92 lines);
    note every @apply value that references gray/blue colors — these need desert equivalents.
    Note: Task 1 has already added dot-delay-* classes and the snake-avatar host div styles to this
    file. Read the file AFTER Task 1 completes to see the current state before adding markdown overrides.
  </read_first>

  <action>
    Update the .markdown-bubble ::ng-deep block to replace gray/blue Tailwind tokens with desert
    equivalents. All rules use @apply (Tailwind-only convention, per CLAUDE.md).

    Replace these specific @apply values — keep all other rules (spacing, sizing, font-weight) unchanged:

    Blockquote border and text color:
      FROM: @apply border-l-2 border-gray-300 pl-3 my-2 text-gray-600 italic;
      TO:   @apply border-l-2 border-desert-border pl-3 my-2 text-desert-brown-muted italic;

    Inline code background:
      FROM: @apply rounded bg-gray-200 px-1 py-0.5 text-xs font-mono;
      TO:   @apply rounded bg-desert-border px-1 py-0.5 text-xs font-mono text-desert-brown;

    Fenced code block (pre) — Prism.js themes override this in practice, but set a safe fallback:
      FROM: @apply rounded-lg bg-gray-800 text-gray-100 p-3 my-2 overflow-x-auto text-xs;
      TO:   @apply rounded-lg bg-[#2d2d2d] text-[#ccc] p-3 my-2 overflow-x-auto text-xs;
      NOTE: Using arbitrary values because the pre background is overridden by Prism themes anyway;
      bg-[#2d2d2d] is a neutral dark that works under both day and night Prism themes as a fallback.

    pre code (transparent override — no change needed, keep as-is):
      @apply bg-transparent p-0 text-xs;  ← leave unchanged

    Table cell borders:
      FROM: @apply border border-gray-300 px-2 py-1;
      TO:   @apply border border-desert-border px-2 py-1;

    HR:
      FROM: @apply my-3 border-gray-300;
      TO:   @apply my-3 border-desert-border;

    Links:
      FROM: @apply text-blue-600 underline break-all;
      TO:   @apply text-desert-terracotta underline break-all;

    The heading, paragraph, list, strong, em rules have no color @apply — leave those unchanged.

    CSS encapsulation note: the ::ng-deep rules inside .markdown-bubble target the [innerHTML]-rendered
    markdown content injected by the &lt;markdown&gt; component. This is the established pattern already
    in the file — leave it as-is. The .snake-avatar host div layout (display, flex-shrink) is handled
    by Task 1's snake-avatar class; no additional snake-avatar rules are needed in this task.
    Do NOT add a `.snake-avatar svg` rule — it would not reach the SVG injected via [innerHTML] under
    emulated encapsulation, and is unnecessary because shape-rendering="crispEdges" is set as an SVG
    attribute directly in the markup.
  </action>

  <verify>
    <automated>grep -n "desert-border\|desert-brown\|desert-terracotta\|dot-delay\|gray-\|blue-6" apps/frontend/src/app/chat/message-bubble/message-bubble.scss</automated>
  </verify>

  <acceptance_criteria>
    - message-bubble.scss blockquote rule uses `border-desert-border` and `text-desert-brown-muted` (not border-gray-300 / text-gray-600)
    - message-bubble.scss inline code rule uses `bg-desert-border text-desert-brown` (not bg-gray-200)
    - message-bubble.scss table border uses `border-desert-border` (not border-gray-300)
    - message-bubble.scss hr uses `border-desert-border` (not border-gray-300)
    - message-bubble.scss link uses `text-desert-terracotta` (not text-blue-600)
    - message-bubble.scss does NOT contain `.snake-avatar svg` rule
    - `pnpm nx build frontend` completes without SCSS errors
    - No `@apply bg-gray-` or `@apply text-gray-` or `@apply border-gray-` or `@apply text-blue-` remain in the file
  </acceptance_criteria>

  <done>All markdown element colors updated to desert palette; no stale gray/blue @apply rules remain; build passes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| snakeSvg → [innerHTML] | Inline SVG injected via Angular innerHTML binding |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04 | Tampering | [innerHTML]="snakeSvg" SVG injection | mitigate | Use DomSanitizer.bypassSecurityTrustHtml() with hardcoded string constant — not user input; no XSS risk from static SVG |
| T-04-05 | Information Disclosure | message-bubble renders user text | accept | User text already rendered via markdown component in Phase 3; no new trust boundary introduced |
| T-04-SC | Tampering | No new npm/pip/cargo installs in this plan | accept | No new packages; prismjs and DomSanitizer are existing dependencies |
</threat_model>

<verification>
After both tasks complete:
1. Build: `pnpm nx build frontend` exits 0
2. `grep "[style.animation-delay]" apps/frontend/src/app/chat/message-bubble/message-bubble.html` returns no matches
3. Manual: Send a message — user bubble appears in terracotta/burnt orange (not blue)
4. Manual: Receive a response — assistant bubble appears in sand/parchment (not gray); snake avatar visible to its left
5. Manual: While waiting for response — typing indicator shows snake avatar + parchment bubble with brown bouncing dots (staggered via CSS classes, not inline styles)
6. Manual: Code block in response — renders with Prism solarizedlight theme in day mode, Prism tomorrow in night mode
7. Manual: Inline code in response — warm background (desert-border) instead of gray-200
8. Manual: Link in response — terracotta/orange color instead of blue
9. Manual: Night mode active — bubbles still legible on dark background; snake still visible
</verification>

<success_criteria>
- Snake avatar (24x24px cactus-green pixel art SVG) appears to the left of every assistant and typing bubble
- No snake avatar on user bubbles or error bubbles
- User bubbles: bg-desert-terracotta (warm orange/burnt clay)
- Assistant bubbles: bg-desert-parchment (sand/parchment)
- Typing indicator dots use CSS classes (dot-delay-0/150/300) not [style.animation-delay] inline bindings
- Markdown elements (blockquote, inline code, links, table borders, hr) all use desert palette colors
- Build passes with no TypeScript or SCSS errors
</success_criteria>

<output>
Create `.planning/phases/04-dark-theme-and-visual-polish/04-03-SUMMARY.md` when done.
</output>
