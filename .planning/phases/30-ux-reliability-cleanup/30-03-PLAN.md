---
phase: 30-ux-reliability-cleanup
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/ui/src/lib/dino-card/dino-card.html
  - libs/ui/src/lib/dino-card/dino-card.stories.ts
autonomous: true
requirements:
  - REL-03
must_haves:
  truths:
    - "The 'Active' text badge no longer renders on any dino-card in the dino picker"
    - "The active dino is still visually distinguished by the existing ring highlight (the `active` input and ring classes remain)"
    - "Storybook for dino-card still builds and renders the active and inactive states"
  artifacts:
    - path: "libs/ui/src/lib/dino-card/dino-card.html"
      provides: "Dino card without the Active text badge"
      exports: []
  key_links: []
---

<objective>
Remove the textual "Active" badge from dino-card. The selection ring stays as the active affordance; only the
redundant pill-shaped "Active" label is removed.

Purpose: cleaner picker UI — the ring already communicates selection.
Output: dino-card with no "Active" badge, ring intact, stories updated if needed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-ux-reliability-cleanup/30-CONTEXT.md
The badge is the `@if (active) { <span>…Active</span> }` block at
[dino-card.html:17-19](../../../libs/ui/src/lib/dino-card/dino-card.html#L17). The `active` input also drives the ring
highlight (lines 6-8) and `aria-pressed` (line 4) — those MUST stay. `dino-card.stories.ts` defines an active story.
</context>

<design_decisions>
- Delete only the badge `@if`/`<span>`. Keep the `active` `@Input`, `aria-pressed`, and ring classes.
- The header row that contained the badge used `flex items-center justify-between` so the title + badge sat on one row;
  with the badge gone the title can keep `justify-between` (harmless) or be simplified — leave layout otherwise unchanged.
- Update `dino-card.stories.ts` only if it asserts on the "Active" text; the active/inactive ring stories should remain.
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Remove the Active badge from the template</name>
  <files>libs/ui/src/lib/dino-card/dino-card.html</files>
  <read_first>libs/ui/src/lib/dino-card/dino-card.html</read_first>
  <behavior>
    - Remove the `@if (active) { <span ...>Active</span> }` block ([dino-card.html:17-19](../../../libs/ui/src/lib/dino-card/dino-card.html#L17)).
    - Keep the `active`-driven ring classes (lines 6-8) and `aria-pressed` (line 4) untouched.
    - Keep the title `<h3>`; the surrounding `flex items-center justify-between` wrapper may stay as-is.
  </behavior>
  <action>
  Delete the badge conditional and its span only. Do not touch the button's `active` ring bindings or `aria-pressed`.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint ui --quiet 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - dino-card.html no longer contains the text `Active` badge span
    - `active` ring classes and `aria-pressed` remain present
    - lint passes
  </acceptance_criteria>
  <done>The badge is gone; selection ring remains.</done>
</task>

<task type="auto">
  <name>Task 2: Keep Storybook consistent</name>
  <files>libs/ui/src/lib/dino-card/dino-card.stories.ts</files>
  <read_first>libs/ui/src/lib/dino-card/dino-card.stories.ts</read_first>
  <behavior>
    - If a story asserts the "Active" badge text, update/remove that assertion; otherwise leave stories as-is.
    - The active and inactive (ring) stories must still exist and build.
  </behavior>
  <action>
  Inspect the stories file; adjust only references to the removed badge. Do not remove the active-state story (it now
  demonstrates the ring without the badge).
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx build-storybook ui --skip-nx-cache 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - dino-card stories build with no reference to the removed badge
    - active + inactive stories still present
  </acceptance_criteria>
  <done>Storybook reflects the badge-free card.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-30-03-01 | Integrity | Accidental removal of active affordance | mitigate | Task explicitly preserves ring classes + aria-pressed; only the text span is deleted. |
</threat_model>

<verification>
- `pnpm nx lint ui` green; `pnpm nx build-storybook ui` green
- Dino picker shows the ring on the active card, no "Active" badge
</verification>

<success_criteria>
- REL-03 satisfied: the "Active" badge is removed from the dino picker; selection ring remains
</success_criteria>

<output>
Create `.planning/phases/30-ux-reliability-cleanup/30-03-SUMMARY.md` when done
</output>
