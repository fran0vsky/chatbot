---
phase: 30-ux-reliability-cleanup
plan: 04
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - libs/ui/src/lib/history-panel/history-panel.html
  - apps/frontend/src/app/chat/chat.html
  - apps/frontend/src/app/store/ui/ui.actions.ts
  - apps/frontend/src/app/store/action-catalogue.ts
  - apps/frontend/src/app/store/action-catalogue.spec.ts
autonomous: true
requirements:
  - REL-04
must_haves:
  truths:
    - "The Explore nav entry is gone from the sidebar — there is no button that navigates to an 'explore' view"
    - "The Explore view block is removed from the chat template — no dead branch renders the dino gallery as a top-level view"
    - "'explore' is no longer a member of the ActiveView type, and no code references it (nav, view switch, voice action catalogue, or tests)"
    - "The dino gallery is still reachable via the existing picker modal; no other view (chats/knowledge/groupchat/arena/leaderboard) regressed"
  artifacts:
    - path: "apps/frontend/src/app/store/ui/ui.actions.ts"
      provides: "ActiveView union without 'explore'"
      exports: ["ActiveView"]
  key_links:
    - from: "history-panel viewChange"
      to: "ActiveView"
      via: "removed 'explore' emitter"
      pattern: "viewChange.emit('chats'|'knowledge'|'groupchat'|'arena'|'leaderboard')"
---

<objective>
Remove the Explore view entirely — sidebar nav entry, chat-template view block, the `'explore'` member of the
`ActiveView` type, and the voice action catalogue's `'explore'` reference — leaving no dead navigation. The dino
gallery remains reachable through the picker modal, so no capability is lost (ROADMAP scope note: supersedes the
Explore integration of Phases 19-20).

Purpose: kill dead UI so navigation has no orphan destinations.
Output: an Explore-free app with a clean `ActiveView` union and green tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/30-ux-reliability-cleanup/30-CONTEXT.md
Explore touch points:
- Nav button: [history-panel.html:87-111](../../../libs/ui/src/lib/history-panel/history-panel.html#L87)
- View block: [chat.html:100-114](../../../apps/frontend/src/app/chat/chat.html#L100) (the `@if (activeView() === 'explore')` branch)
- Type member: [ui.actions.ts:6](../../../apps/frontend/src/app/store/ui/ui.actions.ts#L6)
- Voice catalogue: `'explore'` in the views list ([action-catalogue.ts:42](../../../apps/frontend/src/app/store/action-catalogue.ts#L42)) and the description string (line 94); test fixture `set_active_view: { view: 'explore' }` ([action-catalogue.spec.ts:28](../../../apps/frontend/src/app/store/action-catalogue.spec.ts#L28))
The dino gallery also renders inside the picker modal (chat.html ~line 620, `pickerOpen`), which stays.
This plan edits chat.html — it runs in wave 2 after 30-01 (REL-01) to avoid a collision on that file.
</context>

<design_decisions>
- Remove all `'explore'` references in lockstep so the TypeScript `ActiveView` union has no orphan and nothing emits it.
- In `action-catalogue.spec.ts`, retarget the `set_active_view` fixture to a still-valid view (`'chats'`) so the test
  exercises a real member.
- Leave the picker modal and `pickDino` flow intact (update the stale "or Explore" comment at chat.ts:370 only if it's
  in scope — it is not in files_modified here, so leave it; harmless).
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Remove the Explore nav entry</name>
  <files>libs/ui/src/lib/history-panel/history-panel.html</files>
  <read_first>libs/ui/src/lib/history-panel/history-panel.html</read_first>
  <behavior>
    - Delete the entire Explore `<button>` block ([history-panel.html:87-111](../../../libs/ui/src/lib/history-panel/history-panel.html#L87))
      including its `viewChange.emit('explore')`, active-class bindings, icon, and label.
    - Keep all other nav buttons (chats, knowledge, groupchat, arena, leaderboard) and their order.
  </behavior>
  <action>Remove only the Explore nav button. Do not alter the surrounding `<nav>` or sibling buttons.</action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint ui --quiet 2>&1 | tail -6</automated>
  </verify>
  <acceptance_criteria>
    - history-panel.html has no `viewChange.emit('explore')` and no "Explore" label
    - Other nav buttons remain; lint passes
  </acceptance_criteria>
  <done>The sidebar no longer offers Explore.</done>
</task>

<task type="auto">
  <name>Task 2: Remove the Explore view block from chat.html</name>
  <files>apps/frontend/src/app/chat/chat.html</files>
  <read_first>apps/frontend/src/app/chat/chat.html</read_first>
  <behavior>
    - Remove the `@if (activeView() === 'explore') { ... }` branch ([chat.html:100-114](../../../apps/frontend/src/app/chat/chat.html#L100))
      and promote the following `@else if (activeView() === 'groupchat')` to the leading `@if` so the view chain stays valid.
    - Do not remove the picker-modal dino gallery (`pickerOpen`) or any other view branch.
    - Preserve the REL-01 skeleton changes from 30-01 in the default branch.
  </behavior>
  <action>
  Delete the explore branch and re-link the conditional chain (first branch becomes `@if (activeView() === 'groupchat')`).
  Leave groupchat/arena/leaderboard/knowledge/default branches and the picker modal intact.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint frontend --quiet 2>&1 | tail -6</automated>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx build frontend --skip-nx-cache 2>&1 | tail -12</automated>
  </verify>
  <acceptance_criteria>
    - chat.html has no `activeView() === 'explore'` branch
    - The view conditional chain compiles (build passes); other views unchanged
    - lint passes
  </acceptance_criteria>
  <done>No top-level Explore view renders.</done>
</task>

<task type="auto">
  <name>Task 3: Drop 'explore' from the ActiveView type and voice catalogue</name>
  <files>apps/frontend/src/app/store/ui/ui.actions.ts, apps/frontend/src/app/store/action-catalogue.ts, apps/frontend/src/app/store/action-catalogue.spec.ts</files>
  <read_first>
    apps/frontend/src/app/store/ui/ui.actions.ts
    apps/frontend/src/app/store/action-catalogue.ts
    apps/frontend/src/app/store/action-catalogue.spec.ts
  </read_first>
  <behavior>
    - Remove `'explore'` from the `ActiveView` union ([ui.actions.ts:6](../../../apps/frontend/src/app/store/ui/ui.actions.ts#L6)).
    - Remove `'explore'` from the allowed views list in action-catalogue.ts (line ~42) and from the `set_active_view`
      description string (line ~94) so it no longer advertises Explore.
    - In action-catalogue.spec.ts, change the `set_active_view` fixture from `{ view: 'explore' }` to `{ view: 'chats' }`
      (a valid member).
  </behavior>
  <action>
  Delete the `'explore'` literal from the union, the catalogue views array, and the description; update the spec fixture
  to a valid view. Ensure the `set_active_view` param schema (zod enum, if present) no longer lists `'explore'`.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx test frontend --skip-nx-cache 2>&1 | tail -18</automated>
  </verify>
  <acceptance_criteria>
    - `ActiveView` has no `'explore'`; no compile error from a dangling reference
    - action-catalogue.ts views list + description omit Explore; spec fixture uses a valid view
    - frontend tests pass
  </acceptance_criteria>
  <done>'explore' is fully removed from types, the voice action surface, and tests.</done>
</task>

<task type="manual" autonomous="false">
  <name>Task 4: Verify no dead Explore navigation</name>
  <files>apps/frontend/src/app/chat/chat.html</files>
  <read_first>libs/ui/src/lib/history-panel/history-panel.html</read_first>
  <action>
  Serve the frontend. Confirm: the sidebar has no Explore entry; every remaining nav item navigates correctly; the dino
  gallery is still reachable via "New chat" / the picker modal; no console errors. Optionally try the voice "go to
  explore" intent and confirm it is no longer a recognized destination.
  </action>
  <acceptance_criteria>
    - No Explore nav entry or view anywhere; other views work
    - Dino gallery reachable via the picker modal; no console errors
  </acceptance_criteria>
  <done>REL-04 verified by observation.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-30-04-01 | Integrity | Dangling 'explore' reference | mitigate | TypeScript compile (build) + frontend tests fail on any leftover reference; Task 3 enumerates every touch point. |
| T-30-04-02 | Availability | Lost access to dino gallery | mitigate | Gallery remains reachable via the picker modal; verified in Task 4. |
</threat_model>

<verification>
- `pnpm nx run-many -t lint,test,build --projects=frontend` green; `pnpm nx lint ui` green
- Manual sweep (Task 4): no Explore nav/view, other views intact, gallery via picker
</verification>

<success_criteria>
- REL-04 satisfied: Explore route/view, nav entry, type member, and voice action reference are removed with no dead navigation
</success_criteria>

<output>
Create `.planning/phases/30-ux-reliability-cleanup/30-04-SUMMARY.md` when done
</output>
