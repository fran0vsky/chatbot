---
phase: 06-desert-ui-elevation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/ui/src/lib/message-bubble/message-bubble.html
  - apps/frontend/src/app/chat/chat.html
autonomous: true
requirements:
  - SC-1
  - SC-2
must_haves:
  truths:
    - "Assistant message bubbles show a pixel snake avatar that reads as a snake at 24px rendered size"
    - "The snake has a forked tongue, visible eye pixel, and S-curve body"
    - "Decorative cactus silhouettes are visible in the chat background at low opacity"
    - "Cactus silhouettes never overlap message bubbles"
    - "Cacti do not intercept mouse events over the message area"
  artifacts:
    - path: "libs/ui/src/lib/message-bubble/message-bubble.html"
      provides: "24×24 inline SVG snake replacing old 8×8 SVG"
      contains: 'viewBox="0 0 24 24"'
    - path: "apps/frontend/src/app/chat/chat.html"
      provides: "5 decorative cactus SVG silhouettes inside <main>"
      contains: "opacity-[0.05]"
  key_links:
    - from: "message-bubble.html"
      to: "SVG viewBox"
      via: "viewBox attribute"
      pattern: 'viewBox="0 0 24 24"'
    - from: "chat.html <main>"
      to: "cactus SVGs"
      via: "absolute positioning"
      pattern: "pointer-events-none"
---

<objective>
Replace the 8×8 pixel art snake avatar with a 24×24 profile/S-curve snake, and inject five decorative cactus silhouettes into the chat background area.

Purpose: These are the two largest visual additions in Phase 6 — the snake avatar upgrades the assistant identity mark, and the cactus silhouettes establish desert atmosphere without intruding on readability.
Output: Updated message-bubble.html with 24×24 snake SVG; updated chat.html with 5 positioned cactus SVGs inside a relative-positioned main element.
</objective>

<execution_context>
@C:/Users/Franek/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Franek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/06-desert-ui-elevation/06-CONTEXT.md
@.planning/phases/06-desert-ui-elevation/06-RESEARCH.md
@.planning/phases/06-desert-ui-elevation/06-UI-SPEC.md
@.planning/phases/06-desert-ui-elevation/06-PATTERNS.md

<interfaces>
<!-- Verified current state of both files being modified. -->

From libs/ui/src/lib/message-bubble/message-bubble.html (lines 22-35 — the target replacement block):
The SVG lives inside:
  <div class="flex-shrink-0 self-end mb-1">
    <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 8 8" shape-rendering="crispEdges" aria-hidden="true" focusable="false">
      <!-- 10 <rect> elements with hex fills -->
    </svg>
  </div>
Replace only the <svg> element (lines 23–34). The outer <div class="flex-shrink-0 self-end mb-1"> wrapper stays.

From apps/frontend/src/app/chat/chat.html (line 12 — target for relative, and insertion point):
  <main class="flex-1 overflow-y-auto px-4 py-4 space-y-3 chat-scroll-area">
    @for (m of messages; track $index) { ... }
    @if (isLoading) { ... }
    <div #messageEnd></div>
  </main>
Add "relative" to the <main> class list. Insert 5 cactus SVGs as the first children of <main>, before the @for loop.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Replace 8×8 snake with 24×24 profile/S-curve snake in message-bubble.html</name>
  <files>libs/ui/src/lib/message-bubble/message-bubble.html</files>
  <read_first>
    libs/ui/src/lib/message-bubble/message-bubble.html
    .planning/phases/06-desert-ui-elevation/06-UI-SPEC.md (Component Inventory → Snake Avatar SVG section and pixel layout guide)
    .planning/phases/06-desert-ui-elevation/06-PATTERNS.md (message-bubble.html section)
    .planning/phases/06-desert-ui-elevation/06-RESEARCH.md (Pattern 1: Inline SVG Pixel Art; Code Examples → 24×24 coordinate design guide; Pitfall 1)
  </read_first>
  <action>
    Replace the entire <svg> block on lines 23–34 of message-bubble.html with a new 24×24 profile snake SVG. The outer <div class="flex-shrink-0 self-end mb-1"> wrapper on line 22 must remain unchanged.

    The new <svg> element MUST have these exact attributes: xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" shape-rendering="crispEdges" aria-hidden="true" focusable="false"

    Inside the SVG, place <rect> elements on the 24-unit grid — each SVG unit equals one CSS pixel at this viewBox/size combination. Use these hex fill values per decision D-03:
    - Main body: fill="#4A7C59"
    - Highlight pixels (upper body face on curves): fill="#6BAF82"
    - Eye pixel: fill="#1A1209"
    - Forked tongue (2 pixels): fill="#C1644A"

    Follow the pixel layout intent from the UI-SPEC pixel layout guide (reproduced here for convenience):
    Row 1 (y=1): head top block cols 22-23 — 2px wide, fill="#4A7C59"
    Row 2 (y=2): body cols 15-21 (7 wide), head col 22, eye col 23 — body fill="#4A7C59", eye fill="#1A1209"
    Row 3 (y=3): S upper curve body cols 9-15 (7 wide), fill="#4A7C59"; highlight 1-2 pixels on the top face at "#6BAF82"
    Row 4 (y=4): S mid connection cols 7-8 (2 wide), fill="#4A7C59"
    Row 5 (y=5): S lower curve body cols 7-13 (7 wide), fill="#4A7C59"; highlight 1-2 pixels at "#6BAF82"
    Row 6 (y=6): tail segment cols 13-19 (7 wide), fill="#4A7C59"
    Row 7 (y=7): tail cols 19-21 (3 wide), fill="#4A7C59"
    Row 8 (y=8): tail tip col 21-22 (2 wide or 1 wide tapering), fill="#4A7C59"

    Forked tongue (D-02): Place 2 separate <rect> elements extending forward from the head (right of col 23). Since the viewBox is 0-23 and head is at col 22-23, place tongue pixels at y=1 x=23 (1px) AND y=2 x=23 (1px) — OR at y=1 x=23-24 if the viewBox allows — use your pixel-art judgment to place two adjacent/forked pixels near the snout tip in the rightmost columns with fill="#C1644A". The fork should read as two separate tips: e.g., one rect at y=1 x=23 width=1 height=1 and one at y=2 x=23 width=1 height=1, both colored "#C1644A".

    The exact pixel coordinates are executor discretion within this layout intent. The mandatory constraint is: head on the right side, S-curve body, forked tongue extends from snout (rightmost head pixels), tail tapers to 1 pixel wide at the tip (leftmost/bottom part of the grid). The snake must be recognizable as a snake at 24×24 rendered size.

    Do NOT reuse the old <svg> opening tag. Do NOT keep viewBox="0 0 8 8". Write the entire replacement <svg> element fresh.

    Forbidden: inline style="" attributes; arbitrary Tailwind hex class strings like class="text-[#4A7C59]". Hex values belong inside fill="..." SVG attributes only.
  </action>
  <verify>
    <automated>pnpm nx build frontend --skip-nx-cache 2>&1 | Select-String -Pattern "error|Error" | Select-String -NotMatch "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - message-bubble.html contains viewBox="0 0 24 24" (not viewBox="0 0 8 8")
    - message-bubble.html contains shape-rendering="crispEdges" on the new SVG element
    - message-bubble.html contains fill="#4A7C59" (body color)
    - message-bubble.html contains fill="#1A1209" (eye color)
    - message-bubble.html contains fill="#C1644A" (tongue color — 2 separate rect elements)
    - message-bubble.html contains fill="#6BAF82" (highlight color — at least 1 rect)
    - The <div class="flex-shrink-0 self-end mb-1"> wrapper on line 22 is unchanged
    - No inline style="" attributes present in the new SVG block
    - pnpm nx build frontend exits with 0 errors (TypeScript template compilation passes)
  </acceptance_criteria>
  <done>The assistant message bubble displays a 24×24 profile snake SVG with S-curve body, eye, and forked tongue. The SVG renders crisply at 24px × 24px. The containing div wrapper is unchanged.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add relative to main and inject 5 cactus silhouette SVGs in chat.html</name>
  <files>apps/frontend/src/app/chat/chat.html</files>
  <read_first>
    apps/frontend/src/app/chat/chat.html
    .planning/phases/06-desert-ui-elevation/06-UI-SPEC.md (Component Inventory → Cactus Silhouettes section including position/size table and ASCII layout)
    .planning/phases/06-desert-ui-elevation/06-PATTERNS.md (chat.html section)
    .planning/phases/06-desert-ui-elevation/06-RESEARCH.md (Pattern 2: Inline SVG Silhouette Path; Anti-Patterns; Pitfall 2)
  </read_first>
  <action>
    Two changes to apps/frontend/src/app/chat/chat.html:

    CHANGE 1 — Add "relative" to the <main> element class list on line 12:
    Change: class="flex-1 overflow-y-auto px-4 py-4 space-y-3 chat-scroll-area"
    To:     class="flex-1 overflow-y-auto px-4 py-4 space-y-3 chat-scroll-area relative"

    CHANGE 2 — Insert 5 decorative cactus SVGs as the first children inside <main>, placed before the @for loop. Each SVG must carry these classes: absolute pointer-events-none select-none opacity-[0.05] dark:opacity-[0.03] text-desert-brown dark:text-desert-night-text z-0 — plus per-silhouette position and size classes listed below.

    Each SVG element has: aria-hidden="true" focusable="false" xmlns="http://www.w3.org/2000/svg"
    Each SVG contains a single <path fill="currentColor" d="..." /> element.

    The 5 silhouettes with their exact Tailwind positional/size class combinations and shape descriptions:

    1. Tall saguaro — position: bottom-0 left-4 — size: w-24 h-48 — viewBox="0 0 100 200"
       Shape: classic two-arm saguaro. Path: central vertical trunk from bottom to top, two arms branching upward (one left ~1/3 up, one right ~2/3 up), each arm curves up. Use a simple filled path. Example path intent: M50,200 L50,40 Q50,20 40,20 L40,60 Q40,80 50,80 (left arm) then M50,80 Q50,100 60,100 L60,60 Q60,20 50,20 (right arm merged into trunk). Author a plausible saguaro silhouette path — exact d values are executor discretion; the shape must read as a tall two-arm cactus.

    2. Prickly pear — position: bottom-0 right-16 — size: w-20 h-24 — viewBox="0 0 80 96"
       Shape: two oval pads stacked. Use two ellipse-like filled shapes or a single path combining two oval lobes. The lower pad is wider, the upper pad is smaller and offset slightly.

    3. Mid-right branched — position: top-1/3 right-8 — size: w-16 h-32 — viewBox="0 0 64 128"
       Shape: single-arm branched cactus — a trunk with one arm extending to the right about halfway up.

    4. Top-right small — position: top-8 right-4 — size: w-10 h-16 — viewBox="0 0 40 64"
       Shape: thin barrel / simple vertical column, slightly rounded at top.

    5. Mid-left small — position: top-1/2 left-8 — size: w-12 h-20 — viewBox="0 0 48 80"
       Shape: squat barrel oval — a short rounded rectangle or oval, wider than tall.

    FORBIDDEN patterns:
    - position: fixed (would bleed outside <main> and overlap header/input — use absolute only per Pitfall 2)
    - Omitting pointer-events-none (would intercept mouse events over messages)
    - Adding relative to any element other than <main> for this purpose
    - inline style="" attributes — Tailwind classes only
    - fill="currentColor" on the <svg> element itself — it must be on the <path> element; color is inherited from text-desert-brown Tailwind class on the <svg>

    The @for loop, @if isLoading block, and <div #messageEnd></div> inside <main> must remain completely unchanged.
  </action>
  <verify>
    <automated>pnpm nx build frontend --skip-nx-cache 2>&1 | Select-String -Pattern "error|Error" | Select-String -NotMatch "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - chat.html <main> element class list contains "relative"
    - chat.html contains exactly 5 SVG elements with class containing "opacity-[0.05]"
    - Each of the 5 SVG elements has aria-hidden="true"
    - Each of the 5 SVG elements has class containing "pointer-events-none"
    - Each of the 5 SVG elements has class containing "z-0"
    - Each of the 5 SVG elements has class containing "dark:opacity-[0.03]"
    - Each of the 5 SVG elements has class containing "text-desert-brown"
    - Each of the 5 SVG elements has class containing "absolute"
    - Each <path> inside a cactus SVG has fill="currentColor"
    - The @for loop, @if isLoading block, and #messageEnd div inside <main> are unchanged
    - pnpm nx build frontend exits with 0 errors
  </acceptance_criteria>
  <done>The chat main area contains 5 decorative cactus silhouette SVGs positioned at the five corners/edges of the chat area. All five carry opacity-[0.05] in day mode and dark:opacity-[0.03] in night mode. Pointer events do not reach the SVGs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static template → Browser | Inline SVG rendered in browser DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | Inline SVG `<path d="...">` | accept | Static markup authored by developer; no user input in path data; no script execution risk from SVG path data in HTML context |
| T-06-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — zero npm delta |
</threat_model>

<verification>
After both tasks complete, open the app in browser (pnpm nx serve frontend):
1. Send a test message — verify the assistant bubble shows the 24×24 snake avatar
2. Inspect the snake at 100% zoom — verify S-curve silhouette, visible eye dot, two-pixel forked tongue
3. Toggle to night mode — verify snake remains visible against dark background
4. Scroll back in the message list — verify cactus silhouettes are visible in the background at very low opacity
5. Verify cactus SVGs are positioned at different corners/edges of the chat area
6. Verify clicking/dragging in the message area is not intercepted by any cactus SVG (pointer-events-none)
7. DOM inspect: <main> has class "relative"; each cactus SVG has class "z-0"
</verification>

<success_criteria>
- Snake avatar is a recognizable S-curve snake profile at 24×24 rendered size with visible eye and forked tongue (SC-1)
- Five cactus silhouettes appear in the chat background at ~5% opacity in day mode and ~3% in night mode (SC-2)
- Cactus SVGs never overlap message bubbles (z-0 behind default stacking; pointer-events-none)
- pnpm nx build frontend passes with 0 errors
</success_criteria>

<output>
Create .planning/phases/06-desert-ui-elevation/06-01-SUMMARY.md when done
</output>
