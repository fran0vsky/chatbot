---
phase: 07-visual-overhaul
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/ui/src/lib/input-composer/input-composer.html
autonomous: true
requirements:
  - SC-1
  - SC-4
  - SC-5
  - SC-6
  - SC-7
must_haves:
  truths:
    - "The input pill has a polished focus treatment — a single focus-within ring on the pill card"
    - "The send button is a Gemini-grade circular accent button with hover, active press, disabled, and focus states"
    - "Textarea auto-resize and overflow-y-auto at max height are unchanged"
  artifacts:
    - path: "libs/ui/src/lib/input-composer/input-composer.html"
      provides: "Elevated floating input pill with focus-within ring and polished send button"
      contains: "focus-within:ring-2"
  key_links:
    - from: "pill card"
      to: "focus treatment"
      via: "focus-within ring"
      pattern: "focus-within:ring-2"
---

<objective>
Elevate the `InputComposer` floating pill — a refined shadow, a single polished `focus-within` ring on the pill card, and a Gemini-grade circular send button with complete interaction states — while preserving the auto-resize and overflow behavior.

Purpose: The input is where every interaction starts and is a Success Criterion (SC-4). A polished send button and a clean single-focus surface are what make the composer feel modern.
Output: Restyled `input-composer.html` with a `focus-within` pill ring, a polished send button, and visible focus rings.
</objective>

<execution_context>
@C:/Users/Franek/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Franek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-visual-overhaul/07-CONTEXT.md
@.planning/phases/07-visual-overhaul/07-UI-SPEC.md

<interfaces>
<!-- Verified current state. -->

input-composer.html:
- `<form class="w-full bg-transparent" (submit)="$event.preventDefault(); submit()">`.
- Pill card `<div class="w-full rounded-2xl shadow-xl bg-desert-sand-light dark:bg-desert-night-surface border border-desert-gold/50 dark:border-desert-night-border px-4 py-3">`.
- Row `<div class="flex items-end gap-2">`.
- `<textarea #textareaRef data-testid="message-input" rows="1" [placeholder]="placeholder" class="flex-1 resize-none rounded-lg border border-desert-border dark:border-desert-night-border bg-desert-sand-light dark:bg-desert-night-surface text-desert-brown dark:text-desert-night-text px-3 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-desert-gold dark:focus:ring-desert-night-border disabled:opacity-60 disabled:cursor-not-allowed" [class.overflow-y-auto]="atMaxHeight" [class.overflow-hidden]="!atMaxHeight" [(ngModel)]="draft" name="draft" [disabled]="disabled" (keydown)="onKeydown($event)" (input)="autoResize(textareaRef)">`.
- Send `<button type="submit" [disabled]="disabled || draft.trim().length === 0" class="flex items-center justify-center w-9 h-9 rounded-full bg-desert-terracotta dark:bg-desert-night-amber text-white hover:bg-desert-terracotta-dark dark:hover:bg-desert-night-amber-dark active:scale-95 transition shadow-sm disabled:opacity-50 disabled:cursor-not-allowed">` with a `w-4 h-4` arrow SVG + `sr-only` Send label.

Component `@Input`/`@Output` APIs, `submit`, `onKeydown`, `autoResize`, `atMaxHeight`, `draft` ngModel are frozen — change template classes only. The send button `w-9 h-9` size is locked by the UI-SPEC.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Promote the pill to a focus-within ring and polish the send button</name>
  <files>libs/ui/src/lib/input-composer/input-composer.html</files>
  <read_first>
    libs/ui/src/lib/input-composer/input-composer.html
    .planning/phases/07-visual-overhaul/07-UI-SPEC.md (Input pill contract; Interaction States — Send button, Input pill; Color — focus rings; Animation Contract)
  </read_first>
  <action>
    Restyle input-composer.html to the UI-SPEC Input pill contract. Tailwind utilities only — no inline styles.

    PILL CARD `<div>`:
    - Keep `w-full rounded-2xl shadow-xl bg-desert-sand-light dark:bg-desert-night-surface border border-desert-gold/50 dark:border-desert-night-border px-4 py-3`.
    - Add a single polished focus surface: `focus-within:ring-2 focus-within:ring-desert-gold dark:focus-within:ring-desert-night-border` so focusing the textarea lights the whole pill (the UI-SPEC's preferred "single focus surface").

    TEXTAREA:
    - Since the pill now carries the focus ring, REMOVE the per-textarea ring classes `focus:ring-2 focus:ring-desert-gold dark:focus:ring-desert-night-border` to avoid a double ring. Keep `focus:outline-none`.
    - Keep everything else exactly: `flex-1 resize-none rounded-lg border border-desert-border dark:border-desert-night-border bg-desert-sand-light dark:bg-desert-night-surface text-desert-brown dark:text-desert-night-text px-3 py-2 text-sm disabled:opacity-60 disabled:cursor-not-allowed`, the `[class.overflow-y-auto]="atMaxHeight"` / `[class.overflow-hidden]="!atMaxHeight"` bindings, `[(ngModel)]="draft"`, `name="draft"`, `[disabled]`, `(keydown)`, `(input)`, `rows="1"`, `data-testid="message-input"`, `[placeholder]`.

    SEND BUTTON:
    - Keep `flex items-center justify-center w-9 h-9 rounded-full` (size is locked).
    - Keep accent bg `bg-desert-terracotta dark:bg-desert-night-amber text-white` and hover `hover:bg-desert-terracotta-dark dark:hover:bg-desert-night-amber-dark`.
    - Keep `active:scale-95 transition shadow-sm disabled:opacity-50 disabled:cursor-not-allowed` and the `[disabled]="disabled || draft.trim().length === 0"` binding.
    - Add a visible keyboard focus ring: `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-desert-gold dark:focus-visible:ring-desert-night-border focus-visible:ring-offset-2 focus-visible:ring-offset-desert-sand-light dark:focus-visible:ring-offset-desert-night-surface` (offset so the ring reads clearly around the circular accent button).
    - Keep the `w-4 h-4` arrow SVG and the `sr-only` Send label.

    Forbidden: inline `style=""`; arbitrary hex Tailwind classes; changing the `w-9 h-9` send button size; removing `focus:outline-none` from the textarea; new `@Input`/`@Output`; new keyframe animations.
  </action>
  <verify>
    <automated>pnpm nx build frontend --skip-nx-cache 2>&1 | Select-String -Pattern "error|Error" | Select-String -NotMatch "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - input-composer.html pill card `<div>` contains `focus-within:ring-2` and `focus-within:ring-desert-gold`
    - input-composer.html `<textarea>` no longer contains `focus:ring-2` but still contains `focus:outline-none`
    - input-composer.html `<textarea>` keeps `[class.overflow-y-auto]="atMaxHeight"` and `[(ngModel)]="draft"`
    - input-composer.html send `<button>` keeps `w-9 h-9 rounded-full` and `bg-desert-terracotta dark:bg-desert-night-amber`
    - input-composer.html send `<button>` contains `focus-visible:ring-2` and `active:scale-95`
    - input-composer.html send `<button>` keeps `[disabled]="disabled || draft.trim().length === 0"`
    - No inline `style=""` attributes present
    - pnpm nx build frontend exits with 0 errors
  </acceptance_criteria>
  <done>Focusing the textarea lights a single ring around the whole pill; the circular send button has hover, active-press, disabled, and a visible keyboard focus ring; auto-resize behavior is untouched.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static template → Browser | Angular component template rendered in browser DOM |
| User textarea input → ngModel | `draft` two-way bound; submitted via existing `submit()` handler |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-05 | Tampering | input-composer.html | accept | This plan changes only Tailwind class strings; the `[(ngModel)]`, `submit()`, and `onKeydown()` logic are unchanged — no new input-handling surface introduced |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero dependency delta |
</threat_model>

<verification>
After the task completes, run `pnpm nx serve frontend` and open the app:
1. Click into the textarea — a single ring lights up around the whole pill card (not a separate inner textarea ring).
2. Type a multi-line message — the textarea auto-resizes; past max height it scrolls with the cactus scrollbar.
3. Empty draft — the send button is dimmed and non-interactive; type text — it activates.
4. Hover the send button — background darkens; press it — it scales down briefly.
5. Tab to the send button — it shows a visible focus ring with an offset.
6. Send a message via Enter and via the button — both still work.
7. Toggle night mode — pill, ring, and send button all adapt.
</verification>

<success_criteria>
- The input area has a polished send button and a visible focus treatment comparable to Gemini (SC-4)
- The focus ring is a single clean surface on the pill (SC-1)
- Send button press/hover micro-animations are present without harming performance (SC-6)
- Textarea auto-resize, Enter-to-send, and disabled handling still work (SC-7)
- pnpm nx build frontend passes with 0 errors
</success_criteria>

<output>
Create .planning/phases/07-visual-overhaul/07-05-SUMMARY.md when done
</output>
