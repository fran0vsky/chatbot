---
phase: 07-visual-overhaul
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src/app/chat/chat.html
  - apps/frontend/src/styles.scss
autonomous: true
requirements:
  - SC-1
  - SC-5
  - SC-7
must_haves:
  truths:
    - "Landing state shows a vertically centered greeting + input composer in a max-w-2xl column"
    - "Chat state shows a scrollable message list above a sticky floating input pill with a gradient fade"
    - "Decorative cactus silhouettes stay at z-0 behind message content and never intercept pointer events"
    - "The chat scroll area uses the 6px cactus-green themed scrollbar in both day and night mode"
  artifacts:
    - path: "apps/frontend/src/app/chat/chat.html"
      provides: "Refined landing/chat layout shell with consistent max-w-2xl column"
      contains: "max-w-2xl"
    - path: "apps/frontend/src/styles.scss"
      provides: "Cactus-themed thin scrollbar on .chat-scroll-area"
      contains: "scrollbar-width: thin"
  key_links:
    - from: "chat.html sticky input"
      to: "gradient fade"
      via: "bg-gradient-to-t"
      pattern: "from-desert-sand"
---

<objective>
Modernize the chat shell layout — the landing state, the chat-with-messages state, the sticky floating input region, and the scroll region — while preserving the existing cactus silhouette stacking and the desert palette.

Purpose: The shell is the frame every other component sits inside. Getting its column width, vertical rhythm, scroll behavior, and the gradient fade behind the sticky pill right is what makes the whole app read as ChatGPT/Claude-grade polish.
Output: Refined `chat.html` with consistent `max-w-2xl` columns, polished landing vs. chat layout, and a smooth scroll-region gradient fade; `styles.scss` scrollbar verified/tuned.
</objective>

<execution_context>
@C:/Users/Franek/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Franek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-visual-overhaul/07-CONTEXT.md
@.planning/phases/07-visual-overhaul/07-UI-SPEC.md

<interfaces>
<!-- Verified current state. -->

apps/frontend/src/app/chat/chat.html:
- Root `<div class="flex flex-col h-screen bg-desert-sand dark:bg-desert-night">` wraps `app-history-panel`, `app-header-bar`, and `<main>`.
- `<main class="flex-1 overflow-hidden relative flex flex-col">` contains 12 decorative cactus `<svg>` silhouettes (z-0, pointer-events-none, opacity-[0.08–0.12]) followed by the `@if (messages.length <= 1 && !isLoading)` landing branch and the `@else` chat branch.
- Landing branch: `<div class="flex-1 flex flex-col justify-center px-4 pb-12 gap-6 relative z-10">` with two `max-w-2xl mx-auto` child columns (greeting bubble + input composer).
- Chat branch: scroll wrapper `<div class="flex-1 overflow-y-auto px-4 py-4 chat-scroll-area relative z-10">` containing `<div class="max-w-2xl mx-auto w-full space-y-5">` (message list), then a sticky input wrapper `<div class="px-4 flex-shrink-0 sticky bottom-4 bg-gradient-to-t from-desert-sand dark:from-desert-night to-transparent pt-4 pb-4">`.

apps/frontend/src/styles.scss:
- `.chat-scroll-area` already declares `scrollbar-width: thin`, `scrollbar-color`, and `::-webkit-scrollbar` rules (6px, cactus-green thumb via `--scrollbar-thumb`, `:hover { filter: brightness(1.1) }`).

The 12 cactus SVGs, the `app-history-panel`/`app-header-bar` element bindings, and all Angular control-flow blocks must remain functionally unchanged — this is a presentation-only refinement.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refine landing and chat layout states in chat.html</name>
  <files>apps/frontend/src/app/chat/chat.html</files>
  <read_first>
    apps/frontend/src/app/chat/chat.html
    .planning/phases/07-visual-overhaul/07-UI-SPEC.md (Layout contract — Chat shell; Spacing Scale; Color)
  </read_first>
  <action>
    Refine the layout shell in chat.html. Apply only Tailwind utility classes — no inline styles, no new color tokens (all colors via existing `desert-*` named tokens).

    LANDING STATE (`@if (messages.length <= 1 && !isLoading)` branch):
    - Keep the `flex-1 flex flex-col justify-center` vertical centering, `gap-6` between greeting and composer, `pb-12` bottom offset, `relative z-10`.
    - Keep both child columns at `mx-auto w-full max-w-2xl`.
    - Horizontal padding stays `px-4` (the md/16px spacing token).

    CHAT STATE (`@else` branch):
    - Scroll wrapper keeps `flex-1 overflow-y-auto chat-scroll-area relative z-10` and `px-4 py-4`.
    - Message list column keeps `max-w-2xl mx-auto w-full space-y-5` (the 20px inter-message rhythm is locked by the UI-SPEC — do not change `space-y-5`).
    - Sticky input wrapper keeps `sticky bottom-4` with `pt-4 pb-4` and the gradient fade `bg-gradient-to-t from-desert-sand dark:from-desert-night to-transparent`. The gradient MUST remain so messages dissolve under the pill.
    - Input column keeps `max-w-2xl mx-auto w-full` so the pill aligns to the same column as the messages.

    Verify the cactus stacking contract holds: `<main>` keeps `relative`, every cactus `<svg>` keeps `absolute … z-0 … pointer-events-none`, and all message/input content keeps `z-10`. Do not add, remove, or reposition cactus silhouettes in this plan.

    Do not modify the `app-history-panel`, `app-header-bar`, `app-message-bubble`, or `app-input-composer` element bindings — only their surrounding layout containers.

    Forbidden: inline `style=""` attributes; arbitrary hex Tailwind classes like `bg-[#F5E6C8]`; changing `space-y-5`; animating any cactus.
  </action>
  <verify>
    <automated>pnpm nx build frontend --skip-nx-cache 2>&1 | Select-String -Pattern "error|Error" | Select-String -NotMatch "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - chat.html `<main>` element class list contains "relative"
    - chat.html contains exactly the existing 12 cactus `<svg>` elements, each with class containing "z-0" and "pointer-events-none"
    - chat.html landing branch contains `gap-6` and `pb-12`
    - chat.html chat branch message column contains `max-w-2xl mx-auto w-full space-y-5`
    - chat.html sticky input wrapper contains `sticky bottom-4` and `bg-gradient-to-t from-desert-sand dark:from-desert-night to-transparent`
    - No inline `style=""` attributes present in chat.html
    - No arbitrary hex Tailwind classes (`-[#`) present in chat.html
    - pnpm nx build frontend exits with 0 errors
  </acceptance_criteria>
  <done>The landing state centers a greeting + composer in a max-w-2xl column; the chat state scrolls messages above a sticky gradient-faded input pill aligned to the same column. Cactus stacking is intact.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Verify and tune the cactus scrollbar in styles.scss</name>
  <files>apps/frontend/src/styles.scss</files>
  <read_first>
    apps/frontend/src/styles.scss
    .planning/phases/07-visual-overhaul/07-UI-SPEC.md (Interaction States — Scrollbar)
  </read_first>
  <action>
    Confirm the `.chat-scroll-area` scrollbar block matches the UI-SPEC scrollbar contract: 6px width, thin Firefox scrollbar, thumb driven by `--scrollbar-thumb` (cactus-green day / cactus-green-light night), track by `--scrollbar-track`, and `filter: brightness(1.1)` on thumb hover.

    The current block already satisfies this. The only permitted change in this task: keep the block as-is unless a value is wrong. Do NOT introduce new CSS custom properties, do NOT change the desert palette `--color-*` variables, and do NOT alter the Prism import or night-mode override blocks.

    If no change is needed, leave styles.scss byte-identical and record that in the SUMMARY.
  </action>
  <verify>
    <automated>pnpm nx build frontend --skip-nx-cache 2>&1 | Select-String -Pattern "error|Error" | Select-String -NotMatch "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - styles.scss `.chat-scroll-area` contains `scrollbar-width: thin`
    - styles.scss `::-webkit-scrollbar` width is `6px`
    - styles.scss `::-webkit-scrollbar-thumb` background is `var(--scrollbar-thumb)`
    - styles.scss `::-webkit-scrollbar-thumb:hover` contains `filter: brightness(1.1)`
    - The `.day-mode` / `.night-mode` `--color-*` custom properties are unchanged
    - pnpm nx build frontend exits with 0 errors
  </acceptance_criteria>
  <done>The chat scroll area uses the 6px cactus-green themed scrollbar in both modes; no palette variables were altered.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static template/stylesheet → Browser | Angular template + global SCSS rendered in browser DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | chat.html / styles.scss | accept | Static developer-authored markup and CSS; no user input flows into class names or selectors; no script execution surface |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan — zero dependency delta |
</threat_model>

<verification>
After both tasks complete, run `pnpm nx serve frontend` and open the app:
1. Fresh load (landing state) — greeting bubble + input composer are vertically centered in a centered column, not touching screen edges.
2. Send a message — layout switches to chat state; messages scroll, input pill sticks to the bottom with a visible gradient fade behind it.
3. Scroll the message list — messages dissolve under the pill rather than colliding with it.
4. Inspect the scrollbar while scrolling — 6px wide, green thumb; toggle night mode and confirm thumb stays visible.
5. Toggle night mode — background, gradient, and scrollbar all adapt; cactus silhouettes remain faint and behind content.
6. Click/drag over the message area — cactus SVGs do not intercept the pointer.
</verification>

<success_criteria>
- Landing and chat layout states are polished to a modern column-based layout while retaining the desert palette (SC-1, SC-5)
- The sticky input pill has a gradient fade and aligns to the message column (SC-1)
- No regressions in chat send, scrolling, or theme toggle (SC-7)
- pnpm nx build frontend passes with 0 errors
</success_criteria>

<output>
Create .planning/phases/07-visual-overhaul/07-01-SUMMARY.md when done
</output>
