---
phase: 07-visual-overhaul
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/ui/src/lib/header-bar/header-bar.html
  - libs/ui/src/lib/theme-toggle/theme-toggle.html
  - libs/ui/src/lib/new-button/new-button.html
  - libs/ui/src/lib/model-selector/model-selector.html
autonomous: true
requirements:
  - SC-1
  - SC-5
  - SC-7
must_haves:
  truths:
    - "Header chrome icon buttons (history toggle, ThemeToggle, NewButton) share one consistent 32px (w-8 h-8) sizing and hover treatment"
    - "All header interactive controls show a visible focus-visible ring for keyboard users"
    - "The header keeps its subtle bottom divider and the CHATBOT Cinzel title"
    - "The model selector shares the chrome row's border and height"
  artifacts:
    - path: "libs/ui/src/lib/header-bar/header-bar.html"
      provides: "Tightened header control grouping with consistent gaps"
      contains: "border-b"
    - path: "libs/ui/src/lib/theme-toggle/theme-toggle.html"
      provides: "Chrome icon button with focus-visible ring"
      contains: "focus-visible:ring-2"
  key_links:
    - from: "theme-toggle / new-button"
      to: "chrome button styling"
      via: "shared utility class set"
      pattern: "w-8 h-8 rounded"
---

<objective>
Restyle the header bar and its three chrome controls (history toggle button, `ThemeToggle`, `NewButton`, `ModelSelector`) to a consistent, polished control row — uniform 32px icon buttons, consistent hover, and visible keyboard focus rings.

Purpose: The header is the first chrome the user sees. Mismatched button sizes and missing focus rings are the most visible "unfinished" tells; fixing them lifts perceived polish across the whole app.
Output: `header-bar.html`, `theme-toggle.html`, `new-button.html`, `model-selector.html` restyled with one shared chrome-button contract and accessible focus rings.
</objective>

<execution_context>
@C:/Users/Franek/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Franek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-visual-overhaul/07-CONTEXT.md
@.planning/phases/07-visual-overhaul/07-UI-SPEC.md

<interfaces>
<!-- Verified current state. -->

header-bar.html:
- `<header class="flex items-center px-4 py-3 border-b border-desert-border dark:border-desert-night-border bg-desert-header dark:bg-desert-night-surface">`.
- Left group `flex items-center gap-2`: history-toggle `<button>` (`p-1.5 rounded text-desert-brown-muted … hover:bg-desert-border …`, 16px `w-4 h-4` icon) + `<h1 class="font-title text-lg font-semibold uppercase …">Chatbot</h1>`.
- Right group `flex items-center gap-2 ml-auto`: `app-theme-toggle`, `app-new-button`, `app-model-selector` with `[disabled]` bindings and event emitters.

theme-toggle.html / new-button.html:
- Both are a single `<button class="flex items-center justify-center w-8 h-8 rounded text-desert-brown-muted dark:text-desert-night-muted hover:bg-desert-border dark:hover:bg-desert-night-surface transition disabled:opacity-50 disabled:cursor-not-allowed">` with a `w-5 h-5` icon and `sr-only` label.
- NOTE: their hover uses `dark:hover:bg-desert-night-surface`; the header history toggle uses `dark:hover:bg-desert-night-border`. The UI-SPEC icon-button contract is `hover:bg-desert-border dark:hover:bg-desert-night-border` — converge all three on that.

model-selector.html:
- `<select class="appearance-none bg-desert-sand-light dark:bg-desert-night-surface border border-desert-border dark:border-desert-night-border rounded px-2 py-1 text-sm …">` with `@for` options.

The history-toggle button currently uses `p-1.5` (not the `w-8 h-8` chrome size). Component `@Input`/`@Output` APIs are frozen — change templates only.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Converge ThemeToggle and NewButton on the chrome icon-button contract</name>
  <files>libs/ui/src/lib/theme-toggle/theme-toggle.html, libs/ui/src/lib/new-button/new-button.html</files>
  <read_first>
    libs/ui/src/lib/theme-toggle/theme-toggle.html
    libs/ui/src/lib/new-button/new-button.html
    .planning/phases/07-visual-overhaul/07-UI-SPEC.md (Interaction States — Icon buttons; Color — focus rings)
  </read_first>
  <action>
    Apply the UI-SPEC icon-button contract identically to BOTH `theme-toggle.html` and `new-button.html` `<button>` elements. Tailwind utilities only — no inline styles.

    The button class set must be:
    - Sizing/layout: `flex items-center justify-center w-8 h-8 rounded`
    - Idle: `text-desert-brown-muted dark:text-desert-night-muted`, transparent bg
    - Hover: `hover:bg-desert-border dark:hover:bg-desert-night-border hover:text-desert-brown dark:hover:text-desert-night-text` (change the existing `dark:hover:bg-desert-night-surface` to `dark:hover:bg-desert-night-border`)
    - Disabled: `disabled:opacity-50 disabled:cursor-not-allowed`
    - Focus: add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-desert-gold dark:focus-visible:ring-desert-night-border`
    - Transition: `transition-colors`

    Keep the `[disabled]`, `(click)`, `[attr.aria-label]` / `aria-label`, the `w-5 h-5` icon SVGs, the moon/sun `@if (isDayMode)` swap in theme-toggle, and the `sr-only` labels exactly as they are.

    Forbidden: inline `style=""`; arbitrary hex Tailwind classes; the accent terracotta/amber tokens on these chrome buttons (accent is reserved for the send button + user bubbles only).
  </action>
  <verify>
    <automated>pnpm nx build frontend --skip-nx-cache 2>&1 | Select-String -Pattern "error|Error" | Select-String -NotMatch "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - theme-toggle.html button class contains `w-8 h-8 rounded`
    - new-button.html button class contains `w-8 h-8 rounded`
    - Both buttons contain `focus-visible:ring-2` and `focus-visible:ring-desert-gold`
    - Both buttons contain `dark:hover:bg-desert-night-border` (and no longer `dark:hover:bg-desert-night-surface`)
    - Neither button uses `desert-terracotta`, `desert-night-amber`, or any accent token
    - theme-toggle.html still has the `@if (isDayMode)` moon/sun swap and `sr-only` label
    - No inline `style=""` attributes present
    - pnpm nx build frontend exits with 0 errors
  </acceptance_criteria>
  <done>ThemeToggle and NewButton are visually identical 32px chrome icon buttons with consistent hover and a visible keyboard focus ring.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tighten header-bar layout and model-selector chrome consistency</name>
  <files>libs/ui/src/lib/header-bar/header-bar.html, libs/ui/src/lib/model-selector/model-selector.html</files>
  <read_first>
    libs/ui/src/lib/header-bar/header-bar.html
    libs/ui/src/lib/model-selector/model-selector.html
    .planning/phases/07-visual-overhaul/07-UI-SPEC.md (Component Inventory — HeaderBar / ModelSelector; Interaction States — Icon buttons; Typography)
  </read_first>
  <action>
    HEADER-BAR (header-bar.html):
    - Keep `<header>` padding `px-4 py-3`, the `border-b border-desert-border dark:border-desert-night-border` divider, and `bg-desert-header dark:bg-desert-night-surface`.
    - Keep the `<h1>` exactly: `font-title text-lg font-semibold uppercase text-desert-brown dark:text-desert-night-text` reading `Chatbot`.
    - Restyle the history-toggle `<button>` to the same chrome icon-button contract as Task 1: replace `p-1.5` with `flex items-center justify-center w-8 h-8`, keep `rounded`, keep `text-desert-brown-muted dark:text-desert-night-muted`, set hover to `hover:bg-desert-border dark:hover:bg-desert-night-border hover:text-desert-brown dark:hover:text-desert-night-text`, add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-desert-gold dark:focus-visible:ring-desert-night-border`, keep `disabled:opacity-50 disabled:cursor-not-allowed transition-colors`. Keep its `w-4 h-4` icon, `[disabled]`, `(click)`, and `aria-label`.
    - Keep both control groups' `gap-2` and the `ml-auto` on the right group.

    MODEL-SELECTOR (model-selector.html):
    - Keep `appearance-none`, the `@for` options, `[value]`, `(change)`, `[disabled]` binding.
    - Keep `bg-desert-sand-light dark:bg-desert-night-surface border border-desert-border dark:border-desert-night-border rounded text-sm text-desert-brown dark:text-desert-night-text`.
    - Add a visible focus ring: `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-desert-gold dark:focus-visible:ring-desert-night-border` (Gemini-style ring). Keep `cursor-pointer disabled:opacity-50 disabled:cursor-not-allowed`.
    - Keep padding `px-2 py-1` so it sits at a consistent height with the chrome row.

    Forbidden: inline `style=""`; arbitrary hex Tailwind classes; accent tokens on chrome; new `@Input`/`@Output`; `font-medium` / `font-bold` (only 400/600 weights allowed).
  </action>
  <verify>
    <automated>pnpm nx build frontend --skip-nx-cache 2>&1 | Select-String -Pattern "error|Error" | Select-String -NotMatch "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - header-bar.html history-toggle button class contains `w-8 h-8` and no longer contains `p-1.5`
    - header-bar.html history-toggle button contains `focus-visible:ring-2`
    - header-bar.html `<header>` still contains `border-b` and `bg-desert-header dark:bg-desert-night-surface`
    - header-bar.html `<h1>` contains `font-title text-lg font-semibold uppercase`
    - model-selector.html `<select>` contains `focus-visible:ring-2` and `focus-visible:ring-desert-gold`
    - model-selector.html `<select>` still contains `appearance-none` and the `@for` options block
    - No `font-medium` or `font-bold` in either file
    - No inline `style=""` attributes present
    - pnpm nx build frontend exits with 0 errors
  </acceptance_criteria>
  <done>The header is a tight control row: 32px chrome buttons with consistent hover/focus, a subtle bottom divider, the Cinzel CHATBOT title, and a focus-ringed model selector at consistent height.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static template → Browser | Angular component templates rendered in browser DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-02 | Tampering | header/chrome templates | accept | Static developer-authored markup; no user input in class strings; component `@Input`/`@Output` APIs unchanged |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero dependency delta |
</threat_model>

<verification>
After both tasks complete, run `pnpm nx serve frontend` and open the app:
1. Header — history toggle, theme toggle, and new-chat button are the same size and align on one row.
2. Tab through the header with the keyboard — every control shows a visible gold (day) / border (night) focus ring.
3. Hover each chrome button — consistent background tint, no accent (terracotta/amber) color appears.
4. Open the model selector — it shows a focus ring and sits at the same height as the chrome buttons.
5. Toggle night mode — header divider, button hovers, and focus rings all adapt.
6. Confirm theme toggle, new chat, model change still function (no regressions).
</verification>

<success_criteria>
- Header chrome controls are uniformly sized and styled with consistent hover (SC-1, SC-5)
- All header controls have visible keyboard focus rings (SC-1)
- Theme toggle, new chat, and model selection still work (SC-7)
- pnpm nx build frontend passes with 0 errors
</success_criteria>

<output>
Create .planning/phases/07-visual-overhaul/07-02-SUMMARY.md when done
</output>
