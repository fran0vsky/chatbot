---
phase: 07-visual-overhaul
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/ui/src/lib/message-bubble/message-bubble.html
  - libs/ui/src/lib/typing-indicator/typing-indicator.html
autonomous: true
requirements:
  - SC-1
  - SC-3
  - SC-5
  - SC-6
  - SC-7
must_haves:
  truths:
    - "User and assistant message bubbles are clearly distinct — accent right-aligned bubble vs. parchment left-aligned bubble with avatar, Claude-like spaciousness"
    - "Every bubble variant keeps the single-flattened-corner speech-tail radius"
    - "The typing indicator shares the assistant bubble's styling and snake avatar"
    - "Message entry slide-in animations are preserved and respect prefers-reduced-motion"
  artifacts:
    - path: "libs/ui/src/lib/message-bubble/message-bubble.html"
      provides: "Refined user/assistant/error/typing bubble styling"
      contains: "rounded-2xl"
    - path: "libs/ui/src/lib/typing-indicator/typing-indicator.html"
      provides: "Typing bubble aligned with assistant bubble + snake avatar"
      contains: "creature.png"
  key_links:
    - from: "message bubble variants"
      to: "speech-tail cue"
      via: "single-corner radius"
      pattern: "rounded-br-sm"
---

<objective>
Refine the `MessageBubble` variants (user / assistant / error / typing) and the `TypingIndicator` so human and AI messages have a clear, Claude-grade distinction — spacious padding, gentle elevation, tidy avatar alignment — while preserving markdown, the copy button, and entry animations.

Purpose: Message bubbles are the core surface of a chatbot and a Success Criterion (SC-3). Clear human/AI distinction and spacious layout are what make a chat feel premium.
Output: Restyled `message-bubble.html`; `typing-indicator.html` aligned to the assistant bubble with the shared snake avatar.
</objective>

<execution_context>
@C:/Users/Franek/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Franek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-visual-overhaul/07-CONTEXT.md
@.planning/phases/07-visual-overhaul/07-UI-SPEC.md

<interfaces>
<!-- Verified current state. -->

message-bubble.html — four `@if`/`@else if` branches keyed on `message.role` and `typing`:
- User: `<div class="flex w-full justify-end mb-4" [class.msg-enter-right]="animate">` wrapping `<div class="max-w-[75%] rounded-2xl rounded-br-sm px-5 py-3 bg-desert-terracotta dark:bg-desert-night-amber text-white whitespace-pre-wrap break-words shadow-md font-body leading-relaxed">{{ message.text }}</div>`.
- Error: `justify-start mb-4` + `[class.msg-enter-left]` wrapping `max-w-[75%] rounded-2xl rounded-bl-sm px-4 py-2 bg-red-50 border border-red-200 text-red-700 …` with a warning SVG.
- Typing: `<div class="mb-4" [class.msg-enter-left]="animate"><app-typing-indicator /></div>`.
- Assistant: `<div class="flex w-full justify-start items-end gap-2 mb-4" [class.msg-enter-left]="animate">` with avatar `<img src="/assets/creatures/creature.png" alt="Chatbot" class="w-16 h-auto" style="image-rendering: pixelated;" />` and bubble `<div class="group relative markdown-bubble max-w-[75%] rounded-2xl rounded-bl-sm px-5 py-4 pr-9 bg-desert-parchment dark:bg-desert-night-parchment text-desert-brown dark:text-desert-night-text border border-desert-gold/40 dark:border-desert-night-border shadow-md dark:shadow-black/30 break-words text-sm font-body leading-relaxed">` containing `<markdown>` + a `group-hover` copy `<button>`.

message-bubble.scss — `.markdown-bubble` styles, `@keyframes msg-slide-fade-right/left`, `.msg-enter-right/left { animation: … 150ms ease-out }`, and an existing `@media (prefers-reduced-motion: reduce)` block disabling those animations. The reduced-motion guard ALREADY EXISTS — do not duplicate it.

typing-indicator.html — currently `<div class="flex w-full justify-start items-end gap-2">` with an OLD inline 8×8 `<svg viewBox="0 0 8 8">` pixel snake and a bubble `<div class="max-w-[75%] rounded-2xl rounded-bl-sm px-4 py-2 bg-desert-parchment dark:bg-desert-night-parchment …">` of 3 `animate-bounce dot-delay-*` dots.

The chat shell now uses `space-y-5` (20px) between bubbles, so per-bubble `mb-4` is redundant vertical spacing — see Task 1. Component `@Input`/`@Output` APIs are frozen. The `style="image-rendering: pixelated"` inline attribute on the avatar `<img>` is the one permitted inline-style exception.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refine message-bubble.html variant styling</name>
  <files>libs/ui/src/lib/message-bubble/message-bubble.html</files>
  <read_first>
    libs/ui/src/lib/message-bubble/message-bubble.html
    libs/ui/src/lib/message-bubble/message-bubble.scss
    .planning/phases/07-visual-overhaul/07-UI-SPEC.md (Message bubble contract; Color; Typography; Spacing Scale; Animation Contract)
  </read_first>
  <action>
    Restyle message-bubble.html to the UI-SPEC Message bubble contract. Tailwind utilities only — no inline styles except the permitted `image-rendering: pixelated` on the avatar `<img>`.

    SHARED — inter-message spacing: the chat shell column uses `space-y-5` for vertical rhythm. Remove the per-bubble `mb-4` from ALL four outer wrapper `<div>`s (user, error, typing, assistant) so spacing is owned solely by the parent `space-y-5`. Do not add any replacement margin.

    USER bubble:
    - Keep `flex w-full justify-end` + `[class.msg-enter-right]="animate"`.
    - Bubble: `max-w-[75%] rounded-2xl rounded-br-sm px-5 py-3 bg-desert-terracotta dark:bg-desert-night-amber text-white whitespace-pre-wrap break-words shadow-md font-body text-sm leading-relaxed`. (Ensure `text-sm` is present — body text is 14px.)

    ASSISTANT bubble:
    - Keep `flex w-full justify-start items-end gap-2` + `[class.msg-enter-left]="animate"`.
    - Keep the avatar `<img src="/assets/creatures/creature.png" alt="Chatbot" class="w-16 h-auto" style="image-rendering: pixelated;" />` and its `flex-shrink-0 self-center` wrapper.
    - Bubble: keep `group relative markdown-bubble max-w-[75%] rounded-2xl rounded-bl-sm px-5 py-4 pr-9 bg-desert-parchment dark:bg-desert-night-parchment text-desert-brown dark:text-desert-night-text border border-desert-gold/40 dark:border-desert-night-border shadow-md dark:shadow-black/30 break-words text-sm font-body leading-relaxed`.
    - Copy button: keep `absolute top-1 right-1 p-1 rounded opacity-0 group-hover:opacity-100 transition-opacity`; add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-desert-gold dark:focus-visible:ring-desert-night-border focus-visible:opacity-100`. Keep both copy/check icon `@if (copied)` SVGs, `[attr.aria-label]`, and the `sr-only` span.
    - Keep the `<markdown [data]="message.text" (ready)="onMarkdownReady()">`.

    ERROR bubble:
    - Keep `flex w-full justify-start` + `[class.msg-enter-left]="animate"`.
    - Bubble: `max-w-[75%] rounded-2xl rounded-bl-sm px-4 py-2 bg-red-50 border border-red-200 text-red-700 text-sm whitespace-pre-wrap break-words flex items-start gap-2` + the warning triangle SVG. (Error keeps Tailwind default red — it is the documented destructive exception.)

    TYPING branch: keep `<div [class.msg-enter-left]="animate"><app-typing-indicator /></div>` (mb-4 removed per the shared rule).

    The single-flattened-corner radius (`rounded-br-sm` user, `rounded-bl-sm` assistant/error) is the speech-tail cue — it MUST stay on every variant.

    Forbidden: inline `style=""` (except the avatar `image-rendering`); arbitrary hex Tailwind classes; accent terracotta/amber on anything other than the user bubble background; `font-medium` / `font-bold`; new `@Input`/`@Output`; new keyframe animations.
  </action>
  <verify>
    <automated>pnpm nx build frontend --skip-nx-cache 2>&1 | Select-String -Pattern "error|Error" | Select-String -NotMatch "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - message-bubble.html user bubble contains `rounded-2xl rounded-br-sm` and `bg-desert-terracotta dark:bg-desert-night-amber`
    - message-bubble.html assistant bubble contains `rounded-2xl rounded-bl-sm` and `bg-desert-parchment dark:bg-desert-night-parchment`
    - message-bubble.html error bubble contains `rounded-2xl rounded-bl-sm` and `bg-red-50`
    - No `mb-4` class appears anywhere in message-bubble.html
    - All text-bearing bubbles contain `text-sm` and `leading-relaxed`
    - The copy button contains `focus-visible:ring-2` and `focus-visible:opacity-100`
    - The avatar `<img>` keeps `src="/assets/creatures/creature.png"` and `alt="Chatbot"`
    - The only inline `style=""` in the file is `image-rendering: pixelated` on the avatar `<img>`
    - No `font-medium` or `font-bold` in the file
    - pnpm nx build frontend exits with 0 errors
  </acceptance_criteria>
  <done>User bubbles are accent right-aligned, assistant bubbles are parchment left-aligned with avatar, error bubbles are red — all with the speech-tail radius, 14px body text, and entry animations intact. Vertical spacing is owned by the parent column.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Align typing-indicator.html with the assistant bubble and shared avatar</name>
  <files>libs/ui/src/lib/typing-indicator/typing-indicator.html</files>
  <read_first>
    libs/ui/src/lib/typing-indicator/typing-indicator.html
    libs/ui/src/lib/message-bubble/message-bubble.html
    .planning/phases/07-visual-overhaul/07-UI-SPEC.md (Message bubble contract — Typing row; Animation Contract)
  </read_first>
  <action>
    Update typing-indicator.html so the typing bubble visually matches the assistant bubble and uses the SAME snake avatar as the assistant message.

    - Keep the outer `<div class="flex w-full justify-start items-end gap-2">`.
    - Replace the OLD inline 8×8 `<svg viewBox="0 0 8 8">` pixel snake (and its `flex-shrink-0 self-end mb-1` wrapper) with the same avatar markup the assistant bubble uses:
      `<div class="flex-shrink-0 self-center"><img src="/assets/creatures/creature.png" alt="Chatbot" class="w-16 h-auto" style="image-rendering: pixelated;" /></div>`
      (the `image-rendering: pixelated` inline style is the one permitted exception).
    - The dots bubble: keep the 3 `<span … animate-bounce dot-delay-0/150/300>` dots. Update the bubble `<div>` to match the assistant bubble surface: `max-w-[75%] rounded-2xl rounded-bl-sm px-4 py-2 bg-desert-parchment dark:bg-desert-night-parchment border border-desert-gold/40 dark:border-desert-night-border shadow-md dark:shadow-black/30 flex items-center gap-1`.
    - Keep the dots' colors `bg-desert-brown-muted dark:bg-desert-night-muted` and the `dot-delay-*` classes (defined in typing-indicator.scss — leave that file unchanged).

    Forbidden: inline `style=""` except the avatar `image-rendering`; arbitrary hex Tailwind classes; new keyframe animations; changing typing-indicator.scss.
  </action>
  <verify>
    <automated>pnpm nx build frontend --skip-nx-cache 2>&1 | Select-String -Pattern "error|Error" | Select-String -NotMatch "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - typing-indicator.html contains `src="/assets/creatures/creature.png"` and `alt="Chatbot"`
    - typing-indicator.html no longer contains `viewBox="0 0 8 8"`
    - typing-indicator.html bubble `<div>` contains `rounded-2xl rounded-bl-sm` and `border border-desert-gold/40 dark:border-desert-night-border`
    - typing-indicator.html still contains 3 spans with `animate-bounce` and the `dot-delay-0` / `dot-delay-150` / `dot-delay-300` classes
    - The only inline `style=""` is `image-rendering: pixelated` on the avatar `<img>`
    - pnpm nx build frontend exits with 0 errors
  </acceptance_criteria>
  <done>The typing indicator shows the same snake avatar as assistant messages beside a parchment bubble with 3 bouncing dots, visually consistent with a real assistant bubble.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static template → Browser | Angular component templates rendered in browser DOM |
| Message text → Markdown render | `message.text` rendered by `<markdown>` (ngx-markdown) in assistant bubbles |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering | message-bubble.html | accept | This plan changes only Tailwind class strings; the `<markdown>` binding and ngx-markdown sanitization behavior are unchanged — no new HTML-injection surface introduced |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero dependency delta |
</threat_model>

<verification>
After both tasks complete, run `pnpm nx serve frontend` and open the app:
1. Send a message — the user bubble is accent-colored, right-aligned, with a flattened bottom-right corner.
2. Wait for the reply — the assistant bubble is parchment, left-aligned, with the snake avatar and flattened bottom-left corner; markdown renders.
3. While awaiting the reply — the typing indicator shows the same snake avatar + a parchment bubble with 3 bouncing dots.
4. Hover an assistant bubble — the copy button appears; Tab to it — it shows a focus ring.
5. Trigger a network error — the error bubble is red with a warning icon.
6. Send several messages — vertical spacing between bubbles is even (owned by `space-y-5`), no doubled gaps.
7. Enable OS "reduce motion" — message entry slide-in animations are disabled.
8. Toggle night mode — all bubble variants adapt.
</verification>

<success_criteria>
- User and assistant bubbles have a clear, Claude-like distinction with spacious layout (SC-3)
- Typography (14px body, 2 weights) and spacing are consistent (SC-5)
- Entry micro-animations are preserved and respect reduced-motion (SC-6)
- Markdown, copy button, and error rendering still work (SC-1, SC-7)
- pnpm nx build frontend passes with 0 errors
</success_criteria>

<output>
Create .planning/phases/07-visual-overhaul/07-04-SUMMARY.md when done
</output>
