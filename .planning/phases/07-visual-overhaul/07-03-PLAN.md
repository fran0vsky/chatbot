---
phase: 07-visual-overhaul
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/ui/src/lib/history-panel/history-panel.html
autonomous: true
requirements:
  - SC-1
  - SC-2
  - SC-5
  - SC-7
must_haves:
  truths:
    - "Session rows have a subtle hover state and a clearly distinct active state, comparable to ChatGPT's sidebar"
    - "The delete button stays hidden until row hover and has a visible focus ring"
    - "The New chat button uses font-semibold (not font-medium) per the 2-weight typography rule"
    - "Drawer slide and backdrop scrim transitions are smooth"
  artifacts:
    - path: "libs/ui/src/lib/history-panel/history-panel.html"
      provides: "ChatGPT-grade sidebar with refined session-row hover and active state"
      contains: "group-hover:opacity-100"
  key_links:
    - from: "session row"
      to: "active state"
      via: "conditional class binding"
      pattern: "bg-desert-parchment"
---

<objective>
Elevate the `HistoryPanel` overlay sidebar to ChatGPT-grade polish — refined session-row hover, a clearer active state, a polished delete affordance, consistent 8-point padding, and a smoother drawer slide.

Purpose: The history sidebar is a Success Criterion in its own right (SC-2). It must feel as crisp as ChatGPT's conversation list.
Output: Restyled `history-panel.html` with refined interaction states, an accessible delete button, and a `font-semibold` New-chat button.
</objective>

<execution_context>
@C:/Users/Franek/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Franek/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-visual-overhaul/07-CONTEXT.md
@.planning/phases/07-visual-overhaul/07-UI-SPEC.md

<interfaces>
<!-- Verified current state. -->

history-panel.html:
- Backdrop scrim `@if (open)`: `<div class="fixed inset-0 bg-black/40 z-30" (click)="closed.emit()" aria-hidden="true">`.
- Drawer `<aside class="fixed inset-y-0 left-0 z-40 w-72 flex flex-col bg-desert-header dark:bg-desert-night-surface border-r border-desert-border dark:border-desert-night-border transition-transform duration-300 ease-in-out">` with `[class.translate-x-0]="open"` / `[class.-translate-x-full]="!open"`.
- Header row: `<h2 class="font-title text-sm font-semibold uppercase … tracking-wide">History</h2>` + close `<button class="p-1 rounded text-desert-brown-muted … hover:bg-desert-border …">`.
- New-chat button: `<button class="w-full flex items-center gap-2 px-3 py-2 rounded text-sm font-medium text-desert-brown … hover:bg-desert-border …">` — USES `font-medium` (violates 2-weight rule).
- Session row: `<div class="group relative flex items-start gap-2 px-3 py-2 mx-1 my-0.5 rounded cursor-pointer transition-colors">` with `[class.bg-desert-parchment]` / `[class.dark:bg-desert-night-parchment]` for active, `[class.hover:bg-desert-border]` / `[class.dark:hover:bg-desert-night-border]` for inactive, `role="button"`, `[attr.aria-current]`.
- Delete `<button class="flex-shrink-0 mt-0.5 p-0.5 rounded opacity-0 group-hover:opacity-100 text-desert-brown-muted … transition-opacity" aria-label="Delete conversation">`.
- `@empty` state: `<p>No conversations yet</p>`.

Component `@Input`/`@Output` APIs are frozen; `formatDate`, `onDeleteClick`, all event emitters stay. Delete stays immediate (no confirm modal) per the UI-SPEC copywriting contract.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Refine session rows, active state, and delete affordance in history-panel.html</name>
  <files>libs/ui/src/lib/history-panel/history-panel.html</files>
  <read_first>
    libs/ui/src/lib/history-panel/history-panel.html
    .planning/phases/07-visual-overhaul/07-UI-SPEC.md (Sidebar contract; Interaction States — Session row, Icon buttons; Typography; Animation Contract)
  </read_first>
  <action>
    Restyle history-panel.html. Tailwind utilities only — no inline styles, no new color tokens.

    NEW-CHAT BUTTON:
    - Change `font-medium` to `font-semibold` (the 2-weight typography rule allows only 400/600).
    - Keep `w-full flex items-center gap-2 px-3 py-2 rounded text-sm`, the icon, and the `New chat` label.
    - Set hover to `hover:bg-desert-border dark:hover:bg-desert-night-border` and add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-desert-gold dark:focus-visible:ring-desert-night-border transition-colors`.

    SESSION ROW:
    - Keep `group relative flex items-start gap-2 px-3 py-2 mx-1 my-0.5 rounded cursor-pointer transition-colors`, `role="button"`, `[attr.aria-current]`, `(click)`.
    - Active state: keep `[class.bg-desert-parchment]="session.id === activeSessionId"` and `[class.dark:bg-desert-night-parchment]="…"`. To make the active row clearer, also bind a subtle accent-free emphasis — add `[class.font-semibold]` on the session-title `<p>` for the active row title (executor: bind it to the same `session.id === activeSessionId` condition). Do not use accent terracotta/amber on the row.
    - Inactive hover: keep `[class.hover:bg-desert-border]` / `[class.dark:hover:bg-desert-night-border]` bound to the inactive condition.
    - Keep the title `<p class="text-sm … truncate leading-snug">` and timestamp `<p class="text-xs …">`.

    DELETE BUTTON:
    - Keep `flex-shrink-0 mt-0.5 p-0.5 rounded opacity-0 group-hover:opacity-100 transition-opacity`, `aria-label="Delete conversation"`, `(click)="onDeleteClick(...)"`, the trash icon.
    - Add a visible focus ring so keyboard users can see it: `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-desert-gold dark:focus-visible:ring-desert-night-border focus-visible:opacity-100` (the `focus-visible:opacity-100` makes it appear when focused via keyboard even without a row hover).
    - Keep `text-desert-brown-muted dark:text-desert-night-muted hover:text-desert-brown dark:hover:text-desert-night-text`.

    CLOSE BUTTON: add `focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-desert-gold dark:focus-visible:ring-desert-night-border` to the existing close `<button>`. Keep its other classes.

    DRAWER + SCRIM: keep `transition-transform duration-300 ease-in-out` on the `<aside>` and the `translate-x` class bindings. On the backdrop scrim `<div>`, add `transition-opacity` so the scrim fades smoothly with the drawer (refinement permitted by the Animation Contract). Keep `bg-black/40 z-30` and the `(click)="closed.emit()"`.

    Forbidden: inline `style=""`; arbitrary hex Tailwind classes; accent terracotta/amber tokens on any sidebar element; `font-medium` / `font-bold`; new `@Input`/`@Output`; a delete confirmation modal.
  </action>
  <verify>
    <automated>pnpm nx build frontend --skip-nx-cache 2>&1 | Select-String -Pattern "error|Error" | Select-String -NotMatch "0 errors"</automated>
  </verify>
  <acceptance_criteria>
    - history-panel.html New-chat button uses `font-semibold` and no longer contains `font-medium`
    - history-panel.html contains no `font-medium` or `font-bold` anywhere
    - The session-title `<p>` has a `[class.font-semibold]` binding tied to the active-session condition
    - The session row keeps `[class.bg-desert-parchment]` and `[attr.aria-current]`
    - The delete button keeps `opacity-0 group-hover:opacity-100` and adds `focus-visible:ring-2` and `focus-visible:opacity-100`
    - The close button and New-chat button both contain `focus-visible:ring-2`
    - The backdrop scrim `<div>` contains `transition-opacity`
    - The `<aside>` keeps `transition-transform duration-300 ease-in-out`
    - No accent token (`desert-terracotta`, `desert-night-amber`) appears in history-panel.html
    - No inline `style=""` attributes present
    - pnpm nx build frontend exits with 0 errors
  </acceptance_criteria>
  <done>The sidebar has subtle hover, a clearly emphasized active row, a keyboard-accessible delete button revealed on hover, a font-semibold New-chat button, and a smoothly fading scrim.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Static template → Browser | Angular component template rendered in browser DOM |
| Session data → Template | `session.title` / `session.createdAt` rendered via Angular interpolation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | history-panel.html | accept | Static markup; `session.*` values rendered via `{{ }}` interpolation which auto-escapes — no raw HTML binding introduced |
| T-07-SC | Tampering | npm/pip/cargo installs | accept | No package installs — zero dependency delta |
</threat_model>

<verification>
After the task completes, run `pnpm nx serve frontend` and open the app:
1. Open the history drawer — it slides in over 300ms; the backdrop scrim fades smoothly.
2. Create 2-3 sessions — hover an inactive row: subtle background tint; the active row is clearly emphasized (background + bold title).
3. Hover a row — the delete (trash) button fades in; move away — it fades out.
4. Tab to the delete button with the keyboard — it becomes visible and shows a focus ring.
5. Click delete — the conversation is removed immediately (no modal), as before.
6. Toggle night mode — drawer, rows, hover, active state, and focus rings all adapt.
7. Confirm session switching and new-chat still work.
</verification>

<success_criteria>
- The sidebar shows conversation history with subtle hover states comparable to ChatGPT (SC-2)
- Active vs. inactive rows are clearly distinguishable; delete is accessible (SC-1, SC-2)
- Typography complies with the 2-weight rule (SC-5)
- Session switching, deletion, and new-chat still work (SC-7)
- pnpm nx build frontend passes with 0 errors
</success_criteria>

<output>
Create .planning/phases/07-visual-overhaul/07-03-SUMMARY.md when done
</output>
