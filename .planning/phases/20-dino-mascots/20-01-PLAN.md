---
phase: 20-dino-mascots
plan: 01
type: execute
wave: 1
depends_on:
  - 19-01
files_modified:
  - scripts/split-mascot.js
  - scripts/optimize-spino-assets.js
  - apps/frontend/public/spino/dinos/*
  - libs/ui/src/lib/mascot/mascot.ts
  - libs/ui/src/lib/mascot/mascot.html
  - libs/ui/src/lib/dino-card/dino-card.ts
  - libs/ui/src/lib/dino-card/dino-card.html
  - apps/frontend/src/app/chat/chat.html
autonomous: false
requirements:
  - MASC-06
  - MASC-07
  - MASC-08

must_haves:
  truths:
    - "Each of the >=4 dinos has a unique pixel-art mascot of a distinct dinosaur species, in the same style as dual-mascot.png"
    - "Each mascot has day + night palette variants generated through the split/optimize pipeline"
    - "Mascots appear keyed by dino in the picker, the Explore gallery (DinoCard), and the assistant message bubble"
    - "Mascots render crisply (integer/pixelated scaling, no smoothing artifacts)"
  artifacts:
    - path: "apps/frontend/public/spino/dinos/"
      provides: "Per-dino day/night mascot PNGs named by dino id (e.g. rexford-day.png, rexford-night.png)"
    - path: "libs/ui/src/lib/mascot/mascot.ts"
      provides: "Mascot component accepts a species/dinoId + theme and renders the matching asset"
  key_links:
    - from: "DinoCard / message bubble"
      to: "public/spino/dinos/{id}-{theme}.png"
      via: "Mascot component input bound to dino.id + active theme"
      pattern: "<app-mascot [dinoId]=... [theme]=...>"
---

<objective>
Give each dino a unique pixel-art mascot in the exact style of `dual-mascot.png` (a distinct dinosaur species), with day + night variants produced through the existing asset pipeline, and surface those mascots wherever a dino appears (picker, Explore card, chat bubble). Sequenced AFTER dinos are wired (Phases 18–19), per the maker's explicit ask.

Purpose: Visual identity per dino — the part that makes the platform feel like a cast of characters.
Output: New per-dino mascot assets + a Mascot component that selects the right asset by dino + theme + integration points updated.

NOTE: This phase has a HUMAN ART step. The pixel-art generation is a creative/asset task done by the maker (Franek) — the agent's job is to define the art spec, wire the pipeline, name/optimize assets, and integrate. Mark art-generation tasks `manual`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-dino-abstraction/18-01-PLAN.md
Existing pipeline: a single stacked `apps/frontend/public/spino/dual-mascot.png` (day sprite on top, night on bottom, on a black background) is split by `scripts/split-mascot.js` into `mascot-day.png` / `mascot-night.png` (black keyed to transparent, trimmed), then `scripts/optimize-spino-assets.js` resizes for the app. The Mascot component currently renders the single Spino. Dinos created in Phase 18 each have a `species` field intended to drive mascot art.
</context>

<art_spec>
Each new dino mascot MUST match the established style so the cast feels unified:
- Pixel-art, same resolution/scale and rendering as `dual-mascot.png` (study it first).
- One PNG per dino in the SAME stacked dual format: day palette (warm/sunlit) on top, night palette (deep teal/blue, cooler) on bottom, on a solid black background for keying.
- Distinct species per dino, matching the Phase 18 roster (e.g. Tyrannosaurus=Rexford, Velociraptor=Veloce, Stegosaurus=Glyphos, Pteranodon=Nimbus). Keep silhouette readable at small (~48px) sizes.
- Day/night palettes should echo the existing Spino day (green) / night (teal) treatment so the set is cohesive.
</art_spec>

<tasks>

<task type="manual" autonomous="false">
  <name>Task 1: Generate per-dino dual mascots (human art step)</name>
  <files>apps/frontend/public/spino/dinos/_src/{id}-dual.png</files>
  <read_first>
    apps/frontend/public/spino/dual-mascot.png
    scripts/split-mascot.js
  </read_first>
  <action>
  For each dino id (rexford, veloce, glyphos, nimbus, + any added later), create a stacked dual-mascot PNG following <art_spec> and save it as `apps/frontend/public/spino/dinos/_src/{id}-dual.png`. Use the same generation approach you use for the Spino (your pixel-art tool / model of choice), matched to the reference style. This is offline creative work — it does not consume agent/LLM usage and can be done between coding rotations.
  </action>
  <acceptance_criteria>
    - One `_src/{id}-dual.png` exists per dino, stacked day-over-night on black, in the dual-mascot.png style
    - Species are distinct and match the roster
  </acceptance_criteria>
  <done>Source art for every dino mascot is ready to run through the pipeline.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize the split/optimize scripts for multiple dinos</name>
  <files>scripts/split-mascot.js, scripts/optimize-spino-assets.js</files>
  <read_first>
    scripts/split-mascot.js
    scripts/optimize-spino-assets.js
  </read_first>
  <behavior>
    - `split-mascot.js` can split any `_src/{id}-dual.png` into `dinos/{id}-day.png` and `dinos/{id}-night.png` using the same black-keying + trim logic, in addition to the existing Spino behavior.
    - Accept an optional CLI arg (a dino id or `--all`) or iterate every file in `_src/`. Default behavior (no arg) should remain backwards compatible for the original dual-mascot.png.
    - `optimize-spino-assets.js` likewise processes the new `dinos/*.png` outputs.
  </behavior>
  <action>
  Refactor the core of split-mascot.js (the `processHalf` logic) into a function reusable per source file, then loop over `_src/*-dual.png` writing `{id}-day.png`/`{id}-night.png` into `public/spino/dinos/`. Preserve the original single-file path. Mirror the change in optimize-spino-assets.js so the new assets get resized.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && node scripts/split-mascot.js --all 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Running the split script produces `dinos/{id}-day.png` and `dinos/{id}-night.png` for every `_src/{id}-dual.png`
    - The original dual-mascot.png → mascot-day/night path still works
  </acceptance_criteria>
  <done>The pipeline scales from one Spino to N dinos with one command.</done>
</task>

<task type="auto">
  <name>Task 3: Mascot component selects asset by dino + theme</name>
  <files>libs/ui/src/lib/mascot/mascot.ts, libs/ui/src/lib/mascot/mascot.html, libs/ui/src/lib/mascot/mascot.stories.ts</files>
  <read_first>
    libs/ui/src/lib/mascot/mascot.ts
    libs/ui/src/lib/mascot/mascot.html
    libs/ui/src/lib/mascot/mascot.stories.ts
  </read_first>
  <behavior>
    - Mascot gains an optional `dinoId` input. When set, it renders `spino/dinos/{dinoId}-{theme}.png`; otherwise it falls back to the existing Spino asset (backwards compatible).
    - Theme (day/night) resolves the same way the app currently picks mascot-day vs mascot-night.
    - Crisp rendering: ensure `image-rendering: pixelated` (Tailwind arbitrary value or existing class) is applied so scaling stays sharp (MASC-02 consistency).
    - Add/extend the Storybook story to show a couple of dino mascots in both themes.
  </behavior>
  <action>
  - Add `dinoId?: string` input. Compute the asset path; guard for a missing asset (fall back to the Spino image so a not-yet-drawn dino still renders something).
  - Keep the component presentational (no services). Update the story with dino examples.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint ui --quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - Mascot renders `{dinoId}-{theme}.png` when `dinoId` is provided, Spino otherwise
    - Pixelated rendering preserved
    - Story shows dino mascots; `pnpm nx lint ui` passes
  </acceptance_criteria>
  <done>One Mascot component renders the right species art for any dino and theme.</done>
</task>

<task type="auto">
  <name>Task 4: Bind dino mascots into card, picker, and chat bubble</name>
  <files>libs/ui/src/lib/dino-card/dino-card.html, apps/frontend/src/app/chat/chat.html</files>
  <read_first>
    libs/ui/src/lib/dino-card/dino-card.ts
    libs/ui/src/lib/dino-card/dino-card.html
    apps/frontend/src/app/chat/chat.html
  </read_first>
  <behavior>
    - DinoCard shows the dino's mascot via `<app-mascot [dinoId]="dino.id" [theme]="...">`.
    - The assistant message bubble (or the mascot panel/header) shows the ACTIVE dino's mascot instead of the generic Spino.
  </behavior>
  <action>
  - In dino-card.html, replace the placeholder mascot with `<app-mascot [dinoId]="dino().id" ...>` (use the component's input accessor convention).
  - In chat.html, where the assistant mascot/header renders, bind the active dino id so the chat shows the current dino's species.
  - Ensure Mascot is imported where used (DinoCard imports, ChatComponent imports already include Mascot via MascotPanel — confirm and add if needed).
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint ui frontend --quiet 2>&1 | tail -8</automated>
  </verify>
  <acceptance_criteria>
    - DinoCard and the chat surface render the per-dino mascot
    - Lint passes for ui + frontend
  </acceptance_criteria>
  <done>Each dino visibly shows its own species across picker, Explore, and chat.</done>
</task>

<task type="manual" autonomous="false">
  <name>Task 5: Visual QA across themes</name>
  <files>apps/frontend/src/app/chat/chat.html</files>
  <read_first>apps/frontend/src/app/chat/chat.html</read_first>
  <action>
  `pnpm nx serve frontend`. Toggle day/night. Confirm: every dino in the Explore gallery shows its own crisp pixel-art species in the correct palette; the active chat dino's mascot matches; small sizes stay readable; no smoothing/blur.
  </action>
  <acceptance_criteria>
    - All dino mascots render correctly in both themes at card and bubble sizes
  </acceptance_criteria>
  <done>The dino cast is visually cohesive and on-style in the running app.</done>
</task>

</tasks>

<verification>
- Split/optimize scripts produce day+night assets for every dino source
- Mascot component + DinoCard + chat surface render per-dino species art, crisp, in both themes
- `pnpm nx run-many -t lint --projects=ui,frontend` is green
</verification>

<success_criteria>
- MASC-06..08 satisfied: unique on-style pixel-art species per dino, day/night via the pipeline, integrated in picker/Explore/chat
</success_criteria>

<output>
Create `.planning/phases/20-dino-mascots/20-01-SUMMARY.md` when done
</output>
