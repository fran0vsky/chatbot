---
phase: 09-tool-calling-function-calling
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified:
  - libs/ui/src/lib/tool-call-bubble/tool-call-bubble.ts
  - libs/ui/src/lib/tool-call-bubble/tool-call-bubble.html
  - libs/ui/src/lib/tool-call-bubble/tool-call-bubble.scss
  - libs/ui/src/lib/tool-call-bubble/tool-call-bubble.stories.ts
  - libs/ui/src/index.ts
  - apps/frontend/src/app/chat/chat.ts
  - apps/frontend/src/app/chat/chat.html
autonomous: true
requirements:
  - TOOL-04
  - TOOL-05

must_haves:
  truths:
    - "When the backend returns toolCalls in a chat response, the UI inserts a muted ToolCallBubble for each call between the user prompt and the assistant reply, in order"
    - "ToolCallBubble shows the tool name, arguments (collapsible), and the result text"
    - "Tool-call messages persist across page reloads via HistoryService (they're stored as ChatMessage entries)"
    - "Plain chats (no tools) render identically to before — no regressions"
    - "Tool-call messages reload correctly when switching to a past session from the history panel"
  artifacts:
    - path: "libs/ui/src/lib/tool-call-bubble/tool-call-bubble.ts"
      provides: "Standalone OnPush ToolCallBubble component"
      exports: ["ToolCallBubble"]
    - path: "libs/ui/src/index.ts"
      provides: "Re-export of ToolCallBubble"
    - path: "apps/frontend/src/app/chat/chat.ts"
      provides: "dispatchRequest splices toolCalls into messages before final assistant reply"
    - path: "apps/frontend/src/app/chat/chat.html"
      provides: "Conditional render of app-tool-call-bubble vs app-message-bubble based on message.role"
  key_links:
    - from: "chat.ts dispatchRequest next() handler"
      to: "chat.html @for message rendering"
      via: "messages[] mutation"
      pattern: "messages.splice(insertAt, 0, ...toolMessages)"
---

<objective>
Render the backend's `toolCalls` as distinct, muted message blocks in the chat UI. Add a new `ToolCallBubble` standalone component and wire `ChatComponent` to splice tool messages into the conversation in the correct order. Existing message-bubble behavior is untouched.

Purpose: Make the model's tool use visible so users see what's being fetched (not just the final answer).
Output: New UI component + updated ChatComponent splice logic + updated template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-tool-calling-function-calling/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ToolCallBubble standalone component</name>
  <files>
    libs/ui/src/lib/tool-call-bubble/tool-call-bubble.ts
    libs/ui/src/lib/tool-call-bubble/tool-call-bubble.html
    libs/ui/src/lib/tool-call-bubble/tool-call-bubble.scss
  </files>

  <read_first>
    libs/ui/src/lib/message-bubble/message-bubble.ts
    libs/ui/src/lib/message-bubble/message-bubble.html
    libs/shared-types/src/lib/chat.types.ts
  </read_first>

  <behavior>
    - Standalone component, selector `app-tool-call-bubble`, OnPush change detection
    - Single required `@Input() message: ChatMessage` (assumed to have `role === 'tool'`)
    - Renders a muted parchment-tinted card aligned full-width (not left/right like user/assistant bubbles)
    - Shows: small uppercase label "Tool call · {toolName}", a chevron toggle for arguments, the rendered result text
    - Arguments are hidden by default and revealed when the user clicks the chevron toggle (component-local `expanded: boolean`)
    - Arguments render as `<pre>` JSON-stringified with 2-space indent; falls back to "(no arguments)" when toolArgs is empty or missing
    - Result text renders inside a `<pre>` block with `whitespace-pre-wrap`; falls back to "(no result)" when toolResult is missing
    - Tailwind-only styling; uses existing desert palette tokens (`bg-desert-parchment`, `text-desert-brown-muted`, dark variants) consistent with code-block header in MessageBubble
    - No SCSS rules required — file may be empty or omitted; create empty `tool-call-bubble.scss` only if Angular config requires it (mirror message-bubble structure)
  </behavior>

  <action>
  Create `libs/ui/src/lib/tool-call-bubble/tool-call-bubble.ts`:

  ```
  import {
    ChangeDetectionStrategy,
    ChangeDetectorRef,
    Component,
    Input,
    inject,
  } from '@angular/core';
  import { ChatMessage } from '@org/shared-types';

  @Component({
    standalone: true,
    selector: 'app-tool-call-bubble',
    changeDetection: ChangeDetectionStrategy.OnPush,
    templateUrl: './tool-call-bubble.html',
    styleUrl: './tool-call-bubble.scss',
  })
  export class ToolCallBubble {
    @Input({ required: true }) message!: ChatMessage;

    expanded = false;

    private readonly cdr = inject(ChangeDetectorRef);

    get prettyArgs(): string {
      const args = this.message.toolArgs;
      if (!args || Object.keys(args).length === 0) return '(no arguments)';
      return JSON.stringify(args, null, 2);
    }

    get resultText(): string {
      return this.message.toolResult && this.message.toolResult.length > 0
        ? this.message.toolResult
        : '(no result)';
    }

    toggle(): void {
      this.expanded = !this.expanded;
      this.cdr.markForCheck();
    }
  }
  ```

  Create `libs/ui/src/lib/tool-call-bubble/tool-call-bubble.html`:

  ```
  <div class="my-3 mx-auto w-full max-w-3xl px-3 py-2 rounded-md border border-desert-border dark:border-desert-night-border bg-desert-parchment/60 dark:bg-desert-night-parchment/60 text-sm text-desert-brown-muted dark:text-desert-night-muted">
    <button
      type="button"
      class="flex items-center justify-between w-full font-mono text-xs uppercase tracking-wide focus:outline-none"
      (click)="toggle()"
      [attr.aria-expanded]="expanded"
    >
      <span>Tool call · {{ message.toolName }}</span>
      <span aria-hidden="true">{{ expanded ? '▾' : '▸' }}</span>
    </button>

    @if (expanded) {
      <div class="mt-2">
        <p class="text-[10px] uppercase tracking-wide mb-1 opacity-70">Arguments</p>
        <pre class="text-xs whitespace-pre-wrap font-mono bg-desert-parchment dark:bg-desert-night-parchment p-2 rounded">{{ prettyArgs }}</pre>
      </div>
    }

    <div class="mt-2">
      <p class="text-[10px] uppercase tracking-wide mb-1 opacity-70">Result</p>
      <pre class="text-xs whitespace-pre-wrap font-mono text-desert-brown dark:text-desert-night-text">{{ resultText }}</pre>
    </div>
  </div>
  ```

  Create `libs/ui/src/lib/tool-call-bubble/tool-call-bubble.scss` as an empty file (Angular's `styleUrl` requires the file to exist).
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx build ui --skip-nx-cache 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - All three files exist under `libs/ui/src/lib/tool-call-bubble/`
    - tool-call-bubble.ts declares `@Component({ standalone: true, selector: 'app-tool-call-bubble', changeDetection: ChangeDetectionStrategy.OnPush, ... })`
    - tool-call-bubble.ts has `@Input({ required: true }) message!: ChatMessage`
    - tool-call-bubble.html contains both `{{ prettyArgs }}` and `{{ resultText }}` interpolations and a click handler on the toggle button
    - `pnpm nx build ui` succeeds
  </acceptance_criteria>

  <done>ToolCallBubble component is buildable and ready to import.</done>
</task>

<task type="auto">
  <name>Task 2: Add Storybook story for ToolCallBubble</name>
  <files>libs/ui/src/lib/tool-call-bubble/tool-call-bubble.stories.ts</files>

  <read_first>
    libs/ui/src/lib/message-bubble/message-bubble.stories.ts
  </read_first>

  <behavior>
    - Three stories: GetCurrentTime, WebSearch, EmptyArgs (mirroring `get_current_time` which has no args)
    - Each story passes a fully-formed ChatMessage with role: 'tool' and the appropriate tool fields
    - Component is required by Angular's standalone-component story format consistent with other libs/ui stories
  </behavior>

  <action>
  Mirror the structure of `libs/ui/src/lib/message-bubble/message-bubble.stories.ts`. Create three stories:

  - `GetCurrentTime`: message = `{ text: '', role: 'tool', toolName: 'get_current_time', toolArgs: {}, toolResult: '2026-05-23T14:32:11Z (2026-05-23 14:32 UTC)' }`
  - `WebSearch`: message = `{ text: '', role: 'tool', toolName: 'web_search', toolArgs: { query: 'latest LangGraph release' }, toolResult: '• LangGraph 0.4 released ...' }`
  - `Empty`: message = `{ text: '', role: 'tool', toolName: 'web_search', toolArgs: { query: 'asdfqwerzxcv' }, toolResult: 'No results found for: asdfqwerzxcv' }`

  Use the same `Meta` and `StoryObj` typings as the message-bubble stories file. Wrap the component in a div with `class="p-6 bg-desert-sand dark:bg-desert-night-sand"` so the muted card is visible against the page background.
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx build ui --skip-nx-cache 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File exists at `libs/ui/src/lib/tool-call-bubble/tool-call-bubble.stories.ts`
    - File exports three named stories
    - File imports `ToolCallBubble` from `./tool-call-bubble`
    - Storybook build is unaffected (ui build still passes)
  </acceptance_criteria>

  <done>ToolCallBubble has Storybook coverage matching the other UI primitives.</done>
</task>

<task type="auto">
  <name>Task 3: Re-export ToolCallBubble from libs/ui</name>
  <files>libs/ui/src/index.ts</files>

  <read_first>
    libs/ui/src/index.ts
  </read_first>

  <action>
  Add one line to `libs/ui/src/index.ts` following the existing re-export style (the file already re-exports HeaderBar, HistoryPanel, InputComposer, MessageBubble, ModelSelector). Add:

  ```
  export * from './lib/tool-call-bubble/tool-call-bubble';
  ```

  Preserve every existing export. Do not reorder.
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx build ui --skip-nx-cache 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - libs/ui/src/index.ts contains `export * from './lib/tool-call-bubble/tool-call-bubble';`
    - All previously-listed exports remain
  </acceptance_criteria>

  <done>`import { ToolCallBubble } from '@chatbot/ui'` resolves in the frontend app.</done>
</task>

<task type="auto">
  <name>Task 4: Splice toolCalls into ChatComponent messages</name>
  <files>apps/frontend/src/app/chat/chat.ts</files>

  <read_first>
    apps/frontend/src/app/chat/chat.ts
    apps/frontend/src/app/chat/chat.service.ts
    libs/shared-types/src/lib/chat.types.ts
  </read_first>

  <behavior>
    - When `chatService.sendMessage(...).subscribe` resolves with a response containing `toolCalls`, ChatComponent inserts one ChatMessage with role: 'tool' per tool call BEFORE pushing the final assistant message
    - Insertion preserves the order returned by the backend (chronological)
    - When `toolCalls` is undefined or empty, behavior matches today (just push the assistant message)
    - The error path is unchanged
    - HistoryService receives the full updated messages array including tool entries
    - `ToolCallBubble` is added to the component's `imports` array
  </behavior>

  <action>
  1. Update the import line that pulls from `@chatbot/ui` to include `ToolCallBubble`:
     ```
     import { HeaderBar, HistoryPanel, InputComposer, MessageBubble, ModelSelector, ToolCallBubble } from '@chatbot/ui';
     ```
     Add `ToolCallBubble` to the `imports:` array on the `@Component({...})` decorator.

  2. In `dispatchRequest`, replace the `next:` handler so that before pushing the assistant message it splices any tool calls. The new handler:
     ```
     next: (resp) => {
       if (resp.toolCalls && resp.toolCalls.length > 0) {
         for (const call of resp.toolCalls) {
           this.messages.push({
             text: '',
             role: 'tool',
             toolName: call.name,
             toolArgs: call.args,
             toolResult: call.result,
           });
         }
       }
       this.messages.push({ text: resp.response, role: 'assistant' });
       this.currentRequest = null;
       this.finishRequest();
     },
     ```

  Do NOT change `onSend`, `onRegenerate`, `onEditAndResend`, `onStop`, history-related methods, or theme/placeholder logic. Do NOT change the `error:` handler.
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint frontend --quiet 2>&1 | tail -5</automated>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx build frontend --skip-nx-cache 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - chat.ts imports `ToolCallBubble` from `@chatbot/ui`
    - The component decorator's `imports` array contains `ToolCallBubble`
    - dispatchRequest's next() handler pushes tool messages with `role: 'tool'` before the assistant message
    - The error handler is structurally unchanged
    - frontend lint + build pass
  </acceptance_criteria>

  <done>Tool calls returned by the backend become visible ChatMessage entries in `this.messages`.</done>
</task>

<task type="auto">
  <name>Task 5: Render ToolCallBubble in chat.html</name>
  <files>apps/frontend/src/app/chat/chat.html</files>

  <read_first>
    apps/frontend/src/app/chat/chat.html
  </read_first>

  <behavior>
    - Inside the message list `@for` loop, the rendered element depends on `message.role`:
      - `'tool'` → `<app-tool-call-bubble [message]="message" />`
      - anything else → existing `<app-message-bubble ...>` with current bindings unchanged
    - Typing indicator placement, scroll anchor, and all other layout elements are unchanged
    - All existing Tailwind classes on the wrapping elements are preserved
  </behavior>

  <action>
  Locate the `@for (message of messages; track $index)` block in chat.html (or the equivalent NgFor — match the existing form). Inside the loop body, wrap the existing `<app-message-bubble ... />` element in an `@if / @else` (or `@switch` on `message.role`):

  ```
  @if (message.role === 'tool') {
    <app-tool-call-bubble [message]="message" />
  } @else {
    <!-- existing app-message-bubble element, unchanged -->
  }
  ```

  Preserve the existing bindings on `<app-message-bubble>` exactly — including `[message]`, `[typing]`, `[animate]`, `[canRegenerate]`, `[canEdit]`, `(regenerate)`, `(editSubmit)`, and any structural wrapping element classes that align user vs assistant bubbles.

  Do NOT touch the typing-indicator block, the scroll anchor `#messageEnd`, the header, the input composer, or the sidebar.
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx build frontend --skip-nx-cache 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - chat.html contains `<app-tool-call-bubble` inside the message loop
    - chat.html still contains `<app-message-bubble` with its prior bindings
    - The `#messageEnd` scroll anchor is still present
    - frontend build succeeds
  </acceptance_criteria>

  <done>Tool messages render as ToolCallBubble; user/assistant/error messages render as MessageBubble as before.</done>
</task>

<task type="manual" autonomous="false">
  <name>Task 6: Manual UI smoke test in browser</name>
  <files>apps/frontend/src/app/chat/chat.html</files>

  <read_first>
    apps/frontend/src/app/chat/chat.ts
  </read_first>

  <action>
  1. With `OPENROUTER_API_KEY` set, run `pnpm nx serve backend` and `pnpm nx serve frontend` in parallel terminals.
  2. Open the app, send: "What time is it?". Expect a muted "Tool call · get_current_time" card between your message and the assistant reply, with a chevron that expands to show "(no arguments)".
  3. Send: "What's the latest news on LangGraph?". Expect a "Tool call · web_search" card; expand to see `{ "query": "..." }`; result section shows DDG text.
  4. Refresh the page. Expect the new chat to start fresh (existing behavior).
  5. Open the history panel and click the prior session. Expect tool-call bubbles to reappear in correct positions.
  6. Switch the model selector to a free model and send a plain "Hi". Expect a plain assistant bubble — no tool bubble.
  </action>

  <acceptance_criteria>
    - Tool call card visible between user and assistant messages for both tools
    - Chevron expand/collapse works and shows correctly formatted JSON args
    - Sessions loaded from history retain tool-call bubbles
    - No regression on plain (no-tool) chats or model switching
    - No console errors in the browser DevTools console
  </acceptance_criteria>

  <done>Tool calls are visible, persistent, and don't regress existing chat behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend ChatResponse → Frontend ChatComponent | toolCalls payload is server-controlled; frontend renders it as text |
| ChatMessage.toolResult → DOM | Tool result text is rendered inside `<pre>` (no innerHTML) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-06 | Tampering | toolArgs/toolResult in localStorage | accept | Same trust model as existing message text — user controls their own browser storage |
| T-09-07 | XSS | toolResult rendering | mitigate | Rendered via Angular interpolation inside `<pre>` — no `[innerHTML]`, no markdown pipe — text is auto-escaped |
| T-09-08 | Information Disclosure | toolArgs display | accept | Args are LLM-generated from user prompts; users see their own data |
</threat_model>

<verification>
After all tasks complete:
- `pnpm nx run-many -t lint,build --projects=ui,frontend` passes
- Browser smoke test (Task 6) confirms tool bubbles appear, expand, persist, and don't disrupt plain chats
</verification>

<success_criteria>
- ToolCallBubble component exists, builds, and has Storybook coverage
- ChatComponent inserts tool messages in the correct position
- chat.html renders tool messages via the new component, others via MessageBubble
- Existing chat behavior is unchanged for non-tool turns
</success_criteria>

<output>
Create `.planning/phases/09-tool-calling-function-calling/09-02-SUMMARY.md` when done
</output>
