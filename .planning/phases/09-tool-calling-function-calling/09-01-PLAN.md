---
phase: 09-tool-calling-function-calling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libs/shared-types/src/lib/chat.types.ts
  - apps/backend/src/app/agents/tools/get-current-time.tool.ts
  - apps/backend/src/app/agents/tools/web-search.tool.ts
  - apps/backend/src/app/agents/tools/index.ts
  - apps/backend/src/app/agents/agents.service.ts
autonomous: true
requirements:
  - TOOL-01
  - TOOL-02
  - TOOL-03

must_haves:
  truths:
    - "The LangGraph agent can call `get_current_time` and returns the actual UTC time in the assistant reply"
    - "The LangGraph agent can call `web_search` against DuckDuckGo and incorporates the returned text into its reply"
    - "ChatResponse.toolCalls contains an ordered list of every tool call that happened during the turn, each with name, args, and result"
    - "Multi-turn conversations referencing a previous tool result still work (MemorySaver persistence unchanged)"
    - "Existing chat behavior (no tool needed) returns toolCalls as undefined or empty array — no regression"
  artifacts:
    - path: "libs/shared-types/src/lib/chat.types.ts"
      provides: "MessageRole now includes 'tool'; ChatMessage extended with optional toolName/toolArgs/toolResult; ToolCallRecord interface; ChatResponse.toolCalls optional"
      exports: ["MessageRole", "ChatMessage", "ChatResponse", "ToolCallRecord"]
    - path: "apps/backend/src/app/agents/tools/get-current-time.tool.ts"
      provides: "DynamicStructuredTool for current UTC time"
      exports: ["getCurrentTimeTool"]
    - path: "apps/backend/src/app/agents/tools/web-search.tool.ts"
      provides: "DynamicStructuredTool that calls DuckDuckGo Instant Answer API"
      exports: ["webSearchTool"]
    - path: "apps/backend/src/app/agents/tools/index.ts"
      provides: "Barrel export of all tools as a single array"
      exports: ["tools"]
    - path: "apps/backend/src/app/agents/agents.service.ts"
      provides: "Graph with agent + ToolNode + conditional edge; runAgent returns {response, toolCalls}"
  key_links:
    - from: "agents.service.ts buildGraph"
      to: "tools/index.ts"
      via: "import { tools }"
      pattern: "model.bindTools(tools)"
---

<objective>
Add tool calling to the backend LangGraph agent. Ship two tools (`get_current_time`, `web_search`). Extend shared types so the frontend can render tool call + result blocks. Existing single-turn chat continues to work unchanged when no tool is needed.

Purpose: Let the model fetch real information instead of hallucinating, and expose the work the model did so the user can see it.
Output: Updated backend service with a tools-enabled graph, two tool implementations, and extended shared types.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/09-tool-calling-function-calling/09-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend shared types for tool messages and tool-call records</name>
  <files>libs/shared-types/src/lib/chat.types.ts</files>

  <read_first>
    libs/shared-types/src/lib/chat.types.ts
  </read_first>

  <behavior>
    - MessageRole accepts a new value: 'tool'
    - ChatMessage gains three optional fields: toolName, toolArgs, toolResult
    - A new ToolCallRecord interface is exported with name, args, result
    - ChatResponse gains an optional toolCalls field typed as ToolCallRecord[]
    - All existing fields remain unchanged (text, role on ChatMessage; response on ChatResponse; ChatRequest untouched)
    - libs/shared-types/src/index.ts still re-exports everything correctly
  </behavior>

  <action>
  In `libs/shared-types/src/lib/chat.types.ts`:

  1. Change `export type MessageRole = 'user' | 'assistant' | 'error';` to:
     `export type MessageRole = 'user' | 'assistant' | 'error' | 'tool';`

  2. Extend `ChatMessage` to add three optional fields:
     - `toolName?: string;`
     - `toolArgs?: Record<string, unknown>;`
     - `toolResult?: string;`

  3. Add a new exported interface above `ChatResponse`:
     ```
     export interface ToolCallRecord {
       name: string;
       args: Record<string, unknown>;
       result: string;
     }
     ```

  4. Extend `ChatResponse` to add `toolCalls?: ToolCallRecord[];`

  Do NOT alter `ChatRequest` or `ConversationSession`. Do NOT remove any existing fields.

  Verify `libs/shared-types/src/index.ts` still has `export * from './lib/chat.types';` — no edit needed if so.
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx build shared-types --skip-nx-cache 2>&1 | tail -10</automated>
  </verify>

  <acceptance_criteria>
    - chat.types.ts contains `'tool'` in the MessageRole union
    - chat.types.ts contains `toolName?: string` inside the ChatMessage interface
    - chat.types.ts exports an interface named `ToolCallRecord` with `name`, `args`, `result`
    - chat.types.ts contains `toolCalls?: ToolCallRecord[]` inside ChatResponse
    - `pnpm nx build shared-types` succeeds with no errors
  </acceptance_criteria>

  <done>Shared types compile clean with new tool-related fields available for both backend and frontend.</done>
</task>

<task type="auto">
  <name>Task 2: Implement get_current_time tool</name>
  <files>apps/backend/src/app/agents/tools/get-current-time.tool.ts</files>

  <read_first>
    apps/backend/src/app/agents/agents.service.ts
    apps/backend/package.json
  </read_first>

  <behavior>
    - Exports a single tool named `getCurrentTimeTool` created via `tool()` from `@langchain/core/tools`
    - Tool name (as seen by the LLM): `get_current_time`
    - Description (as seen by the LLM): "Returns the current date and time in UTC. Use this when the user asks for the current time, today's date, or anything time-relative."
    - Zero-argument schema (empty zod object)
    - Returns a string like `"2026-05-23T14:32:11Z (2026-05-23 14:32 UTC)"`
  </behavior>

  <action>
  Create `apps/backend/src/app/agents/tools/get-current-time.tool.ts`:

  ```
  import { tool } from '@langchain/core/tools';
  import { z } from 'zod';

  export const getCurrentTimeTool = tool(
    async () => {
      const now = new Date();
      const iso = now.toISOString().split('.')[0] + 'Z';
      const human =
        now.toISOString().slice(0, 10) +
        ' ' +
        now.toISOString().slice(11, 16) +
        ' UTC';
      return `${iso} (${human})`;
    },
    {
      name: 'get_current_time',
      description:
        "Returns the current date and time in UTC. Use this when the user asks for the current time, today's date, or anything time-relative.",
      schema: z.object({}),
    },
  );
  ```

  If `zod` is not already in `apps/backend/package.json` (it should be, via langchain), add it: `pnpm add zod -w` is acceptable, or pin to the langchain peer.
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint backend --quiet 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File `apps/backend/src/app/agents/tools/get-current-time.tool.ts` exists
    - Exports a `getCurrentTimeTool` constant created via `tool(...)` from `@langchain/core/tools`
    - Tool `name` literal equals `'get_current_time'`
    - Tool `schema` is `z.object({})`
    - `pnpm nx lint backend` passes
  </acceptance_criteria>

  <done>get_current_time tool is implemented and ready to be registered with the agent.</done>
</task>

<task type="auto">
  <name>Task 3: Implement web_search tool (DuckDuckGo)</name>
  <files>apps/backend/src/app/agents/tools/web-search.tool.ts</files>

  <read_first>
    apps/backend/src/app/agents/tools/get-current-time.tool.ts
  </read_first>

  <behavior>
    - Exports a single tool `webSearchTool` named `web_search` to the LLM
    - Description: "Searches the web for fresh, factual information. Use this when the user asks about current events, recent news, or anything that may have changed after your training cutoff."
    - Schema: `{ query: string (1..200) }`
    - On call: GET `https://api.duckduckgo.com/?q={encoded}&format=json&no_html=1&no_redirect=1`
    - Returns a string summary built from `AbstractText` (if present) plus up to 3 `RelatedTopics[].Text` entries (skip groups without `Text`)
    - Returns `"No results found for: {query}"` when both `AbstractText` is empty and `RelatedTopics` produces zero usable items
    - On network error: returns `"Search failed: {error.message}"` instead of throwing
    - Total returned string is hard-capped at 600 characters (truncate with `…`)
  </behavior>

  <action>
  Create `apps/backend/src/app/agents/tools/web-search.tool.ts`:

  ```
  import { tool } from '@langchain/core/tools';
  import { z } from 'zod';

  interface DdgRelatedTopic { Text?: string; Topics?: DdgRelatedTopic[]; }
  interface DdgResponse { AbstractText?: string; RelatedTopics?: DdgRelatedTopic[]; }

  const MAX_LEN = 600;

  function flattenTopics(topics: DdgRelatedTopic[] | undefined, out: string[]): void {
    if (!topics) return;
    for (const t of topics) {
      if (out.length >= 3) return;
      if (t.Text) out.push(t.Text);
      else if (t.Topics) flattenTopics(t.Topics, out);
    }
  }

  export const webSearchTool = tool(
    async ({ query }: { query: string }) => {
      const url = `https://api.duckduckgo.com/?q=${encodeURIComponent(query)}&format=json&no_html=1&no_redirect=1`;
      try {
        const res = await fetch(url, { headers: { 'User-Agent': 'chatbot-web-search/1.0' } });
        if (!res.ok) return `Search failed: HTTP ${res.status}`;
        const data = (await res.json()) as DdgResponse;
        const lines: string[] = [];
        if (data.AbstractText) lines.push(data.AbstractText);
        const related: string[] = [];
        flattenTopics(data.RelatedTopics, related);
        lines.push(...related);
        if (lines.length === 0) return `No results found for: ${query}`;
        const joined = lines.join('\n• ');
        const body = lines.length > 1 ? `• ${joined}` : joined;
        return body.length > MAX_LEN ? body.slice(0, MAX_LEN - 1) + '…' : body;
      } catch (err) {
        const msg = err instanceof Error ? err.message : String(err);
        return `Search failed: ${msg}`;
      }
    },
    {
      name: 'web_search',
      description:
        'Searches the web for fresh, factual information. Use this when the user asks about current events, recent news, or anything that may have changed after your training cutoff.',
      schema: z.object({
        query: z.string().min(1).max(200).describe('The search query'),
      }),
    },
  );
  ```

  Uses global `fetch` (Node 18+ — confirmed since project runs on Node 24).
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint backend --quiet 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File `apps/backend/src/app/agents/tools/web-search.tool.ts` exists
    - Exports `webSearchTool` with `name: 'web_search'`
    - Schema includes a `query` string field
    - Tool body catches network errors and returns a string starting with `"Search failed:"`
    - Tool body returns `"No results found for:"` string when both abstract and related topics are empty
    - `pnpm nx lint backend` passes
  </acceptance_criteria>

  <done>web_search tool calls DuckDuckGo and returns compact text suitable for the model to incorporate.</done>
</task>

<task type="auto">
  <name>Task 4: Add tools barrel export</name>
  <files>apps/backend/src/app/agents/tools/index.ts</files>

  <read_first>
    apps/backend/src/app/agents/tools/get-current-time.tool.ts
    apps/backend/src/app/agents/tools/web-search.tool.ts
  </read_first>

  <action>
  Create `apps/backend/src/app/agents/tools/index.ts`:

  ```
  import { getCurrentTimeTool } from './get-current-time.tool';
  import { webSearchTool } from './web-search.tool';

  export const tools = [getCurrentTimeTool, webSearchTool] as const;
  ```
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint backend --quiet 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - File exists at `apps/backend/src/app/agents/tools/index.ts`
    - Exports a `tools` const containing both `getCurrentTimeTool` and `webSearchTool`
  </acceptance_criteria>

  <done>A single import gives the agent service access to all tools.</done>
</task>

<task type="auto">
  <name>Task 5: Rewire AgentsService graph to use ToolNode + conditional edge</name>
  <files>apps/backend/src/app/agents/agents.service.ts</files>

  <read_first>
    apps/backend/src/app/agents/agents.service.ts
    apps/backend/src/app/agents/tools/index.ts
    libs/shared-types/src/lib/chat.types.ts
  </read_first>

  <behavior>
    - `buildGraph(modelId)` binds tools onto the ChatOpenAI model via `model.bindTools(tools)`
    - Graph topology: `START → agent → (conditional: tools | END) → tools → agent` (loop until no tool call, then END)
    - `MemorySaver` and per-model graph caching behavior are unchanged (one shared checkpointer, one compiled graph per modelId)
    - `runAgent` return type changes to `{ response: string; toolCalls: ToolCallRecord[] }`
    - `toolCalls` contains only calls that occurred in **this turn** — i.e. messages added by `graph.invoke` beyond the input. Implementation: capture `result.messages.length` from a no-op state read before invoke is not possible since MemorySaver hides prior turn count; instead, walk `result.messages` from the END and pull pairs of (AIMessage with tool_calls, ToolMessage) until we hit a HumanMessage. Reverse the result so calls are in chronological order.
    - Existing error handling (`isCapabilityError`, HttpException for 402/429 etc., InternalServerErrorException fallback) is unchanged
    - Existing logger calls are preserved
  </behavior>

  <action>
  Edit `apps/backend/src/app/agents/agents.service.ts`:

  1. Add imports near the top:
     ```
     import { AIMessage, ToolMessage, HumanMessage, BaseMessage } from '@langchain/core/messages';
     import { ToolNode } from '@langchain/langgraph/prebuilt';
     import { ToolCallRecord } from '@org/shared-types';
     import { tools } from './tools';
     ```
     (Keep existing `BaseMessage` import — consolidate so there is a single import line from `@langchain/core/messages`.)

  2. Replace the body of `buildGraph(modelId)` so it:
     - Builds `model` as before, then immediately calls `const boundModel = model.bindTools(tools);`
     - Defines `callModel` using `boundModel.invoke(state.messages)`
     - Defines `toolNode = new ToolNode(tools as unknown as Parameters<typeof ToolNode>[0]);` (or cast `[...tools]` to mutable array — pick what compiles)
     - Defines `shouldContinue(state)`: returns `'tools'` when the last message is an `AIMessage` whose `tool_calls?.length` is > 0, else `END`
     - Builds the graph:
       ```
       return new StateGraph(AgentState)
         .addNode('agent', callModel)
         .addNode('tools', toolNode)
         .addEdge(START, 'agent')
         .addConditionalEdges('agent', shouldContinue, { tools: 'tools', [END]: END })
         .addEdge('tools', 'agent')
         .compile({ checkpointer: this.checkpointer });
       ```

  3. Add a private helper `extractTurnToolCalls(messages: BaseMessage[]): ToolCallRecord[]`:
     - Walk `messages` from the end backward.
     - Stop as soon as a `HumanMessage` is encountered (do not include it).
     - Collect `ToolMessage` and `AIMessage` entries seen along the way.
     - Build a `toolCallId → ToolMessage` map from the collected ToolMessages.
     - For each collected `AIMessage` with a non-empty `tool_calls` array, push one `ToolCallRecord` per call: `{ name: call.name, args: call.args ?? {}, result: String(map.get(call.id ?? '')?.content ?? '') }`.
     - Reverse the final array so calls appear in chronological order.
     - Return the array (empty if none).

  4. Update `runAgent`:
     - Change return type signature to `Promise<{ response: string; toolCalls: ToolCallRecord[] }>`.
     - After `result = await graph.invoke(...)`, compute `const toolCalls = this.extractTurnToolCalls(result.messages);`.
     - Return `{ response, toolCalls }`.

  5. Update `agents.controller.ts`:
     - The signature already returns `ChatResponse`. The richer payload is structurally compatible because `toolCalls` is optional on `ChatResponse`. No edit needed if NestJS does not strip extra fields — verify by reading the file; if there's an explicit DTO mapping, update it.

  Do NOT remove `isCapabilityError`, `InternalServerErrorException`, or the per-model `graphs` cache. Do NOT change the `MemorySaver` strategy.
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint backend --quiet 2>&1 | tail -10</automated>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx run backend:build --skip-nx-cache 2>&1 | tail -15</automated>
  </verify>

  <acceptance_criteria>
    - agents.service.ts imports `ToolNode` from `@langchain/langgraph/prebuilt`
    - agents.service.ts imports `tools` from `./tools`
    - `buildGraph` calls `model.bindTools(tools)`
    - Graph definition contains `.addNode('tools', ...)`, `.addConditionalEdges('agent', ...)`, and `.addEdge('tools', 'agent')`
    - `runAgent` returns an object that includes a `toolCalls` field typed as `ToolCallRecord[]`
    - Existing capability-error handling (402/429/quota check) is still present
    - `pnpm nx lint backend` passes
    - `pnpm nx run backend:build` succeeds
  </acceptance_criteria>

  <done>The agent can route to the ToolNode when the LLM emits tool calls, executes them, loops back, and returns both the final text and the tool-call trace.</done>
</task>

<task type="manual" autonomous="false">
  <name>Task 6: Manual smoke test (requires OPENROUTER_API_KEY)</name>
  <files>apps/backend/src/app/agents/agents.service.ts</files>

  <read_first>
    .env.example
    README.md
  </read_first>

  <action>
  In a terminal with `OPENROUTER_API_KEY` set:

  1. `pnpm nx serve backend`
  2. From another terminal:
     ```
     curl -X POST http://localhost:3000/api/agents/chat \
       -H "Content-Type: application/json" \
       -d '{"message":"What time is it right now?","threadId":"smoke-1","model":"openai/gpt-4o-mini"}'
     ```
     Expect: `response` mentions the time, `toolCalls` contains one entry with `name: "get_current_time"`.
  3. Then:
     ```
     curl -X POST http://localhost:3000/api/agents/chat \
       -H "Content-Type: application/json" \
       -d '{"message":"What is the latest news about LangGraph?","threadId":"smoke-2","model":"openai/gpt-4o-mini"}'
     ```
     Expect: `toolCalls` contains one entry with `name: "web_search"` and a non-empty `result`.
  4. Follow-up turn on `smoke-1`:
     ```
     curl ... -d '{"message":"Repeat that time exactly.","threadId":"smoke-1","model":"openai/gpt-4o-mini"}'
     ```
     Expect: assistant references the prior time (MemorySaver works across tool calls).
  </action>

  <acceptance_criteria>
    - All three curl calls return 200
    - First call's response payload has `toolCalls[0].name === 'get_current_time'`
    - Second call's response payload has `toolCalls[0].name === 'web_search'`
    - Third call demonstrates the model recalls the earlier tool result
  </acceptance_criteria>

  <done>Backend tool calling verified end-to-end against a live OpenRouter call.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User → LLM → web_search input | The `query` arg is LLM-generated but ultimately driven by user text |
| web_search → DuckDuckGo API | Outbound HTTP to a third party |
| DuckDuckGo response → LLM context | Third-party text is concatenated into the model's next prompt |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | web_search query | mitigate | zod schema enforces 1..200 chars; `encodeURIComponent` prevents URL injection |
| T-09-02 | Denial of Service | web_search fetch | accept | Single outbound call per tool invocation; failures return a string instead of throwing, keeping the agent loop alive |
| T-09-03 | Information Disclosure | DuckDuckGo response → LLM | accept | DDG Instant Answer endpoint returns public summaries only — no auth, no PII; treated as untrusted text the LLM may surface to the user |
| T-09-04 | Elevation of Privilege | Prompt injection via search results | accept-for-v1 | Search result text is read by the LLM and could include injection attempts; out of scope for v1 starter tooling — flagged as deferred follow-up |
| T-09-05 | Spoofing | Outbound network | accept | HTTPS endpoint pinned; no auth secret transmitted |
</threat_model>

<verification>
After all tasks complete:
- `pnpm nx run-many -t lint,build --projects=shared-types,backend` passes
- Backend smoke test (Task 6) confirms both tools are invoked end-to-end and tool calls round-trip through MemorySaver
- ChatResponse payload now includes `toolCalls: ToolCallRecord[]` for tool-using turns and an empty array (or absent field) for plain turns
</verification>

<success_criteria>
- All five backend files exist with the structure described
- Shared types build is green and exports the new symbols
- Backend lint + build are green
- Tool calls observable in the JSON response from `/api/agents/chat`
</success_criteria>

<output>
Create `.planning/phases/09-tool-calling-function-calling/09-01-SUMMARY.md` when done
</output>
