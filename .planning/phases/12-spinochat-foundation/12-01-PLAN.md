---
plan_id: 12-01
phase: 12
title: Branding text swap — SpinoChat naming + tagline
wave: 1
depends_on: []
requirements: [BRAND-01, BRAND-02, BRAND-03]
files_modified:
  - apps/frontend/src/index.html
  - apps/frontend/src/app/chat/chat.ts
  - libs/ui/src/lib/header-bar/header-bar.html
  - README.md
  - CLAUDE.md
autonomous: true
type: execute
---

# Plan 12-01: Branding Text Swap

## Objective

Replace all visible "Chatbot" naming with "SpinoChat" (long form) / "Spino" (short form),
add the tagline "The AI that survived" to the landing/empty state, and update project docs.
This is a pure copy/string swap — no palette or mascot work here.

## Tasks

<task id="12-01-01" wave="1">
  <name>Swap header title from "Chatbot" to "SpinoChat"</name>
  <read_first>
    - libs/ui/src/lib/header-bar/header-bar.html (current state — header `<h1>` reads "Chatbot")
  </read_first>
  <action>
    In libs/ui/src/lib/header-bar/header-bar.html line 22, replace the literal text
    `Chatbot` inside the `<h1>` element with `SpinoChat`. Keep all surrounding Tailwind
    classes and the `font-title` styling unchanged.
  </action>
  <acceptance_criteria>
    - libs/ui/src/lib/header-bar/header-bar.html contains `>SpinoChat<` exactly once
    - libs/ui/src/lib/header-bar/header-bar.html does NOT contain the string `Chatbot`
    - The `<h1>` still carries the classes `font-title text-lg font-semibold uppercase`
  </acceptance_criteria>
</task>

<task id="12-01-02" wave="1">
  <name>Update browser tab title and meta description in index.html</name>
  <read_first>
    - apps/frontend/src/index.html (current `<title>frontend</title>`, no meta description)
  </read_first>
  <action>
    In apps/frontend/src/index.html:
    1. Replace `<title>frontend</title>` with `<title>SpinoChat — The AI that survived</title>`
    2. Add immediately after the `<title>` line:
       `<meta name="description" content="SpinoChat — the AI that survived. Ask anything, keep the conversation going." />`
    Leave all other tags (viewport, charset, preconnect, fonts, favicon, theme bootstrap script) unchanged.
  </action>
  <acceptance_criteria>
    - apps/frontend/src/index.html contains `<title>SpinoChat — The AI that survived</title>`
    - apps/frontend/src/index.html contains a `<meta name="description"` tag whose content includes `SpinoChat` and `survived`
    - File does NOT contain `<title>frontend</title>`
  </acceptance_criteria>
</task>

<task id="12-01-03" wave="1">
  <name>Update landing-state greeting message with tagline</name>
  <read_first>
    - apps/frontend/src/app/chat/chat.ts (lines 39 and 193 — both contain the initial assistant message string)
    - apps/frontend/src/app/chat/chat.html (lines 24-31 — landing state renders messages[0] as a MessageBubble when messages.length === 1)
  </read_first>
  <action>
    In apps/frontend/src/app/chat/chat.ts, change the initial assistant message text on
    both line 39 (field initializer) AND line 193 (newChat reset) from
    `'Hello! How can I assist you today?'`
    to
    `'Welcome to SpinoChat — the AI that survived. What can I help you with?'`

    Keep the rest of the object literal (`role: 'assistant'`) identical. The tagline
    "The AI that survived" lives inside this greeting so it appears in the landing
    state via the existing `messages[0]` MessageBubble render (chat.html:29).
  </action>
  <acceptance_criteria>
    - apps/frontend/src/app/chat/chat.ts contains the exact substring `the AI that survived` (lowercased 't', em-dash-joined inside the greeting)
    - apps/frontend/src/app/chat/chat.ts contains `SpinoChat` at least once
    - apps/frontend/src/app/chat/chat.ts does NOT contain `Hello! How can I assist you today?`
    - At runtime, opening the app on a fresh session shows the tagline text inside the assistant bubble above the input composer
  </acceptance_criteria>
</task>

<task id="12-01-04" wave="1">
  <name>Update README.md project description to SpinoChat</name>
  <read_first>
    - README.md (top-of-file title and one-line description)
  </read_first>
  <action>
    Update README.md so that:
    - The top-level H1 title reads `# SpinoChat` (or `# SpinoChat — The AI that survived`)
    - The first descriptive paragraph names the project "SpinoChat" instead of "Chatbot"
    - References to the workspace folder name (Nx monorepo path) may stay as-is — only update
      product naming, not filesystem identifiers
  </action>
  <acceptance_criteria>
    - README.md first H1 line contains `SpinoChat`
    - README.md first descriptive paragraph contains `SpinoChat`
    - README.md does NOT describe the project as "a general-purpose chatbot" without the SpinoChat name
  </acceptance_criteria>
</task>

<task id="12-01-05" wave="1">
  <name>Update CLAUDE.md project description to SpinoChat</name>
  <read_first>
    - CLAUDE.md (current ## Project section names the project "Chatbot" and describes it as a "general-purpose text chatbot")
  </read_first>
  <action>
    In CLAUDE.md, under `## Project`:
    - Change the bolded project name from `**Chatbot**` to `**SpinoChat**`
    - Update the descriptive sentence to name the project "SpinoChat" while preserving
      the rest of the architecture description (Nx monorepo with Angular frontend and
      NestJS backend, LangGraph orchestration, OpenRouter LLM provider).
    Do not touch any other section (constraints, GSD workflow, developer profile).
  </action>
  <acceptance_criteria>
    - CLAUDE.md contains `**SpinoChat**` under the `## Project` heading
    - CLAUDE.md `## Project` section still describes the Angular + NestJS + LangGraph stack
    - CLAUDE.md does NOT contain `**Chatbot**` as the project name marker
  </acceptance_criteria>
</task>

## Verification

After all tasks complete:
1. Run `pnpm nx build frontend` — must succeed with no template errors
2. Run `pnpm nx run frontend:serve` and open the app — header reads `SpinoChat`, browser tab reads `SpinoChat — The AI that survived`, landing-state greeting contains `the AI that survived`
3. `grep -r "Chatbot" apps/frontend/src libs/ui/src` returns no product-name hits (workspace package names like `@chatbot/ui` are fine — those are filesystem identifiers, not product naming)

## must_haves

- BRAND-01: header title, browser tab title, meta description, landing-state copy all read SpinoChat/Spino
- BRAND-02: tagline "The AI that survived" appears in landing-state assistant bubble
- BRAND-03: README.md and CLAUDE.md project description updated to SpinoChat
