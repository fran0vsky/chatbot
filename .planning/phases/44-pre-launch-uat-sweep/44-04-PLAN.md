---
phase: 44-pre-launch-uat-sweep
plan: 04
type: execute
wave: 3
depends_on: [03]
autonomous: false
files_modified:
  - TBD (determined by the blocker list from Plan 03)
requirements:
  - UAT-01
must_haves:
  truths:
    - "Every blocker in the runbook's 'Blockers for Plan 04' section is either fixed in code and re-verified PASS on https://dinoagents.duckdns.org, or explicitly downgraded to minor/backlog with a written rationale — none left in an undecided state"
    - "Each fix is a real code change with a passing local gate (lint + build + relevant test) before it ships to prod for re-verification"
    - "After remediation, the five first-touch features all pass a happy-path run on prod (Roadmap SC#2 holds true post-fix)"
  artifacts:
    - path: ".planning/phases/44-pre-launch-uat-sweep/44-PROD-UAT-RUNBOOK.md"
      provides: "Each blocker updated to FIXED+re-verified or DOWNGRADED with rationale"
      exports: []
  key_links:
    - from: "each blocker in 'Blockers for Plan 04'"
      to: "a code fix + prod re-verification"
      via: "diagnose → fix → local gate (lint/build/test) → ship → re-run that check on prod"
      pattern: "contingent-remediation — concrete tasks are filled in from Plan 03's blocker list at execution time"
---

<objective>
Close out the blockers the prod sweep surfaced. For each item in the runbook's
"Blockers for Plan 04" section: diagnose, fix in code, pass the local gate, ship, and re-verify on
https://dinoagents.duckdns.org — or, if on inspection it is not truly launch-blocking, downgrade it
to the backlog with a written reason. This plan is **contingent**: its concrete tasks are unknown
until Plan 03 runs, so it defines the remediation *process* and exit bar rather than pre-named fixes.
It is `autonomous: false` because which blockers exist (and whether each is fixed vs downgraded) is a
human-reviewed decision.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/44-pre-launch-uat-sweep/44-PROD-UAT-RUNBOOK.md
The "## Blockers for Plan 04" section (appended by Plan 03 Task 3) is the input list for this plan.
Deploy path / prod truth: project memory `project_deployment_vm` — VM `spinochat-backend`, HTTPS via Caddy container at https://dinoagents.duckdns.org; see [.planning/ROADMAP.md](../../ROADMAP.md) deploy notes.
Local gate conventions: per-phase plans use `npx nx lint @org/backend`, `npx nx build @org/backend`, `npx nx test @org/backend` (and `frontend` equivalents). Windows env caveats: see project memory `env_windows_lowercase_drive`.
Backlog for downgrades: [.planning/STATE.md](../../STATE.md) `### Pending Todos`.
</context>

<design_decisions>
- **Contingent scope:** `files_modified` and concrete tasks cannot be enumerated before Plan 03. At execution, read the blocker list and create one remediation task per blocker (or run them as a checklist), updating `files_modified` to the actual files touched.
- **Fix-or-downgrade exit bar:** every blocker must reach a terminal state — FIXED+re-verified or DOWNGRADED (with rationale + backlog entry). No blocker may stay open at phase verification.
- **Local gate before prod:** each fix passes lint/build/relevant-test locally before shipping, then is re-verified by re-running that specific runbook check on prod.
- **Zero-blocker fast path:** if Plan 03 found no blockers, this plan is a no-op — record "no blockers; nothing to remediate" in the summary and pass.
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Enumerate blockers and create the remediation checklist</name>
  <files>.planning/phases/44-pre-launch-uat-sweep/44-PROD-UAT-RUNBOOK.md</files>
  <read_first>.planning/phases/44-pre-launch-uat-sweep/44-PROD-UAT-RUNBOOK.md</read_first>
  <behavior>
    - Read the "## Blockers for Plan 04" section. For each blocker, decide fix vs downgrade.
    - If zero blockers: record the no-op and skip to verification.
  </behavior>
  <action>
  Parse the blocker list from the runbook and, for each, note the affected feature, suspected files,
  and the chosen disposition (fix or downgrade). This produces the concrete work list the next task executes.
  </action>
  <acceptance_criteria>
    - Each blocker has a disposition recorded (fix / downgrade)
    - If zero blockers, that is explicitly recorded
  </acceptance_criteria>
  <done>The contingent scope is now concrete.</done>
</task>

<task type="manual">
  <name>Task 2: Fix each blocker (or downgrade) and re-verify on prod</name>
  <files>TBD (per blocker — update this list at execution)</files>
  <read_first>
    .planning/phases/44-pre-launch-uat-sweep/44-PROD-UAT-RUNBOOK.md
    .planning/ROADMAP.md
  </read_first>
  <behavior>
    - For each "fix" blocker: implement the code change, run the local gate (lint + build + relevant test) green, ship to prod, then re-run that runbook check on https://dinoagents.duckdns.org and mark it FIXED when it passes.
    - For each "downgrade" blocker: write the rationale in the runbook and add a Pending Todos / deferred-items entry.
    - Re-confirm the five first-touch features still pass after fixes.
  </behavior>
  <action>
  Implement and ship each fix, gating locally before prod re-verification; downgrade the rest with a
  written reason and a backlog entry. Update the runbook so every blocker is FIXED or DOWNGRADED, and
  re-run the five Tier-0 checks to confirm no regression.
  </action>
  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && npx nx run-many -t lint,build --projects=@org/backend,frontend 2>&1 | tail -15</automated>
  </verify>
  <acceptance_criteria>
    - Every blocker in the runbook is marked FIXED (re-verified PASS on prod) or DOWNGRADED (rationale + backlog entry)
    - Local lint + build pass for every touched project
    - The five first-touch features pass a post-fix happy-path run on prod
  </acceptance_criteria>
  <done>No launch-blocking defect remains open; the app is ready to show real users.</done>
</task>

</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|------------|
| T-44-04-01 | Regression from rushed fixes | Code changed to clear blockers | mitigate | Each fix passes the local lint/build/test gate before shipping and is re-verified on prod; the five first-touch features are re-run post-fix to catch regressions. |
| T-44-04-02 | Scope creep — fixing non-blockers | Contingent remediation | mitigate | Only items in the runbook's "Blockers for Plan 04" section are in scope; everything else is backlogged. Downgrades are explicit and recorded. |
</threat_model>

<verification>
- Runbook shows every blocker terminal (FIXED+re-verified or DOWNGRADED with rationale).
- `npx nx run-many -t lint,build --projects=@org/backend,frontend` green for touched projects.
- The five first-touch features pass a post-fix happy-path run on prod.
</verification>

<success_criteria>
- Roadmap Phase 44 SC#1 (fix half): blocker defects from the sweep are fixed in-phase; minors filed to backlog — UAT-01.
- Roadmap Phase 44 SC#2 holds after remediation: the five first-touch features run clean on the live site — UAT-01.
</success_criteria>

<output>
Create `.planning/phases/44-pre-launch-uat-sweep/44-04-SUMMARY.md` when done
</output>
