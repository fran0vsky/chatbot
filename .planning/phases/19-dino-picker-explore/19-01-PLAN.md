---
phase: 19-dino-picker-explore
plan: 01
type: execute
wave: 1
depends_on:
  - 18-01
files_modified:
  - apps/frontend/src/app/chat/dino.service.ts
  - apps/frontend/src/app/chat/dino.service.spec.ts
  - apps/frontend/src/app/chat/chat.service.ts
  - apps/frontend/src/app/chat/chat.ts
  - apps/frontend/src/app/chat/chat.html
  - libs/ui/src/lib/dino-card/dino-card.ts
  - libs/ui/src/lib/dino-card/dino-card.html
  - libs/ui/src/lib/dino-card/dino-card.stories.ts
  - libs/ui/src/lib/dino-picker/dino-picker.ts
  - libs/ui/src/lib/dino-picker/dino-picker.html
  - libs/ui/src/lib/dino-picker/dino-picker.stories.ts
  - libs/ui/src/index.ts
autonomous: true
requirements:
  - PICK-01
  - PICK-02
  - PICK-03
  - PICK-04
  - UX-01

must_haves:
  truths:
    - "Starting a new chat presents a dino picker; the chosen dino is bound to the new session and sent as dinoId on every message"
    - "The model dropdown (ModelSelector) is no longer rendered anywhere in the chat UI"
    - "The Explore view lists every dino fetched from GET /api/dinos with name, persona/blurb, specialty, and toolset"
    - "Selecting a dino from Explore starts (or retargets) a chat with that dino"
    - "The active dino's name (and mascot slot) is shown in the chat header"
    - "The footer is pinned to the bottom of the viewport at all screen sizes (no floating gap on short content)"
  artifacts:
    - path: "apps/frontend/src/app/chat/dino.service.ts"
      provides: "Fetches and caches the dino roster from GET /api/dinos"
      exports: ["DinoService"]
    - path: "libs/ui/src/lib/dino-card/dino-card.ts"
      provides: "Presentational dino card (name, species, persona, specialty, tools)"
      exports: ["DinoCard"]
    - path: "libs/ui/src/lib/dino-picker/dino-picker.ts"
      provides: "Presentational grid of dino cards with a (dinoSelected) output"
      exports: ["DinoPicker"]
  key_links:
    - from: "chat.service.ts streamMessage"
      to: "backend /api/agents/chat"
      via: "ChatRequest.dinoId"
      pattern: "body.dinoId = activeDinoId"
    - from: "chat.ts"
      to: "DinoService"
      via: "inject + load roster on init"
      pattern: "this.dinoService.loadDinos()"
---

<objective>
Make choosing a dino replace choosing a model. Fetch the roster from `GET /api/dinos`, present a dino picker when starting a new chat, render the Explore view as a gallery of dinos, send the chosen `dinoId` with every message, show the active dino in the header, and remove the model dropdown. Also pin the footer to the bottom (UX-01). Mascots remain the existing Spino placeholder until Phase 20 supplies per-species art.

Purpose: This is the visible product pivot — users pick a characterful agent, not a raw model.
Output: A DinoService, two presentational UI components (DinoCard, DinoPicker), updated ChatComponent wiring, and the model selector removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/18-dino-abstraction/18-01-PLAN.md
The frontend is a single `ChatComponent` (apps/frontend/src/app/chat/chat.ts) with NO router. It switches screens via `activeView = signal<'chats' | 'explore' | 'knowledge'>`. It already has `pickModelFromExplore()` and a `models` array + `selectedModel` and `availableTools` + `enabledToolNames`. History is localStorage via HistoryService. ModelSelector is imported from `@chatbot/ui` and used in chat.html. ConversationSession (shared type) currently has no dino field.
</context>

<design_decisions>
- Keep the `activeView` signal pattern (no router). Add the dino picker as an overlay/panel shown when starting a new chat, and turn the existing `explore` view into the dino gallery.
- Bind a dino to a session: add `dinoId?: string` to `ConversationSession` (shared type) and persist it via HistoryService. The ChatComponent tracks `activeDinoId` and sends it on each request.
- Remove `selectedModel`/`models`/ModelSelector and the per-message tool toggle UI is now driven by the dino (tools are server-gated). Keep `availableTools` only if still used for display; otherwise remove. (Tools are now the dino's responsibility — DINO-04.)
- DinoCard and DinoPicker are PRESENTATIONAL (no injected services, get a Storybook story) per project component rules. The chat component (smart) injects DinoService.
</design_decisions>

<tasks>

<task type="auto">
  <name>Task 1: Add dinoId to ConversationSession + DinoService</name>
  <files>libs/shared-types/src/lib/chat.types.ts, apps/frontend/src/app/chat/dino.service.ts, apps/frontend/src/app/chat/dino.service.spec.ts</files>

  <read_first>
    libs/shared-types/src/lib/chat.types.ts
    apps/frontend/src/app/chat/chat.service.ts
    apps/frontend/src/app/chat/history.service.ts
    apps/frontend/src/app/environments/environment.ts
  </read_first>

  <behavior>
    - `ConversationSession` gains optional `dinoId?: string`.
    - `DinoService` (providedIn: 'root') fetches `GET {apiUrl}/api/dinos` once, caches the `DinoSummary[]`, and exposes a signal or promise accessor plus a `getById(id)` helper. Uses HttpClient (per Angular rules — no raw fetch in components; this is a service so HttpClient is the convention).
    - Graceful failure: on network error, expose an empty roster and a loaded/error flag; do not throw into the component.
  </behavior>

  <action>
  - In chat.types.ts add `dinoId?: string;` to `ConversationSession`.
  - Create `dino.service.ts`:
    ```ts
    import { Injectable, inject, signal } from '@angular/core';
    import { HttpClient } from '@angular/common/http';
    import { DinoSummary } from '@org/shared-types';
    import { environment } from '../../environments/environment';

    @Injectable({ providedIn: 'root' })
    export class DinoService {
      private readonly http = inject(HttpClient);
      readonly dinos = signal<DinoSummary[]>([]);
      readonly loaded = signal(false);

      loadDinos(): void {
        this.http.get<DinoSummary[]>(`${environment.apiUrl}/api/dinos`).subscribe({
          next: (list) => { this.dinos.set(list); this.loaded.set(true); },
          error: () => { this.dinos.set([]); this.loaded.set(true); },
        });
      }

      getById(id: string | undefined): DinoSummary | undefined {
        return this.dinos().find((d) => d.id === id);
      }
    }
    ```
  - Add `dino.service.spec.ts` using `HttpClientTestingModule`/`provideHttpClientTesting` to assert roster load and error fallback.
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx test frontend --skip-nx-cache 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - `ConversationSession` has optional `dinoId`
    - DinoService loads the roster via HttpClient and exposes it as a signal
    - On HTTP error the roster is empty and `loaded` becomes true (no throw)
    - dino.service.spec passes
  </acceptance_criteria>

  <done>The frontend can retrieve and cache the dino roster, and sessions can remember their dino.</done>
</task>

<task type="auto">
  <name>Task 2: DinoCard + DinoPicker presentational components</name>
  <files>libs/ui/src/lib/dino-card/*, libs/ui/src/lib/dino-picker/*, libs/ui/src/index.ts</files>

  <read_first>
    libs/ui/src/lib/model-selector/model-selector.ts
    libs/ui/src/lib/model-selector/model-selector.stories.ts
    libs/ui/src/lib/mascot/mascot.ts
    libs/ui/src/index.ts
  </read_first>

  <behavior>
    - `DinoCard`: standalone, OnPush, selector `app-dino-card`. Inputs: `dino: DinoSummary`, `active?: boolean`. Output: `selected = output<DinoSummary>()` (or EventEmitter per existing convention — match model-selector). Renders name, species, persona, specialty, and tool chips. Mascot slot uses the existing Mascot/placeholder for now (Phase 20 swaps real art keyed by species).
    - `DinoPicker`: standalone, OnPush, selector `app-dino-picker`. Inputs: `dinos: DinoSummary[]`, `activeDinoId?: string`. Output: `dinoSelected`. Renders a responsive grid of DinoCard. Tailwind only; no services.
    - Both get a `.stories.ts` (presentational rule). Export both from libs/ui/src/index.ts.
  </behavior>

  <action>
  - Build the two components matching the existing component style (external .html, OnPush, `app-` selector, Tailwind classes consistent with the desert/jungle tokens used elsewhere — reuse classes seen in model-selector/history-panel).
  - Tool chips: iterate `dino.toolNames`.
  - Add Storybook stories with 2–3 sample DinoSummary fixtures.
  - Add exports to libs/ui/src/index.ts following the existing `export { X } from './lib/.../x.js';` pattern.
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint ui --quiet 2>&1 | tail -8</automated>
  </verify>

  <acceptance_criteria>
    - `app-dino-card` and `app-dino-picker` exist, standalone + OnPush, Tailwind-only, no injected services
    - Each has a Storybook story
    - Both exported from libs/ui/src/index.ts
    - `pnpm nx lint ui` passes
  </acceptance_criteria>

  <done>Reusable, story-backed presentational building blocks for the picker and Explore gallery.</done>
</task>

<task type="auto">
  <name>Task 3: Wire ChatComponent to dinos; remove model dropdown</name>
  <files>apps/frontend/src/app/chat/chat.ts, apps/frontend/src/app/chat/chat.service.ts, apps/frontend/src/app/chat/chat.html</files>

  <read_first>
    apps/frontend/src/app/chat/chat.ts
    apps/frontend/src/app/chat/chat.html
    apps/frontend/src/app/chat/chat.service.ts
  </read_first>

  <behavior>
    - chat.service.ts `streamMessage` accepts `dinoId` and includes it in the `ChatRequest` body (drop the obligation to send model; model may be omitted now).
    - chat.ts: add `activeDinoId` (signal), load roster in ngOnInit via DinoService. Replace `selectedModel`/`models`/`pickModelFromExplore` with dino equivalents: `activeDinoId`, `pickDino(dino)`, and a `startNewChatWith(dino)` flow.
    - "New chat" opens the dino picker (set a `pickerOpen` signal / route to a picker overlay) instead of immediately starting; once a dino is chosen, start the new chat bound to it and persist `dinoId` on the session.
    - The `explore` view renders `app-dino-picker` with the roster; choosing a dino there starts/retargets a chat.
    - Header shows the active dino's name + mascot slot (PICK-04).
    - Remove `ModelSelector` from `imports` and from chat.html. Remove the model `models` array and `selectedModel`. If the per-message tool toggle popover was tied to `availableTools`/`enabledToolNames`, remove or hide it (tools are now dino-gated server-side); keep code only if still meaningful.
    - On send, pass `this.activeDinoId()` to `streamMessage`.
  </behavior>

  <action>
  - chat.service.ts: change signature to `streamMessage(message, dinoId, signal, enabledTools?)` (drop `model` or make it optional/last). Body becomes `{ message, threadId, dinoId, enabledTools }`. Update the type usage.
  - chat.ts:
    - `import { DinoService }`; `private readonly dinoService = inject(DinoService);`
    - `readonly activeDinoId = signal<string | undefined>(undefined);`
    - `readonly pickerOpen = signal(false);`
    - ngOnInit: `this.dinoService.loadDinos();`
    - Replace `pickModelFromExplore(modelId)` with `pickDino(dino: DinoSummary)` that sets `activeDinoId`, starts a new chat bound to it, and sets `activeView('chats')`.
    - `newChat()` now opens the picker: `this.pickerOpen.set(true)` (or sets activeView to a picker view). After selection, run the existing startNewChat path and store `dinoId` on the session via HistoryService.
    - `switchToSession`: restore `activeDinoId` from `session.dinoId`.
    - `onSend`/`dispatchRequest`: pass `this.activeDinoId()` instead of `selectedModel`.
    - `saveCurrentSession`/`finishRequest`/`onStop`: include `dinoId: this.activeDinoId()` in the upserted session object.
  - chat.html: remove `<app-model-selector .../>`; add the picker overlay (`@if (pickerOpen()) { <app-dino-picker ... /> }`) and render `<app-dino-picker>` inside the explore view; show the active dino name in the header area.
  - Remove `ModelSelector` from the component `imports` array; add `DinoPicker` (and `DinoCard` if used directly).
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint frontend --quiet 2>&1 | tail -8</automated>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx test frontend --skip-nx-cache 2>&1 | tail -20</automated>
  </verify>

  <acceptance_criteria>
    - No reference to `ModelSelector`, `selectedModel`, or a `models` array remains in chat.ts/chat.html
    - `streamMessage` sends `dinoId`; the active dino drives the request
    - New chat opens the picker; choosing a dino binds it to the session and persists `dinoId`
    - Explore view renders the dino gallery; selecting a dino starts/retargets a chat
    - Header shows the active dino's name
    - `pnpm nx lint frontend` and `pnpm nx test frontend` pass
  </acceptance_criteria>

  <done>The model dropdown is gone; the app is dino-driven end to end.</done>
</task>

<task type="auto">
  <name>Task 4: Pin the footer to the bottom (UX-01)</name>
  <files>apps/frontend/src/app/chat/chat.html, apps/frontend/src/app/chat/chat.scss</files>

  <read_first>
    apps/frontend/src/app/chat/chat.html
    apps/frontend/src/app/chat/chat.scss
  </read_first>

  <behavior>
    - The footer sits at the bottom of the viewport regardless of content height; it never floats mid-screen on short pages and never overlaps the composer.
    - Use the existing flex column layout (the chat shell is already `h-screen` flex). Ensure the main content area is `flex-1` and the footer is a non-shrinking final row, or use `mt-auto` on the footer within the flex column.
  </behavior>

  <action>
  Identify the footer element in chat.html. Apply a layout fix using Tailwind utilities only:
  - Ensure the column wrapper is `flex flex-col min-h-screen` (or h-screen as already present).
  - Give the scrollable content `flex-1` and the footer `mt-auto shrink-0` so it's pushed to the bottom.
  Do not introduce inline styles; Tailwind utilities only (project rule).
  </action>

  <verify>
    <automated>cd C:\Users\Franek\Desktop\coding_projects\Chatbot && pnpm nx lint frontend --quiet 2>&1 | tail -5</automated>
  </verify>

  <acceptance_criteria>
    - Footer is pinned to the bottom via flex (`mt-auto`/`flex-1`) — verified by reading the markup
    - No inline styles added; Tailwind only
    - `pnpm nx lint frontend` passes
  </acceptance_criteria>

  <done>The footer is anchored at the bottom on all viewport sizes.</done>
</task>

<task type="manual" autonomous="false">
  <name>Task 5: Manual UX check</name>
  <files>apps/frontend/src/app/chat/chat.ts</files>
  <read_first>apps/frontend/src/app/chat/chat.ts</read_first>
  <action>
  Run `pnpm nx serve frontend` (backend serving /api/dinos too). Verify:
  1. New chat → picker shows >=4 dinos; choosing one starts the chat and the header shows that dino.
  2. Sending a message gets a response in that dino's voice; switching dinos via Explore changes the persona.
  3. No model dropdown anywhere.
  4. Footer sits at the bottom on a near-empty chat and after a long conversation.
  </action>
  <acceptance_criteria>
    - Picker, Explore gallery, header dino, and dino-driven responses all work
    - Model dropdown absent; footer pinned
  </acceptance_criteria>
  <done>The dino picker + Explore experience is verified in the running app.</done>
</task>

</tasks>

<threat_model>
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation |
|-----------|----------|-----------|-------------|-----------|
| T-19-01 | Tampering | dinoId from client | accept | Backend validates via getDino fallback (Phase 18); unknown id → default |
| T-19-02 | DoS | /api/dinos fetch | accept | Cached once; error path yields empty roster, UI degrades gracefully |
</threat_model>

<verification>
- `pnpm nx run-many -t lint,test --projects=shared-types,ui,frontend` is green
- Model dropdown removed; dino picker + Explore gallery functional; header shows active dino; footer pinned
- Each message sends `dinoId`; responses reflect the dino persona
</verification>

<success_criteria>
- PICK-01..04 + UX-01 satisfied
- ModelSelector fully removed from the chat surface
- DinoCard/DinoPicker are presentational with stories
- Lint + tests green
</success_criteria>

<output>
Create `.planning/phases/19-dino-picker-explore/19-01-SUMMARY.md` when done
</output>
